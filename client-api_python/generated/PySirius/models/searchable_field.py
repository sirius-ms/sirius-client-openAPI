# coding: utf-8

"""
    SIRIUS Nightsky API


    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from PySirius.models.searchable_field_type import SearchableFieldType
from typing import Optional, Set
from typing_extensions import Self

class SearchableField(BaseModel):
    """
    Describes one field of the search index that can be used in lucene search queries (searchQuery parameter).  Use this information to build valid queries, e.g. which fields support range queries ([300 TO 400])  and which support word based (full text) search.
    """ # noqa: E501
    name: StrictStr = Field(description="Name of the field to be used in search queries, e.g. <code>ionMass:[300 TO 400]</code>.  Nested fields are addressed with dot notation. Dynamic, map-like fields are reported as concrete,  directly usable field names - one entry per key currently present in the project index - rather  than a wildcard template, e.g. <code>tags.&lt;tagName&gt;</code>, an element symbol in  <code>...molecularFormula.&lt;element&gt;</code>, or a database id in  <code>topAnnotations.matchedDatabases.&lt;dbId&gt;</code>.")
    field_type: SearchableFieldType = Field(alias="fieldType")
    full_text_search: Optional[StrictBool] = Field(default=None, description="If true, the field content is split into words and can be searched word by word (full text search).  If false, TEXT fields only match as exact terms (though wildcards and regex are still possible).", alias="fullTextSearch")
    sortable: Optional[StrictBool] = Field(default=None, description="If true, search results can be sorted by this field.")
    default_search_field: Optional[StrictBool] = Field(default=None, description="If true, this field is searched when a query term does not specify a field name.", alias="defaultSearchField")
    possible_values: Optional[List[Optional[StrictStr]]] = Field(default=None, description="The values this field can take, exactly as they are indexed, or null if the field accepts free text.  <p>  Reported for ENUM and BOOLEAN fields, for TEXT fields that hold a closed vocabulary (the compound class  ontologies), and for fields whose values are project state: a tag restricted by its definition, or the  adducts detected in this project. Searchable fields are described per project, so the latter are the  values actually present in it rather than everything that could ever occur.  <p>  Note that the indexed value is not always the notation you would expect: an adduct is indexed as  '[M + H]+', with spaces. Use the values as offered rather than constructing them.", alias="possibleValues")
    description: Optional[StrictStr] = Field(default=None, description="Optional human-readable description of the field content, as shown in the API documentation.  Plain text suitable for direct display (e.g. tooltips); deliberate paragraph breaks are newlines.")
    significant_suffix_length: StrictInt = Field(description="How many trailing dot-separated segments of the field name carry its meaning, for compact  display. <code>1</code> for a normal field (show the terminal segment, e.g. <code>lipid</code>); <code>2</code>  for a dynamic map key (show field + key, e.g. <code>matchedDatabases.GNPS</code>,  <code>qualities.PEAK_QUALITY</code>, <code>tags.pfas</code>); larger for multi-segment keys. Clients may  show the last <code>significantSuffixLength</code> name segments as a short label; the full field name  always stays authoritative for queries.", alias="significantSuffixLength")
    __properties: ClassVar[List[str]] = ["name", "fieldType", "fullTextSearch", "sortable", "defaultSearchField", "possibleValues", "description", "significantSuffixLength"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SearchableField from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if possible_values (nullable) is None
        # and model_fields_set contains the field
        if self.possible_values is None and "possible_values" in self.model_fields_set:
            _dict['possibleValues'] = None

        # set to None if description (nullable) is None
        # and model_fields_set contains the field
        if self.description is None and "description" in self.model_fields_set:
            _dict['description'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SearchableField from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "fieldType": obj.get("fieldType"),
            "fullTextSearch": obj.get("fullTextSearch"),
            "sortable": obj.get("sortable"),
            "defaultSearchField": obj.get("defaultSearchField"),
            "possibleValues": obj.get("possibleValues"),
            "description": obj.get("description"),
            "significantSuffixLength": obj.get("significantSuffixLength")
        })
        return _obj


