# coding: utf-8

"""
    SIRIUS Nightsky API

    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


import os
import json
import time
import unittest

import PySirius
from PySirius import PySiriusAPI, SiriusSDK, StoredJobSubmission
from PySirius.models.job import Job
from PySirius.models.paged_model_job import PagedModelJob
from PySirius.models.job_submission import JobSubmission


class TestJobsApi(unittest.TestCase):
    """JobsApi unit test stubs"""

    def setUp(self) -> None:
        self.api = SiriusSDK().attach_or_start_sirius()
        self.project_id = "test_jobs_api"
        self.path_to_project = f"{os.environ.get('HOME')}/test_jobs_api.sirius"
        if self.api.projects().get_project_without_preload_content(self.project_id).status == 404:
            if os.path.exists(self.path_to_project):
                os.remove(self.path_to_project)
            self.api.projects().create_project(self.project_id, self.path_to_project)

        path_to_demo_data = f"{os.environ.get('HOME')}/sirius-client-openAPI/.updater/clientTests/Data"
        preproc_ms2_file_1 = path_to_demo_data + "/Kaempferol.ms"
        preproc_ms2_file_2 = path_to_demo_data + "/laudanosine.mgf"
        input_files = [preproc_ms2_file_1, preproc_ms2_file_2]
        self.api.projects().import_preprocessed_data_as_job(self.project_id, input_files=input_files)

    def tearDown(self) -> None:
        self.api.projects().close_project(self.project_id)
        os.remove(self.path_to_project)

    def test_delete_job(self) -> None:
        """Test case for delete_job

        Delete job.
        """
        response_before = self.api.jobs().get_jobs(self.project_id)
        print(response_before)
        self.api.jobs().delete_job(self.project_id, response_before[0].id, cancel_if_running=True, await_deletion=True)
        for i in range(0,1000):
            if len(self.api.jobs().get_jobs(self.project_id)) > 0:
                print(self.api.jobs().get_jobs(self.project_id))
                time.sleep(0.1)
            else: break
        response_after = self.api.jobs().get_jobs(self.project_id)

        self.assertIsInstance(response_before, list)
        self.assertEqual(len(response_before), 1)
        self.assertIsInstance(response_before[0], Job)
        self.assertIsInstance(response_after, list)
        self.assertEqual(len(response_after), 0)

    def test_delete_job_config(self) -> None:
        """Test case for delete_job_config

        Delete job configuration with given name.
        """
        config_name = "test_delete_job_config"
        default_config = self.api.jobs().get_default_job_config()
        self.api.jobs().save_job_config(config_name, default_config)

        response_before = self.api.jobs().get_job_configs()
        self.api.jobs().delete_job_config(config_name)
        response_after = self.api.jobs().get_job_configs()

        self.assertIsInstance(response_before, list)
        self.assertIsInstance(response_after, list)
        self.assertEqual(len(response_before) - 1, len(response_after))

    def test_delete_jobs(self) -> None:
        """Test case for delete_jobs

        * Delete ALL jobs.
        """
        response_before = self.api.jobs().get_jobs(self.project_id)
        self.api.jobs().delete_jobs_with_http_info(self.project_id)
        response_after = self.api.jobs().get_jobs(self.project_id)

        self.assertIsInstance(response_before, list)
        self.assertEqual(len(response_before), 1)
        self.assertIsInstance(response_before[0], Job)
        self.assertIsInstance(response_after, list)
        self.assertEqual(len(response_after), 0)

    def test_get_default_job_config(self) -> None:
        """Test case for get_default_job_config

        Request default job configuration
        """
        response = self.api.jobs().get_default_job_config()
        self.assertIsInstance(response, JobSubmission)

    def test_get_job(self) -> None:
        """Test case for get_job

        Get job information and its current state and progress (if available).
        """
        job = self.api.jobs().get_jobs(self.project_id)[0]
        response = self.api.jobs().get_job(self.project_id, job.id)
        self.assertIsInstance(response, Job)

    def test_get_job_config(self) -> None:
        """Test case for get_job_config

        Request job configuration with given name.
        """
        config_name = "test_get_job_config"
        default_config = self.api.jobs().get_default_job_config()
        self.api.jobs().save_job_config(config_name, default_config)

        response = self.api.jobs().get_job_config(config_name)
        self.api.jobs().delete_job_config(config_name)

        self.assertIsInstance(response, StoredJobSubmission)

    def test_get_job_configs(self) -> None:
        """Test case for get_job_configs

        Request all available job configurations
        """
        config_name = "test_get_job_configs"
        default_config = self.api.jobs().get_default_job_config()
        self.api.jobs().save_job_config(config_name, default_config)

        response = self.api.jobs().get_job_configs()
        self.api.jobs().delete_job_config(config_name)

        self.assertIsInstance(response, list)
        self.assertIsInstance(response[0], StoredJobSubmission)

    def test_get_jobs(self) -> None:
        """Test case for get_jobs

        Get job information and its current state and progress (if available).
        """
        response = self.api.jobs().get_jobs(self.project_id)
        self.assertIsInstance(response, list)
        self.assertIsInstance(response[0], Job)

    def test_get_jobs_paged(self) -> None:
        """Test case for get_jobs_paged

        Get Page of jobs with information such as current state and progress (if available).
        """
        response = self.api.jobs().get_jobs_paged(self.project_id)
        self.assertIsInstance(response, PagedModelJob)

    def test_has_jobs(self) -> None:
        """Test case for has_jobs

        """
        response = self.api.jobs().has_jobs(self.project_id)
        self.assertIsInstance(response, bool)
        self.assertTrue(response)

    def test_save_job_config(self) -> None:
        """Test case for save_job_config

        Add new job configuration with given name.
        """
        config_name = "test_save_job_config"
        default_config = self.api.jobs().get_default_job_config()

        response = self.api.jobs().save_job_config(config_name, default_config)
        self.api.jobs().delete_job_config(config_name)

        self.assertIsInstance(response, StoredJobSubmission)
        self.assertEqual(response.name, "test_save_job_config")

    def test_start_job(self) -> None:
        """Test case for start_job

        Start computation for given compounds and with given parameters.
        """
        time.sleep(1)
        aligned_feature_id = self.api.features().get_aligned_features(self.project_id)[0].aligned_feature_id
        sirius_json = {
            "enabled": True,
            "profile": "QTOF"
        }
        job_submission_json = {
            "alignedFeatureIds": [
                aligned_feature_id
            ],
            "formulaIdParams":
                sirius_json
        }
        job_submission = JobSubmission.from_json(json.dumps(job_submission_json))
        response = self.api.jobs().start_job(self.project_id, job_submission)
        # time.sleep(3)

        self.assertIsInstance(response, Job)
        # # TODO find substitute for this line for SIRIUS6
        # self.assertTrue(os.path.exists(self.path_to_project + "/" + aligned_feature_id + "/scores"))

    def test_start_job_from_config(self) -> None:
        """Test case for start_job_from_config

        Start computation for given compounds and with parameters from a stored job-config.
        """
        time.sleep(1)
        aligned_feature_id = self.api.features().get_aligned_features(self.project_id)[0].aligned_feature_id
        config_name = "test_start_job_from_config"
        sirius_json = {
            "enabled": True,
            "profile": "QTOF"
        }
        job_submission_json = {
            "alignedFeatureIds": [
                aligned_feature_id
            ],
            "formulaIdParams":
                sirius_json
        }
        job_submission = JobSubmission.from_json(json.dumps(job_submission_json))
        self.api.jobs().save_job_config(config_name, job_submission)
        response = self.api.jobs().start_job_from_config(self.project_id, config_name, [aligned_feature_id])
        # time.sleep(3)
        self.api.jobs().delete_job_config(config_name)

        self.assertIsInstance(response, Job)
        # # TODO find substitute for this line for SIRIUS6
        # self.assertTrue(os.path.exists(self.path_to_project + "/" + aligned_feature_id + "/scores"))


if __name__ == '__main__':
    unittest.main()
