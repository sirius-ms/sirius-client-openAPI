# SIRIUS Nightsky API
#
# REST API that provides the full functionality of SIRIUS and its web services as background service. It is intended as entry-point for scripting languages and software integration SDKs.
#
# OpenAPI spec version: v0.9 on SIRIUS 5.6.1-SNAPSHOT
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git
#' @title Compounds operations
#' @description RSirius.Compounds
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' delete_compound Delete compound/feature with the given identifier from the specified project-space.
#'
#'
#' get_compound Get compound/feature with the given identifier from the specified project-space.
#'
#'
#' get_compounds Get all available compounds/features in the given project-space.
#'
#'
#' import_compounds Import ms/ms data in given format from local filesystem into the specified project-space
#'
#'
#' import_compounds_from_string Import ms/ms data from the given format into the specified project-space  Possible formats (ms, mgf, cef, msp, mzML, mzXML)
#'
#' }
#'
#' @export
CompoundsApi <- R6::R6Class(
  'CompoundsApi',
  public = list(
    userAgent = "Swagger-Codegen/1.0.0/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    delete_compound = function(project_id, cid, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/api/projects/{projectId}/compounds/{cid}"
      if (!missing(`project_id`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), `project_id`, urlPath)
      }

      if (!missing(`cid`)) {
        urlPath <- gsub(paste0("\\{", "cid", "\\}"), `cid`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    get_compound = function(project_id, cid, top_annotation, ms_data, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`top_annotation`)) {
        queryParams['topAnnotation'] <- top_annotation
      }

      if (!missing(`ms_data`)) {
        queryParams['msData'] <- ms_data
      }

      urlPath <- "/api/projects/{projectId}/compounds/{cid}"
      if (!missing(`project_id`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), `project_id`, urlPath)
      }

      if (!missing(`cid`)) {
        urlPath <- gsub(paste0("\\{", "cid", "\\}"), `cid`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- CompoundId$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    get_compounds = function(project_id, top_annotation, ms_data, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`top_annotation`)) {
        queryParams['topAnnotation'] <- top_annotation
      }

      if (!missing(`ms_data`)) {
        queryParams['msData'] <- ms_data
      }

      urlPath <- "/api/projects/{projectId}/compounds"
      if (!missing(`project_id`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), `project_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- CompoundId$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    import_compounds = function(body, project_id, align_lcms_runs, allow_ms1_only_data, ignore_formulas, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`align_lcms_runs`)) {
        queryParams['alignLCMSRuns'] <- align_lcms_runs
      }

      if (!missing(`allow_ms1_only_data`)) {
        queryParams['allowMs1OnlyData'] <- allow_ms1_only_data
      }

      if (!missing(`ignore_formulas`)) {
        queryParams['ignoreFormulas'] <- ignore_formulas
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/api/projects/{projectId}/compounds"
      if (!missing(`project_id`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), `project_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- JobId$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    import_compounds_from_string = function(body, project_id, format, source_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`format`)) {
        queryParams['format'] <- format
      }

      if (!missing(`source_name`)) {
        queryParams['sourceName'] <- source_name
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/api/projects/{projectId}/compounds/import-from-string"
      if (!missing(`project_id`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), `project_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- CompoundId$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
  )
)
