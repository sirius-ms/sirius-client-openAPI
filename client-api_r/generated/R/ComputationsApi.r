# SIRIUS Nightsky API
#
# REST API that provides the full functionality of SIRIUS and its web services as background service. It is intended as entry-point for scripting languages and software integration SDKs.
#
# OpenAPI spec version: v0.9 on SIRIUS 5.6.1-SNAPSHOT
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git
#' @title Computations operations
#' @description RSirius.Computations
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' delete_job Delete job.
#'
#'
#' delete_job_config Delete job configuration with given name.
#'
#'
#' get_default_job_config Request default job configuration
#'
#'
#' get_job Get job information and its current state and progress (if available).
#'
#'
#' get_job_config Request job configuration with given name.
#'
#'
#' get_job_configs Request all available job configurations
#'
#'
#' get_jobs Get job information and its current state and progress (if available).
#'
#'
#' post_job_config Add new job configuration with given name.
#'
#'
#' start_job Start computation for given compounds and with given parameters.
#'
#'
#' start_job_from_config Start computation for given compounds and with parameters from a stored job-config.
#'
#' }
#'
#' @export
ComputationsApi <- R6::R6Class(
  'ComputationsApi',
  public = list(
    userAgent = "Swagger-Codegen/1.0.0/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    delete_job = function(project_id, job_id, cancel_if_running, await_deletion, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`cancel_if_running`)) {
        queryParams['cancelIfRunning'] <- cancel_if_running
      }

      if (!missing(`await_deletion`)) {
        queryParams['awaitDeletion'] <- await_deletion
      }

      urlPath <- "/api/projects/{projectId}/jobs/{jobId}"
      if (!missing(`project_id`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), `project_id`, urlPath)
      }

      if (!missing(`job_id`)) {
        urlPath <- gsub(paste0("\\{", "jobId", "\\}"), `job_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    delete_job_config = function(name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/api/job-configs/{name}"
      if (!missing(`name`)) {
        urlPath <- gsub(paste0("\\{", "name", "\\}"), `name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    get_default_job_config = function(include_config_map, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`include_config_map`)) {
        queryParams['includeConfigMap'] <- include_config_map
      }

      urlPath <- "/api/default-job-config"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- JobSubmission$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    get_job = function(project_id, job_id, include_state, include_command, include_affected_compounds, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`include_state`)) {
        queryParams['includeState'] <- include_state
      }

      if (!missing(`include_command`)) {
        queryParams['includeCommand'] <- include_command
      }

      if (!missing(`include_affected_compounds`)) {
        queryParams['includeAffectedCompounds'] <- include_affected_compounds
      }

      urlPath <- "/api/projects/{projectId}/jobs/{jobId}"
      if (!missing(`project_id`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), `project_id`, urlPath)
      }

      if (!missing(`job_id`)) {
        urlPath <- gsub(paste0("\\{", "jobId", "\\}"), `job_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- JobId$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    get_job_config = function(name, include_config_map, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`include_config_map`)) {
        queryParams['includeConfigMap'] <- include_config_map
      }

      urlPath <- "/api/job-configs/{name}"
      if (!missing(`name`)) {
        urlPath <- gsub(paste0("\\{", "name", "\\}"), `name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- JobSubmission$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    get_job_configs = function(include_config_map, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`include_config_map`)) {
        queryParams['includeConfigMap'] <- include_config_map
      }

      urlPath <- "/api/job-configs"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- JobSubmission$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    get_jobs = function(project_id, include_state, include_command, include_affected_compounds, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`include_state`)) {
        queryParams['includeState'] <- include_state
      }

      if (!missing(`include_command`)) {
        queryParams['includeCommand'] <- include_command
      }

      if (!missing(`include_affected_compounds`)) {
        queryParams['includeAffectedCompounds'] <- include_affected_compounds
      }

      urlPath <- "/api/projects/{projectId}/jobs"
      if (!missing(`project_id`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), `project_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- JobId$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    post_job_config = function(body, name, override_existing, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`override_existing`)) {
        queryParams['overrideExisting'] <- override_existing
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/api/job-configs/{name}"
      if (!missing(`name`)) {
        urlPath <- gsub(paste0("\\{", "name", "\\}"), `name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Character$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    start_job = function(body, project_id, include_state, include_command, include_affected_compounds, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`include_state`)) {
        queryParams['includeState'] <- include_state
      }

      if (!missing(`include_command`)) {
        queryParams['includeCommand'] <- include_command
      }

      if (!missing(`include_affected_compounds`)) {
        queryParams['includeAffectedCompounds'] <- include_affected_compounds
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/api/projects/{projectId}/jobs"
      if (!missing(`project_id`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), `project_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- JobId$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    start_job_from_config = function(body, project_id, job_config_name, recompute, include_state, include_command, include_affected_compounds, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`job_config_name`)) {
        queryParams['jobConfigName'] <- job_config_name
      }

      if (!missing(`recompute`)) {
        queryParams['recompute'] <- recompute
      }

      if (!missing(`include_state`)) {
        queryParams['includeState'] <- include_state
      }

      if (!missing(`include_command`)) {
        queryParams['includeCommand'] <- include_command
      }

      if (!missing(`include_affected_compounds`)) {
        queryParams['includeAffectedCompounds'] <- include_affected_compounds
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/api/projects/{projectId}/jobs-from-config"
      if (!missing(`project_id`)) {
        urlPath <- gsub(paste0("\\{", "projectId", "\\}"), `project_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- JobId$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
  )
)
