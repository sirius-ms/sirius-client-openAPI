#' SIRIUS Nightsky API
#'
#' REST API that provides the full functionality of SIRIUS and its web services as background service. It is intended as entry-point for scripting languages and software integration SDKs.
#'
#' The version of the OpenAPI document: v0.9 on SIRIUS 5.6.4-SNAPSHOT
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Compounds operations
#' @description CompoundsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ DeleteCompound } \emph{ Delete compound/feature with the given identifier from the specified project-space. }
#' Delete compound/feature with the given identifier from the specified project-space.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } cid character
#'
#'
#' \item status code : 200 | OK
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetCompound } \emph{ Get compound/feature with the given identifier from the specified project-space. }
#' Get compound/feature with the given identifier from the specified project-space.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } cid character
#' \item \emph{ @param } top_annotation character
#' \item \emph{ @param } ms_data character
#' \item \emph{ @returnType } \link{CompoundId} \cr
#'
#'
#' \item status code : 200 | CompoundId with additional annotations and MS/MS data (if specified).
#'
#' \item return type : CompoundId
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetCompounds } \emph{ Get all available compounds/features in the given project-space. }
#' Get all available compounds/features in the given project-space.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } top_annotation character
#' \item \emph{ @param } ms_data character
#' \item \emph{ @returnType } list( \link{CompoundId} ) \cr
#'
#'
#' \item status code : 200 | CompoundIds with additional annotations and MS/MS data (if specified).
#'
#' \item return type : array[CompoundId]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ImportCompounds } \emph{ Import ms/ms data in given format from local filesystem into the specified project-space }
#' Import ms/ms data in given format from local filesystem into the specified project-space.  The import will run in a background job  Possible formats (ms, mgf, cef, msp, mzML, mzXML, project-space)  <p>
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } request_body list( character )
#' \item \emph{ @param } align_lcms_runs character
#' \item \emph{ @param } allow_ms1_only_data character
#' \item \emph{ @param } ignore_formulas character
#' \item \emph{ @returnType } \link{JobId} \cr
#'
#'
#' \item status code : 200 | JobId background job that imports given compounds/features.
#'
#' \item return type : JobId
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ImportCompoundsFromString } \emph{ Import ms/ms data from the given format into the specified project-space  Possible formats (ms, mgf, cef, msp, mzML, mzXML) }
#' Import ms/ms data from the given format into the specified project-space  Possible formats (ms, mgf, cef, msp, mzML, mzXML)
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } format character
#' \item \emph{ @param } body character
#' \item \emph{ @param } source_name character
#' \item \emph{ @returnType } list( \link{CompoundId} ) \cr
#'
#'
#' \item status code : 200 | CompoundIds of the imported compounds/features.
#'
#' \item return type : array[CompoundId]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  DeleteCompound  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to delete from.
#' var_cid <- "cid_example" # character | identifier of compound to delete.
#'
#' #Delete compound/feature with the given identifier from the specified project-space.
#' api_instance <- rsirius_api$new()
#'
#' api_instance$compounds_api$DeleteCompound(var_project_id, var_cid)
#'
#'
#' ####################  GetCompound  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_cid <- "cid_example" # character | identifier of compound to access.
#' var_top_annotation <- FALSE # character | include the top annotation of this feature into the output (if available). (Optional)
#' var_ms_data <- FALSE # character | include corresponding source data (MS and MS/MS) into the output. (Optional)
#'
#' #Get compound/feature with the given identifier from the specified project-space.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCompound(var_project_id, var_cid, top_annotation = var_top_annotation, ms_data = var_ms_datadata_file = "result.txt")
#' result <- api_instance$compounds_api$GetCompound(var_project_id, var_cid, top_annotation = var_top_annotation, ms_data = var_ms_data)
#' dput(result)
#'
#'
#' ####################  GetCompounds  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_top_annotation <- FALSE # character | include the top annotation of this feature into the output (if available). (Optional)
#' var_ms_data <- FALSE # character | include corresponding source data (MS and MS/MS) into the output. (Optional)
#'
#' #Get all available compounds/features in the given project-space.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCompounds(var_project_id, top_annotation = var_top_annotation, ms_data = var_ms_datadata_file = "result.txt")
#' result <- api_instance$compounds_api$GetCompounds(var_project_id, top_annotation = var_top_annotation, ms_data = var_ms_data)
#' dput(result)
#'
#'
#' ####################  ImportCompounds  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to import into.
#' var_request_body <- c("property_example") # array[character] | List of file and directory paths to import
#' var_align_lcms_runs <- FALSE # character | If true, multiple LCMS Runs (mzML, mzXML) will be aligned during import/feature finding (Optional)
#' var_allow_ms1_only_data <- TRUE # character |  (Optional)
#' var_ignore_formulas <- FALSE # character |  (Optional)
#'
#' #Import ms/ms data in given format from local filesystem into the specified project-space
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ImportCompounds(var_project_id, var_request_body, align_lcms_runs = var_align_lcms_runs, allow_ms1_only_data = var_allow_ms1_only_data, ignore_formulas = var_ignore_formulasdata_file = "result.txt")
#' result <- api_instance$compounds_api$ImportCompounds(var_project_id, var_request_body, align_lcms_runs = var_align_lcms_runs, allow_ms1_only_data = var_allow_ms1_only_data, ignore_formulas = var_ignore_formulas)
#' dput(result)
#'
#'
#' ####################  ImportCompoundsFromString  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to import into.
#' var_format <- "format_example" # character | data format specified by the usual file extension of the format (without [.])
#' var_body <- "body_example" # character | data content in specified format
#' var_source_name <- "source_name_example" # character | name that specifies the data source. Can e.g. be a file path or just a name. (Optional)
#'
#' #Import ms/ms data from the given format into the specified project-space  Possible formats (ms, mgf, cef, msp, mzML, mzXML)
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ImportCompoundsFromString(var_project_id, var_format, var_body, source_name = var_source_namedata_file = "result.txt")
#' result <- api_instance$compounds_api$ImportCompoundsFromString(var_project_id, var_format, var_body, source_name = var_source_name)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
CompoundsApi <- R6::R6Class(
  "CompoundsApi",
  public = list(
    api_client = NULL,
    #' Initialize a new CompoundsApi.
    #'
    #' @description
    #' Initialize a new CompoundsApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Delete compound/feature with the given identifier from the specified project-space.
    #'
    #' @description
    #' Delete compound/feature with the given identifier from the specified project-space.
    #'
    #' @param project_id project-space to delete from.
    #' @param cid identifier of compound to delete.
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    DeleteCompound = function(project_id, cid, ...) {
      local_var_response <- self$DeleteCompoundWithHttpInfo(project_id, cid, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Delete compound/feature with the given identifier from the specified project-space.
    #'
    #' @description
    #' Delete compound/feature with the given identifier from the specified project-space.
    #'
    #' @param project_id project-space to delete from.
    #' @param cid identifier of compound to delete.
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    DeleteCompoundWithHttpInfo = function(project_id, cid, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`cid`)) {
        stop("Missing required parameter `cid`.")
      }



      local_var_url_path <- "/api/projects/{projectId}/compounds/{cid}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`cid`)) {
        local_var_url_path <- gsub("\\{cid\\}", URLencode(as.character(`cid`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get compound/feature with the given identifier from the specified project-space.
    #'
    #' @description
    #' Get compound/feature with the given identifier from the specified project-space.
    #'
    #' @param project_id project-space to read from.
    #' @param cid identifier of compound to access.
    #' @param top_annotation (optional) include the top annotation of this feature into the output (if available). (default value: FALSE)
    #' @param ms_data (optional) include corresponding source data (MS and MS/MS) into the output. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return CompoundId
    #' @export
    GetCompound = function(project_id, cid, top_annotation = FALSE, ms_data = FALSE, data_file = NULL, ...) {
      local_var_response <- self$GetCompoundWithHttpInfo(project_id, cid, top_annotation, ms_data, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get compound/feature with the given identifier from the specified project-space.
    #'
    #' @description
    #' Get compound/feature with the given identifier from the specified project-space.
    #'
    #' @param project_id project-space to read from.
    #' @param cid identifier of compound to access.
    #' @param top_annotation (optional) include the top annotation of this feature into the output (if available). (default value: FALSE)
    #' @param ms_data (optional) include corresponding source data (MS and MS/MS) into the output. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (CompoundId) with additional information such as HTTP status code, headers
    #' @export
    GetCompoundWithHttpInfo = function(project_id, cid, top_annotation = FALSE, ms_data = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`cid`)) {
        stop("Missing required parameter `cid`.")
      }





      query_params[["topAnnotation"]] <- `top_annotation`

      query_params[["msData"]] <- `ms_data`

      local_var_url_path <- "/api/projects/{projectId}/compounds/{cid}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`cid`)) {
        local_var_url_path <- gsub("\\{cid\\}", URLencode(as.character(`cid`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "CompoundId", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get all available compounds/features in the given project-space.
    #'
    #' @description
    #' Get all available compounds/features in the given project-space.
    #'
    #' @param project_id project-space to read from.
    #' @param top_annotation (optional) include the top annotation of this feature into the output (if available). (default value: FALSE)
    #' @param ms_data (optional) include corresponding source data (MS and MS/MS) into the output. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CompoundId]
    #' @export
    GetCompounds = function(project_id, top_annotation = FALSE, ms_data = FALSE, data_file = NULL, ...) {
      local_var_response <- self$GetCompoundsWithHttpInfo(project_id, top_annotation, ms_data, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get all available compounds/features in the given project-space.
    #'
    #' @description
    #' Get all available compounds/features in the given project-space.
    #'
    #' @param project_id project-space to read from.
    #' @param top_annotation (optional) include the top annotation of this feature into the output (if available). (default value: FALSE)
    #' @param ms_data (optional) include corresponding source data (MS and MS/MS) into the output. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CompoundId]) with additional information such as HTTP status code, headers
    #' @export
    GetCompoundsWithHttpInfo = function(project_id, top_annotation = FALSE, ms_data = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }




      query_params[["topAnnotation"]] <- `top_annotation`

      query_params[["msData"]] <- `ms_data`

      local_var_url_path <- "/api/projects/{projectId}/compounds"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CompoundId]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Import ms/ms data in given format from local filesystem into the specified project-space
    #'
    #' @description
    #' Import ms/ms data in given format from local filesystem into the specified project-space
    #'
    #' @param project_id project-space to import into.
    #' @param request_body List of file and directory paths to import
    #' @param align_lcms_runs (optional) If true, multiple LCMS Runs (mzML, mzXML) will be aligned during import/feature finding (default value: FALSE)
    #' @param allow_ms1_only_data (optional) No description (default value: TRUE)
    #' @param ignore_formulas (optional) No description (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return JobId
    #' @export
    ImportCompounds = function(project_id, request_body, align_lcms_runs = FALSE, allow_ms1_only_data = TRUE, ignore_formulas = FALSE, data_file = NULL, ...) {
      local_var_response <- self$ImportCompoundsWithHttpInfo(project_id, request_body, align_lcms_runs, allow_ms1_only_data, ignore_formulas, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Import ms/ms data in given format from local filesystem into the specified project-space
    #'
    #' @description
    #' Import ms/ms data in given format from local filesystem into the specified project-space
    #'
    #' @param project_id project-space to import into.
    #' @param request_body List of file and directory paths to import
    #' @param align_lcms_runs (optional) If true, multiple LCMS Runs (mzML, mzXML) will be aligned during import/feature finding (default value: FALSE)
    #' @param allow_ms1_only_data (optional) No description (default value: TRUE)
    #' @param ignore_formulas (optional) No description (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (JobId) with additional information such as HTTP status code, headers
    #' @export
    ImportCompoundsWithHttpInfo = function(project_id, request_body, align_lcms_runs = FALSE, allow_ms1_only_data = TRUE, ignore_formulas = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`request_body`)) {
        stop("Missing required parameter `request_body`.")
      }






      query_params[["alignLCMSRuns"]] <- `align_lcms_runs`

      query_params[["allowMs1OnlyData"]] <- `allow_ms1_only_data`

      query_params[["ignoreFormulas"]] <- `ignore_formulas`

      if (!is.null(`request_body`)) {
        local_var_body <- jsonlite::toJSON(`request_body`)
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/projects/{projectId}/compounds"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "JobId", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Import ms/ms data from the given format into the specified project-space  Possible formats (ms, mgf, cef, msp, mzML, mzXML)
    #'
    #' @description
    #' Import ms/ms data from the given format into the specified project-space  Possible formats (ms, mgf, cef, msp, mzML, mzXML)
    #'
    #' @param project_id project-space to import into.
    #' @param format data format specified by the usual file extension of the format (without [.])
    #' @param body data content in specified format
    #' @param source_name (optional) name that specifies the data source. Can e.g. be a file path or just a name.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[CompoundId]
    #' @export
    ImportCompoundsFromString = function(project_id, format, body, source_name = NULL, data_file = NULL, ...) {
      local_var_response <- self$ImportCompoundsFromStringWithHttpInfo(project_id, format, body, source_name, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Import ms/ms data from the given format into the specified project-space  Possible formats (ms, mgf, cef, msp, mzML, mzXML)
    #'
    #' @description
    #' Import ms/ms data from the given format into the specified project-space  Possible formats (ms, mgf, cef, msp, mzML, mzXML)
    #'
    #' @param project_id project-space to import into.
    #' @param format data format specified by the usual file extension of the format (without [.])
    #' @param body data content in specified format
    #' @param source_name (optional) name that specifies the data source. Can e.g. be a file path or just a name.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[CompoundId]) with additional information such as HTTP status code, headers
    #' @export
    ImportCompoundsFromStringWithHttpInfo = function(project_id, format, body, source_name = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`format`)) {
        stop("Missing required parameter `format`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }





      query_params[["format"]] <- `format`

      query_params[["sourceName"]] <- `source_name`

      if (!is.null(`body`)) {
        local_var_body <- `body`
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/projects/{projectId}/compounds/import-from-string"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("text/plain")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[CompoundId]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
