#' SIRIUS Nightsky API
#'
#' REST API that provides the full functionality of SIRIUS and its web services as background service. It is intended as entry-point for scripting languages and software integration SDKs.This API is exposed by SIRIUS 6
#'
#' The version of the OpenAPI document: 3.2
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Compounds operations
#' @description CompoundsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  AddCompounds  ####################
#'
#' library(RSirius)
#' var_project_id <- "project_id_example" # character | project-space to import into.
#' var_compound_import <- c(CompoundImport$new(c(FeatureImport$new(123, 123, "name_example", "externalFeatureId_example", c("detectedAdducts_example"), 123, 123, 123, "NOT_APPLICABLE", BasicSpectrum$new("cosineQuery_example", c(SimplePeak$new(123, 123)), "name_example", 123, "collisionEnergy_example", "instrument_example", 123, 123, SimplePeak$new(123, 123), 123, 123, 123, 123, 123), c(BasicSpectrum$new("cosineQuery_example", c(SimplePeak$new(..., ...)), "name_example", 123, "collisionEnergy_example", "instrument_example", 123, 123, SimplePeak$new(123, 123), 123, 123, 123, 123, 123)), c(BasicSpectrum$new("cosineQuery_example", c(SimplePeak$new(..., ...)), "name_example", 123, "collisionEnergy_example", "instrument_example", 123, 123, SimplePeak$new(123, 123), 123, 123, 123, 123, 123)))), "name_example")) # array[CompoundImport] | the compound data to be imported
#' var_profile <- "profile_example" # character | profile describing the instrument used to measure the data. Used to merge spectra. (Optional)
#' var_opt_fields <- c("none") # array[character] | set of optional fields to be included. Use 'none' to override defaults. (Optional)
#' var_opt_fields_features <- c("none") # array[character] | set of optional fields of the nested features to be included. Use 'none' to override defaults. (Optional)
#'
#' #Import compounds and their contained features.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AddCompounds(var_project_id, var_compound_import, profile = var_profile, opt_fields = var_opt_fields, opt_fields_features = var_opt_fields_featuresdata_file = "result.txt")
#' result <- api_instance$compounds_api$AddCompounds(var_project_id, var_compound_import, profile = var_profile, opt_fields = var_opt_fields, opt_fields_features = var_opt_fields_features)
#' dput(result)
#'
#'
#' ####################  DeleteCompound  ####################
#'
#' library(RSirius)
#' var_project_id <- "project_id_example" # character | project-space to delete from.
#' var_compound_id <- "compound_id_example" # character | identifier of the compound to delete.
#'
#' #Delete compound (group of ion identities) with the given identifier (and the included features) from the  specified project-space.
#' api_instance <- rsirius_api$new()
#'
#' api_instance$compounds_api$DeleteCompound(var_project_id, var_compound_id)
#'
#'
#' ####################  GetCompound  ####################
#'
#' library(RSirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_compound_id <- "compound_id_example" # character | identifier of the compound (group of ion identities) to access.
#' var_ms_data_search_prepared <- FALSE # character | Returns all fragment spectra in a preprocessed form as used for fast                             Cosine/Modified Cosine computation. Gives you spectra compatible with SpectralLibraryMatch                             peak assignments and reference spectra. (Optional)
#' var_opt_fields <- c("none") # array[character] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#' var_opt_fields_features <- c("none") # array[character] |  (Optional)
#'
#' #Get compound (group of ion identities) with the given identifier from the specified project-space.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCompound(var_project_id, var_compound_id, ms_data_search_prepared = var_ms_data_search_prepared, opt_fields = var_opt_fields, opt_fields_features = var_opt_fields_featuresdata_file = "result.txt")
#' result <- api_instance$compounds_api$GetCompound(var_project_id, var_compound_id, ms_data_search_prepared = var_ms_data_search_prepared, opt_fields = var_opt_fields, opt_fields_features = var_opt_fields_features)
#' dput(result)
#'
#'
#' ####################  GetCompoundQuantTable  ####################
#'
#' library(RSirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_search_query <- "search_query_example" # character | Optional query in lucene syntax selecting compounds by id. Omit this parameter to quantify all compounds. (Optional)
#' var_type <- "APEX_INTENSITY" # character | quantification type. (Optional)
#' var_opt_fields <- c(QuantTableOptField$new()) # array[QuantTableOptField] |  (Optional)
#'
#' #Returns the full quantification table of compounds
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCompoundQuantTable(var_project_id, search_query = var_search_query, type = var_type, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$compounds_api$GetCompoundQuantTable(var_project_id, search_query = var_search_query, type = var_type, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetCompounds  ####################
#'
#' library(RSirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_ms_data_search_prepared <- FALSE # character | Returns all fragment spectra in a preprocessed form as used for fast                             Cosine/Modified Cosine computation. Gives you spectra compatible with SpectralLibraryMatch                             peak assignments and reference spectra. (Optional)
#' var_opt_fields <- c("none") # array[character] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#' var_opt_fields_features <- c("none") # array[character] |  (Optional)
#'
#' #[DEPRECATED] List of all available compounds (group of ion identities) in the given project-space
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCompounds(var_project_id, ms_data_search_prepared = var_ms_data_search_prepared, opt_fields = var_opt_fields, opt_fields_features = var_opt_fields_featuresdata_file = "result.txt")
#' result <- api_instance$compounds_api$GetCompounds(var_project_id, ms_data_search_prepared = var_ms_data_search_prepared, opt_fields = var_opt_fields, opt_fields_features = var_opt_fields_features)
#' dput(result)
#'
#'
#' ####################  GetCompoundsPage  ####################
#'
#' library(RSirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_search_query <- "search_query_example" # character | Optional search query in lucene syntax. Not yet supported for compounds; a non-empty                      query responds with 405 METHOD_NOT_ALLOWED. Omit this parameter to page over all compounds. (Optional)
#' var_page <- 0 # integer | Zero-based page index (0..N) (Optional)
#' var_size <- 20 # integer | The size of the page to be returned (Optional)
#' var_sort <- c("inner_example") # array[character] | Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported. (Optional)
#' var_ms_data_search_prepared <- FALSE # character | Returns all fragment spectra in a preprocessed form as used for fast                             Cosine/Modified Cosine computation. Gives you spectra compatible with SpectralLibraryMatch                             peak assignments and reference spectra. (Optional)
#' var_opt_fields <- c("none") # array[character] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#' var_opt_fields_features <- c("none") # array[character] |  (Optional)
#'
#' #Page of available compounds (group of ion identities) in the given project-space
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCompoundsPage(var_project_id, search_query = var_search_query, page = var_page, size = var_size, sort = var_sort, ms_data_search_prepared = var_ms_data_search_prepared, opt_fields = var_opt_fields, opt_fields_features = var_opt_fields_featuresdata_file = "result.txt")
#' result <- api_instance$compounds_api$GetCompoundsPage(var_project_id, search_query = var_search_query, page = var_page, size = var_size, sort = var_sort, ms_data_search_prepared = var_ms_data_search_prepared, opt_fields = var_opt_fields, opt_fields_features = var_opt_fields_features)
#' dput(result)
#'
#'
#' ####################  GetCompoundsSearchableFields  ####################
#'
#' library(RSirius)
#' var_project_id <- "project_id_example" # character | project space to read the searchable compound fields from.
#'
#' #Get all fields that can be used in the searchQuery parameter of compound endpoints
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCompoundsSearchableFields(var_project_iddata_file = "result.txt")
#' result <- api_instance$compounds_api$GetCompoundsSearchableFields(var_project_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
CompoundsApi <- R6::R6Class(
  "CompoundsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new CompoundsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Import compounds and their contained features.
    #'
    #' @param project_id project-space to import into.
    #' @param compound_import the compound data to be imported
    #' @param profile (optional) profile describing the instrument used to measure the data. Used to merge spectra.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' to override defaults. (default value: ["none"])
    #' @param opt_fields_features (optional) set of optional fields of the nested features to be included. Use 'none' to override defaults. (default value: ["none"])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[Compound]
    AddCompounds = function(project_id, compound_import, profile = NULL, opt_fields = list("none"), opt_fields_features = list("none"), data_file = NULL, ...) {
      local_var_response <- self$AddCompoundsWithHttpInfo(project_id, compound_import, profile, opt_fields, opt_fields_features, data_file = data_file, ...)
      if (is.null(local_var_response$status_code)) { # defensive: no status code means the call never reached the API
        local_var_response
      } else if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Import compounds and their contained features.
    #'
    #' @param project_id project-space to import into.
    #' @param compound_import the compound data to be imported
    #' @param profile (optional) profile describing the instrument used to measure the data. Used to merge spectra.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' to override defaults. (default value: ["none"])
    #' @param opt_fields_features (optional) set of optional fields of the nested features to be included. Use 'none' to override defaults. (default value: ["none"])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[Compound]) with additional information such as HTTP status code, headers
    AddCompoundsWithHttpInfo = function(project_id, compound_import, profile = NULL, opt_fields = list("none"), opt_fields_features = list("none"), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`compound_import`)) {
        stop("Missing required parameter `compound_import`.")
      }






      if (!is.null(`profile`) && !(`profile` %in% c("QTOF", "ORBITRAP"))) {
        stop("Invalid value for profile when calling CompoundsApi$AddCompounds. Must be [QTOF, ORBITRAP].")
      }
      query_params[["profile"]] <- `profile`

      # explore
      for (query_item in `opt_fields`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("none", "consensusAnnotations", "consensusAnnotationsDeNovo", "customAnnotations", "tags"))) {
          stop("Invalid value for opt_fields when calling CompoundsApi$AddCompounds. Must be [none, consensusAnnotations, consensusAnnotationsDeNovo, customAnnotations, tags].")
        }
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      # explore
      for (query_item in `opt_fields_features`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("none", "msData", "topAnnotationsSummary", "topAnnotations", "topAnnotationsDeNovo", "computedTools", "qualities", "tags"))) {
          stop("Invalid value for opt_fields_features when calling CompoundsApi$AddCompounds. Must be [none, msData, topAnnotationsSummary, topAnnotations, topAnnotationsDeNovo, computedTools, qualities, tags].")
        }
        query_params[["optFieldsFeatures"]] <- c(query_params[["optFieldsFeatures"]], list(`optFieldsFeatures` = query_item))
      }

      if (!is.null(`compound_import`)) {
        body.items <- paste(unlist(lapply(`compound_import`, function(param) {
          if (inherits(param, "character")) {
            param
          } else {
            param$toJSONString()
          }
        })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/projects/{projectId}/compounds"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        # Check if we are expecting a CSV response
        is_csv_response <- any(grepl("csv", local_var_accepts, ignore.case = TRUE))

        if (is_csv_response) {
          # For CSV responses, parse into data.frame
          csv_resp_obj <- tryCatch(
            {
              csv_text <- rawToChar(local_var_resp$response)

              # Detect separator by examining first line
              first_line <- strsplit(csv_text, "\n")[[1]][1]
              if (grepl("\t", first_line)) {
                # Tab-separated (TSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = "\t")
              } else {
                # Comma-separated (CSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = ",")
              }
            },
            error = function(e) {
              stop("Failed to parse CSV response")
            }
          )
          local_var_resp$content <- csv_resp_obj
        } else {
          # For JSON responses, deserialize normally
          deserialized_resp_obj <- tryCatch(
            self$api_client$deserialize(local_var_resp$response_as_text(), "array[Compound]", loadNamespace("RSirius")),
            error = function(e) {
              stop("Failed to deserialize response")
            }
          )
          local_var_resp$content <- deserialized_resp_obj
        }
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (length(local_var_resp$response) == 0) {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Delete compound (group of ion identities) with the given identifier (and the included features) from the  specified project-space.
    #'
    #' @param project_id project-space to delete from.
    #' @param compound_id identifier of the compound to delete.
    #' @param ... Other optional arguments
    #'
    #' @return void
    DeleteCompound = function(project_id, compound_id, ...) {
      local_var_response <- self$DeleteCompoundWithHttpInfo(project_id, compound_id, ...)
      if (is.null(local_var_response$status_code)) { # defensive: no status code means the call never reached the API
        local_var_response
      } else if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Delete compound (group of ion identities) with the given identifier (and the included features) from the  specified project-space.
    #'
    #' @param project_id project-space to delete from.
    #' @param compound_id identifier of the compound to delete.
    #' @param ... Other optional arguments
    #'
    #' @return API response (void) with additional information such as HTTP status code, headers
    DeleteCompoundWithHttpInfo = function(project_id, compound_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`compound_id`)) {
        stop("Missing required parameter `compound_id`.")
      }



      local_var_url_path <- "/api/projects/{projectId}/compounds/{compoundId}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`compound_id`)) {
        local_var_url_path <- gsub("\\{compoundId\\}", URLencode(as.character(`compound_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (length(local_var_resp$response) == 0) {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get compound (group of ion identities) with the given identifier from the specified project-space.
    #'
    #' @param project_id project-space to read from.
    #' @param compound_id identifier of the compound (group of ion identities) to access.
    #' @param ms_data_search_prepared (optional) Returns all fragment spectra in a preprocessed form as used for fast                             Cosine/Modified Cosine computation. Gives you spectra compatible with SpectralLibraryMatch                             peak assignments and reference spectra. (default value: FALSE)
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: ["none"])
    #' @param opt_fields_features (optional) No description (default value: ["none"])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return Compound
    GetCompound = function(project_id, compound_id, ms_data_search_prepared = FALSE, opt_fields = list("none"), opt_fields_features = list("none"), data_file = NULL, ...) {
      local_var_response <- self$GetCompoundWithHttpInfo(project_id, compound_id, ms_data_search_prepared, opt_fields, opt_fields_features, data_file = data_file, ...)
      if (is.null(local_var_response$status_code)) { # defensive: no status code means the call never reached the API
        local_var_response
      } else if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get compound (group of ion identities) with the given identifier from the specified project-space.
    #'
    #' @param project_id project-space to read from.
    #' @param compound_id identifier of the compound (group of ion identities) to access.
    #' @param ms_data_search_prepared (optional) Returns all fragment spectra in a preprocessed form as used for fast                             Cosine/Modified Cosine computation. Gives you spectra compatible with SpectralLibraryMatch                             peak assignments and reference spectra. (default value: FALSE)
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: ["none"])
    #' @param opt_fields_features (optional) No description (default value: ["none"])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (Compound) with additional information such as HTTP status code, headers
    GetCompoundWithHttpInfo = function(project_id, compound_id, ms_data_search_prepared = FALSE, opt_fields = list("none"), opt_fields_features = list("none"), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`compound_id`)) {
        stop("Missing required parameter `compound_id`.")
      }






      query_params[["msDataSearchPrepared"]] <- `ms_data_search_prepared`

      # explore
      for (query_item in `opt_fields`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("none", "consensusAnnotations", "consensusAnnotationsDeNovo", "customAnnotations", "tags"))) {
          stop("Invalid value for opt_fields when calling CompoundsApi$GetCompound. Must be [none, consensusAnnotations, consensusAnnotationsDeNovo, customAnnotations, tags].")
        }
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      # explore
      for (query_item in `opt_fields_features`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("none", "msData", "topAnnotationsSummary", "topAnnotations", "topAnnotationsDeNovo", "computedTools", "qualities", "tags"))) {
          stop("Invalid value for opt_fields_features when calling CompoundsApi$GetCompound. Must be [none, msData, topAnnotationsSummary, topAnnotations, topAnnotationsDeNovo, computedTools, qualities, tags].")
        }
        query_params[["optFieldsFeatures"]] <- c(query_params[["optFieldsFeatures"]], list(`optFieldsFeatures` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/compounds/{compoundId}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`compound_id`)) {
        local_var_url_path <- gsub("\\{compoundId\\}", URLencode(as.character(`compound_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        # Check if we are expecting a CSV response
        is_csv_response <- any(grepl("csv", local_var_accepts, ignore.case = TRUE))

        if (is_csv_response) {
          # For CSV responses, parse into data.frame
          csv_resp_obj <- tryCatch(
            {
              csv_text <- rawToChar(local_var_resp$response)

              # Detect separator by examining first line
              first_line <- strsplit(csv_text, "\n")[[1]][1]
              if (grepl("\t", first_line)) {
                # Tab-separated (TSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = "\t")
              } else {
                # Comma-separated (CSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = ",")
              }
            },
            error = function(e) {
              stop("Failed to parse CSV response")
            }
          )
          local_var_resp$content <- csv_resp_obj
        } else {
          # For JSON responses, deserialize normally
          deserialized_resp_obj <- tryCatch(
            self$api_client$deserialize(local_var_resp$response_as_text(), "Compound", loadNamespace("RSirius")),
            error = function(e) {
              stop("Failed to deserialize response")
            }
          )
          local_var_resp$content <- deserialized_resp_obj
        }
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (length(local_var_resp$response) == 0) {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Returns the full quantification table of compounds
    #'
    #' @param project_id project-space to read from.
    #' @param search_query (optional) Optional query in lucene syntax selecting compounds by id. Omit this parameter to quantify all compounds.
    #' @param type (optional) quantification type. (default value: "APEX_INTENSITY")
    #' @param opt_fields (optional) No description (default value: ["none"])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return QuantTable
    GetCompoundQuantTable = function(project_id, search_query = NULL, type = "APEX_INTENSITY", opt_fields = list("none"), data_file = NULL, ...) {
      local_var_response <- self$GetCompoundQuantTableWithHttpInfo(project_id, search_query, type, opt_fields, data_file = data_file, ...)
      if (is.null(local_var_response$status_code)) { # defensive: no status code means the call never reached the API
        local_var_response
      } else if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Returns the full quantification table of compounds
    #'
    #' @param project_id project-space to read from.
    #' @param search_query (optional) Optional query in lucene syntax selecting compounds by id. Omit this parameter to quantify all compounds.
    #' @param type (optional) quantification type. (default value: "APEX_INTENSITY")
    #' @param opt_fields (optional) No description (default value: ["none"])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (QuantTable) with additional information such as HTTP status code, headers
    GetCompoundQuantTableWithHttpInfo = function(project_id, search_query = NULL, type = "APEX_INTENSITY", opt_fields = list("none"), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }





      query_params[["searchQuery"]] <- `search_query`

      if (!is.null(`type`) && !(`type` %in% c("APEX_INTENSITY", "AREA_UNDER_CURVE"))) {
        stop("Invalid value for type when calling CompoundsApi$GetCompoundQuantTable. Must be [APEX_INTENSITY, AREA_UNDER_CURVE].")
      }
      query_params[["type"]] <- `type`

      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/compounds/quant-table"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        # Check if we are expecting a CSV response
        is_csv_response <- any(grepl("csv", local_var_accepts, ignore.case = TRUE))

        if (is_csv_response) {
          # For CSV responses, parse into data.frame
          csv_resp_obj <- tryCatch(
            {
              csv_text <- rawToChar(local_var_resp$response)

              # Detect separator by examining first line
              first_line <- strsplit(csv_text, "\n")[[1]][1]
              if (grepl("\t", first_line)) {
                # Tab-separated (TSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = "\t")
              } else {
                # Comma-separated (CSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = ",")
              }
            },
            error = function(e) {
              stop("Failed to parse CSV response")
            }
          )
          local_var_resp$content <- csv_resp_obj
        } else {
          # For JSON responses, deserialize normally
          deserialized_resp_obj <- tryCatch(
            self$api_client$deserialize(local_var_resp$response_as_text(), "QuantTable", loadNamespace("RSirius")),
            error = function(e) {
              stop("Failed to deserialize response")
            }
          )
          local_var_resp$content <- deserialized_resp_obj
        }
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (length(local_var_resp$response) == 0) {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' [DEPRECATED] List of all available compounds (group of ion identities) in the given project-space
    #'
    #' @param project_id project-space to read from.
    #' @param ms_data_search_prepared (optional) Returns all fragment spectra in a preprocessed form as used for fast                             Cosine/Modified Cosine computation. Gives you spectra compatible with SpectralLibraryMatch                             peak assignments and reference spectra. (default value: FALSE)
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: ["none"])
    #' @param opt_fields_features (optional) No description (default value: ["none"])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[Compound]
    GetCompounds = function(project_id, ms_data_search_prepared = FALSE, opt_fields = list("none"), opt_fields_features = list("none"), data_file = NULL, ...) {
      local_var_response <- self$GetCompoundsWithHttpInfo(project_id, ms_data_search_prepared, opt_fields, opt_fields_features, data_file = data_file, ...)
      if (is.null(local_var_response$status_code)) { # defensive: no status code means the call never reached the API
        local_var_response
      } else if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' [DEPRECATED] List of all available compounds (group of ion identities) in the given project-space
    #'
    #' @param project_id project-space to read from.
    #' @param ms_data_search_prepared (optional) Returns all fragment spectra in a preprocessed form as used for fast                             Cosine/Modified Cosine computation. Gives you spectra compatible with SpectralLibraryMatch                             peak assignments and reference spectra. (default value: FALSE)
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: ["none"])
    #' @param opt_fields_features (optional) No description (default value: ["none"])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[Compound]) with additional information such as HTTP status code, headers
    GetCompoundsWithHttpInfo = function(project_id, ms_data_search_prepared = FALSE, opt_fields = list("none"), opt_fields_features = list("none"), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }





      query_params[["msDataSearchPrepared"]] <- `ms_data_search_prepared`

      # explore
      for (query_item in `opt_fields`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("none", "consensusAnnotations", "consensusAnnotationsDeNovo", "customAnnotations", "tags"))) {
          stop("Invalid value for opt_fields when calling CompoundsApi$GetCompounds. Must be [none, consensusAnnotations, consensusAnnotationsDeNovo, customAnnotations, tags].")
        }
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      # explore
      for (query_item in `opt_fields_features`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("none", "msData", "topAnnotationsSummary", "topAnnotations", "topAnnotationsDeNovo", "computedTools", "qualities", "tags"))) {
          stop("Invalid value for opt_fields_features when calling CompoundsApi$GetCompounds. Must be [none, msData, topAnnotationsSummary, topAnnotations, topAnnotationsDeNovo, computedTools, qualities, tags].")
        }
        query_params[["optFieldsFeatures"]] <- c(query_params[["optFieldsFeatures"]], list(`optFieldsFeatures` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/compounds"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        # Check if we are expecting a CSV response
        is_csv_response <- any(grepl("csv", local_var_accepts, ignore.case = TRUE))

        if (is_csv_response) {
          # For CSV responses, parse into data.frame
          csv_resp_obj <- tryCatch(
            {
              csv_text <- rawToChar(local_var_resp$response)

              # Detect separator by examining first line
              first_line <- strsplit(csv_text, "\n")[[1]][1]
              if (grepl("\t", first_line)) {
                # Tab-separated (TSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = "\t")
              } else {
                # Comma-separated (CSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = ",")
              }
            },
            error = function(e) {
              stop("Failed to parse CSV response")
            }
          )
          local_var_resp$content <- csv_resp_obj
        } else {
          # For JSON responses, deserialize normally
          deserialized_resp_obj <- tryCatch(
            self$api_client$deserialize(local_var_resp$response_as_text(), "array[Compound]", loadNamespace("RSirius")),
            error = function(e) {
              stop("Failed to deserialize response")
            }
          )
          local_var_resp$content <- deserialized_resp_obj
        }
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (length(local_var_resp$response) == 0) {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Page of available compounds (group of ion identities) in the given project-space
    #'
    #' @param project_id project-space to read from.
    #' @param search_query (optional) Optional search query in lucene syntax. Not yet supported for compounds; a non-empty                      query responds with 405 METHOD_NOT_ALLOWED. Omit this parameter to page over all compounds.
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param ms_data_search_prepared (optional) Returns all fragment spectra in a preprocessed form as used for fast                             Cosine/Modified Cosine computation. Gives you spectra compatible with SpectralLibraryMatch                             peak assignments and reference spectra. (default value: FALSE)
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: ["none"])
    #' @param opt_fields_features (optional) No description (default value: ["none"])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PagedModelCompound
    GetCompoundsPage = function(project_id, search_query = NULL, page = 0, size = 20, sort = NULL, ms_data_search_prepared = FALSE, opt_fields = list("none"), opt_fields_features = list("none"), data_file = NULL, ...) {
      local_var_response <- self$GetCompoundsPageWithHttpInfo(project_id, search_query, page, size, sort, ms_data_search_prepared, opt_fields, opt_fields_features, data_file = data_file, ...)
      if (is.null(local_var_response$status_code)) { # defensive: no status code means the call never reached the API
        local_var_response
      } else if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Page of available compounds (group of ion identities) in the given project-space
    #'
    #' @param project_id project-space to read from.
    #' @param search_query (optional) Optional search query in lucene syntax. Not yet supported for compounds; a non-empty                      query responds with 405 METHOD_NOT_ALLOWED. Omit this parameter to page over all compounds.
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param ms_data_search_prepared (optional) Returns all fragment spectra in a preprocessed form as used for fast                             Cosine/Modified Cosine computation. Gives you spectra compatible with SpectralLibraryMatch                             peak assignments and reference spectra. (default value: FALSE)
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: ["none"])
    #' @param opt_fields_features (optional) No description (default value: ["none"])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PagedModelCompound) with additional information such as HTTP status code, headers
    GetCompoundsPageWithHttpInfo = function(project_id, search_query = NULL, page = 0, size = 20, sort = NULL, ms_data_search_prepared = FALSE, opt_fields = list("none"), opt_fields_features = list("none"), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }



      if (`page` < 0) {
        stop("Invalid value for `page` when calling CompoundsApi$GetCompoundsPage, must be bigger than or equal to 0.")
      }

      if (`size` < 1) {
        stop("Invalid value for `size` when calling CompoundsApi$GetCompoundsPage, must be bigger than or equal to 1.")
      }





      query_params[["searchQuery"]] <- `search_query`

      query_params[["page"]] <- `page`

      query_params[["size"]] <- `size`

      # explore
      for (query_item in `sort`) {
        query_params[["sort"]] <- c(query_params[["sort"]], list(`sort` = query_item))
      }

      query_params[["msDataSearchPrepared"]] <- `ms_data_search_prepared`

      # explore
      for (query_item in `opt_fields`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("none", "consensusAnnotations", "consensusAnnotationsDeNovo", "customAnnotations", "tags"))) {
          stop("Invalid value for opt_fields when calling CompoundsApi$GetCompoundsPage. Must be [none, consensusAnnotations, consensusAnnotationsDeNovo, customAnnotations, tags].")
        }
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      # explore
      for (query_item in `opt_fields_features`) {
        # validate enum values
        if (!is.null(query_item) && !(query_item %in% c("none", "msData", "topAnnotationsSummary", "topAnnotations", "topAnnotationsDeNovo", "computedTools", "qualities", "tags"))) {
          stop("Invalid value for opt_fields_features when calling CompoundsApi$GetCompoundsPage. Must be [none, msData, topAnnotationsSummary, topAnnotations, topAnnotationsDeNovo, computedTools, qualities, tags].")
        }
        query_params[["optFieldsFeatures"]] <- c(query_params[["optFieldsFeatures"]], list(`optFieldsFeatures` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/compounds/page"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        # Check if we are expecting a CSV response
        is_csv_response <- any(grepl("csv", local_var_accepts, ignore.case = TRUE))

        if (is_csv_response) {
          # For CSV responses, parse into data.frame
          csv_resp_obj <- tryCatch(
            {
              csv_text <- rawToChar(local_var_resp$response)

              # Detect separator by examining first line
              first_line <- strsplit(csv_text, "\n")[[1]][1]
              if (grepl("\t", first_line)) {
                # Tab-separated (TSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = "\t")
              } else {
                # Comma-separated (CSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = ",")
              }
            },
            error = function(e) {
              stop("Failed to parse CSV response")
            }
          )
          local_var_resp$content <- csv_resp_obj
        } else {
          # For JSON responses, deserialize normally
          deserialized_resp_obj <- tryCatch(
            self$api_client$deserialize(local_var_resp$response_as_text(), "PagedModelCompound", loadNamespace("RSirius")),
            error = function(e) {
              stop("Failed to deserialize response")
            }
          )
          local_var_resp$content <- deserialized_resp_obj
        }
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (length(local_var_resp$response) == 0) {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get all fields that can be used in the searchQuery parameter of compound endpoints
    #'
    #' @param project_id project space to read the searchable compound fields from.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[SearchableField]
    GetCompoundsSearchableFields = function(project_id, data_file = NULL, ...) {
      local_var_response <- self$GetCompoundsSearchableFieldsWithHttpInfo(project_id, data_file = data_file, ...)
      if (is.null(local_var_response$status_code)) { # defensive: no status code means the call never reached the API
        local_var_response
      } else if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get all fields that can be used in the searchQuery parameter of compound endpoints
    #'
    #' @param project_id project space to read the searchable compound fields from.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[SearchableField]) with additional information such as HTTP status code, headers
    GetCompoundsSearchableFieldsWithHttpInfo = function(project_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }


      local_var_url_path <- "/api/projects/{projectId}/compounds/searchable-fields"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        # Check if we are expecting a CSV response
        is_csv_response <- any(grepl("csv", local_var_accepts, ignore.case = TRUE))

        if (is_csv_response) {
          # For CSV responses, parse into data.frame
          csv_resp_obj <- tryCatch(
            {
              csv_text <- rawToChar(local_var_resp$response)

              # Detect separator by examining first line
              first_line <- strsplit(csv_text, "\n")[[1]][1]
              if (grepl("\t", first_line)) {
                # Tab-separated (TSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = "\t")
              } else {
                # Comma-separated (CSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = ",")
              }
            },
            error = function(e) {
              stop("Failed to parse CSV response")
            }
          )
          local_var_resp$content <- csv_resp_obj
        } else {
          # For JSON responses, deserialize normally
          deserialized_resp_obj <- tryCatch(
            self$api_client$deserialize(local_var_resp$response_as_text(), "array[SearchableField]", loadNamespace("RSirius")),
            error = function(e) {
              stop("Failed to deserialize response")
            }
          )
          local_var_resp$content <- deserialized_resp_obj
        }
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (length(local_var_resp$response) == 0) {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
