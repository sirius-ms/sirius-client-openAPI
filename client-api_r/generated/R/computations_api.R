#' SIRIUS Nightsky API
#'
#' REST API that provides the full functionality of SIRIUS and its web services as background service. It is intended as entry-point for scripting languages and software integration SDKs.
#'
#' The version of the OpenAPI document: v0.9 on SIRIUS 5.6.4-SNAPSHOT
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Computations operations
#' @description ComputationsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ DeleteJob } \emph{ Delete job. }
#' Delete job. Specify how to behave for running jobs.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } job_id character
#' \item \emph{ @param } cancel_if_running character
#' \item \emph{ @param } await_deletion character
#'
#'
#' \item status code : 202 | Accepted
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ DeleteJobConfig } \emph{ Delete job configuration with given name. }
#' Delete job configuration with given name.
#'
#' \itemize{
#' \item \emph{ @param } name character
#'
#'
#' \item status code : 202 | Accepted
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetDefaultJobConfig } \emph{ Request default job configuration }
#' Request default job configuration
#'
#' \itemize{
#' \item \emph{ @param } include_config_map character
#' \item \emph{ @returnType } \link{JobSubmission} \cr
#'
#'
#' \item status code : 200 | {@link JobSubmission JobSubmission} with all parameters set to default values.
#'
#' \item return type : JobSubmission
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetJob } \emph{ Get job information and its current state and progress (if available). }
#' Get job information and its current state and progress (if available).
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } job_id character
#' \item \emph{ @param } include_state character
#' \item \emph{ @param } include_command character
#' \item \emph{ @param } include_affected_compounds character
#' \item \emph{ @returnType } \link{JobId} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : JobId
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetJobConfig } \emph{ Request job configuration with given name. }
#' Request job configuration with given name.
#'
#' \itemize{
#' \item \emph{ @param } name character
#' \item \emph{ @param } include_config_map character
#' \item \emph{ @returnType } \link{JobSubmission} \cr
#'
#'
#' \item status code : 200 | {@link JobSubmission JobSubmission} for given name.
#'
#' \item return type : JobSubmission
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetJobConfigs } \emph{ Request all available job configurations }
#' Request all available job configurations
#'
#' \itemize{
#' \item \emph{ @param } include_config_map character
#' \item \emph{ @returnType } list( \link{JobSubmission} ) \cr
#'
#'
#' \item status code : 200 | list of available {@link JobSubmission JobSubmission}s
#'
#' \item return type : array[JobSubmission]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetJobs } \emph{ Get job information and its current state and progress (if available). }
#' Get job information and its current state and progress (if available).
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } include_state character
#' \item \emph{ @param } include_command character
#' \item \emph{ @param } include_affected_compounds character
#' \item \emph{ @returnType } list( \link{JobId} ) \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : array[JobId]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostJobConfig } \emph{ Add new job configuration with given name. }
#' Add new job configuration with given name.
#'
#' \itemize{
#' \item \emph{ @param } name character
#' \item \emph{ @param } job_submission \link{JobSubmission}
#' \item \emph{ @param } override_existing character
#'
#'
#' \item status code : 200 | Probably modified name of the config (to ensure filesystem path compatibility).
#'
#' \item return type : character
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ StartJob } \emph{ Start computation for given compounds and with given parameters. }
#' Start computation for given compounds and with given parameters.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } job_submission \link{JobSubmission}
#' \item \emph{ @param } include_state character
#' \item \emph{ @param } include_command character
#' \item \emph{ @param } include_affected_compounds character
#' \item \emph{ @returnType } \link{JobId} \cr
#'
#'
#' \item status code : 202 | Accepted
#'
#' \item return type : JobId
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ StartJobFromConfig } \emph{ Start computation for given compounds and with parameters from a stored job-config. }
#' Start computation for given compounds and with parameters from a stored job-config.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } job_config_name character
#' \item \emph{ @param } request_body list( character )
#' \item \emph{ @param } recompute character
#' \item \emph{ @param } include_state character
#' \item \emph{ @param } include_command character
#' \item \emph{ @param } include_affected_compounds character
#' \item \emph{ @returnType } \link{JobId} \cr
#'
#'
#' \item status code : 202 | Accepted
#'
#' \item return type : JobId
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  DeleteJob  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to run jobs on
#' var_job_id <- "job_id_example" # character | of the job to be deleted
#' var_cancel_if_running <- TRUE # character | If true job will be canceled if it is not finished. Otherwise,                         deletion will fail for running jobs or request will block until job has finished. (Optional)
#' var_await_deletion <- TRUE # character | If true request will block until deletion succeeded or failed.                         If the job is still running the request will wait until the job has finished. (Optional)
#'
#' #Delete job.
#' api_instance <- rsirius_api$new()
#'
#' api_instance$computations_api$DeleteJob(var_project_id, var_job_id, cancel_if_running = var_cancel_if_running, await_deletion = var_await_deletion)
#'
#'
#' ####################  DeleteJobConfig  ####################
#'
#' library(Rsirius)
#' var_name <- "name_example" # character | name of the job-config to delete
#'
#' #Delete job configuration with given name.
#' api_instance <- rsirius_api$new()
#'
#' api_instance$computations_api$DeleteJobConfig(var_name)
#'
#'
#' ####################  GetDefaultJobConfig  ####################
#'
#' library(Rsirius)
#' var_include_config_map <- FALSE # character | if true, generic configmap with-defaults will be included (Optional)
#'
#' #Request default job configuration
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetDefaultJobConfig(include_config_map = var_include_config_mapdata_file = "result.txt")
#' result <- api_instance$computations_api$GetDefaultJobConfig(include_config_map = var_include_config_map)
#' dput(result)
#'
#'
#' ####################  GetJob  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to run jobs on
#' var_job_id <- "job_id_example" # character | of the job to be returned
#' var_include_state <- TRUE # character | include {@link de.unijena.bioinf.ms.middleware.compute.model.JobProgress de.unijena.bioinf.ms.middleware.compute.model.JobProgress} state. (Optional)
#' var_include_command <- FALSE # character | include job command. (Optional)
#' var_include_affected_compounds <- FALSE # character | include list of compound ids affected by this job (if available) (Optional)
#'
#' #Get job information and its current state and progress (if available).
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetJob(var_project_id, var_job_id, include_state = var_include_state, include_command = var_include_command, include_affected_compounds = var_include_affected_compoundsdata_file = "result.txt")
#' result <- api_instance$computations_api$GetJob(var_project_id, var_job_id, include_state = var_include_state, include_command = var_include_command, include_affected_compounds = var_include_affected_compounds)
#' dput(result)
#'
#'
#' ####################  GetJobConfig  ####################
#'
#' library(Rsirius)
#' var_name <- "name_example" # character | name of the job-config to return
#' var_include_config_map <- FALSE # character | if true the generic configmap will be part of the output (Optional)
#'
#' #Request job configuration with given name.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetJobConfig(var_name, include_config_map = var_include_config_mapdata_file = "result.txt")
#' result <- api_instance$computations_api$GetJobConfig(var_name, include_config_map = var_include_config_map)
#' dput(result)
#'
#'
#' ####################  GetJobConfigs  ####################
#'
#' library(Rsirius)
#' var_include_config_map <- FALSE # character | if true the generic configmap will be part of the output (Optional)
#'
#' #Request all available job configurations
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetJobConfigs(include_config_map = var_include_config_mapdata_file = "result.txt")
#' result <- api_instance$computations_api$GetJobConfigs(include_config_map = var_include_config_map)
#' dput(result)
#'
#'
#' ####################  GetJobs  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to run jobs on
#' var_include_state <- FALSE # character | include {@link de.unijena.bioinf.ms.middleware.compute.model.JobProgress de.unijena.bioinf.ms.middleware.compute.model.JobProgress} states. (Optional)
#' var_include_command <- FALSE # character | include job commands. (Optional)
#' var_include_affected_compounds <- FALSE # character | include list of compound ids affected by this job (if available) (Optional)
#'
#' #Get job information and its current state and progress (if available).
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetJobs(var_project_id, include_state = var_include_state, include_command = var_include_command, include_affected_compounds = var_include_affected_compoundsdata_file = "result.txt")
#' result <- api_instance$computations_api$GetJobs(var_project_id, include_state = var_include_state, include_command = var_include_command, include_affected_compounds = var_include_affected_compounds)
#' dput(result)
#'
#'
#' ####################  PostJobConfig  ####################
#'
#' library(Rsirius)
#' var_name <- "name_example" # character | name of the job-config to add
#' var_job_submission <- JobSubmission$new(c("compoundIds_example"), c("fallbackAdducts_example"), c("enforcedAdducts_example"), c("detectableAdducts_example"), "recompute_example", Sirius$new("enabled_example", "QTOF", 123, 123, 123, "IGNORE", c("ALL"), "enforcedFormulaConstraints_example", "fallbackFormulaConstraints_example", c("detectableElements_example"), Timeout$new(123, 123, "identifier_example"), UseHeuristic$new(123, 123, "identifier_example")), Zodiac$new("enabled_example", ZodiacNumberOfConsideredCandidatesAt300Mz$new(123, "identifier_example"), ZodiacNumberOfConsideredCandidatesAt800Mz$new(123, "identifier_example"), ZodiacRunInTwoSteps$new("value_example", "identifier_example"), ZodiacEdgeFilterThresholds$new(123, 123, 123, "identifier_example"), ZodiacEpochs$new(123, 123, 123, "identifier_example")), FingerprintPrediction$new("enabled_example", "useScoreThreshold_example"), StructureDbSearch$new("enabled_example", c("ALL"), "tagLipids_example"), Canopus$new("enabled_example"), c(key = "inner_example")) # JobSubmission | to add
#' var_override_existing <- FALSE # character |  (Optional)
#'
#' #Add new job configuration with given name.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostJobConfig(var_name, var_job_submission, override_existing = var_override_existingdata_file = "result.txt")
#' result <- api_instance$computations_api$PostJobConfig(var_name, var_job_submission, override_existing = var_override_existing)
#' dput(result)
#'
#'
#' ####################  StartJob  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to run jobs on
#' var_job_submission <- JobSubmission$new(c("compoundIds_example"), c("fallbackAdducts_example"), c("enforcedAdducts_example"), c("detectableAdducts_example"), "recompute_example", Sirius$new("enabled_example", "QTOF", 123, 123, 123, "IGNORE", c("ALL"), "enforcedFormulaConstraints_example", "fallbackFormulaConstraints_example", c("detectableElements_example"), Timeout$new(123, 123, "identifier_example"), UseHeuristic$new(123, 123, "identifier_example")), Zodiac$new("enabled_example", ZodiacNumberOfConsideredCandidatesAt300Mz$new(123, "identifier_example"), ZodiacNumberOfConsideredCandidatesAt800Mz$new(123, "identifier_example"), ZodiacRunInTwoSteps$new("value_example", "identifier_example"), ZodiacEdgeFilterThresholds$new(123, 123, 123, "identifier_example"), ZodiacEpochs$new(123, 123, 123, "identifier_example")), FingerprintPrediction$new("enabled_example", "useScoreThreshold_example"), StructureDbSearch$new("enabled_example", c("ALL"), "tagLipids_example"), Canopus$new("enabled_example"), c(key = "inner_example")) # JobSubmission | configuration of the job that will be submitted of the job to be returned
#' var_include_state <- TRUE # character | include {@link de.unijena.bioinf.ms.middleware.compute.model.JobProgress de.unijena.bioinf.ms.middleware.compute.model.JobProgress} state. (Optional)
#' var_include_command <- TRUE # character | include job command. (Optional)
#' var_include_affected_compounds <- FALSE # character | include list of compound ids affected by this job (if available) (Optional)
#'
#' #Start computation for given compounds and with given parameters.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$StartJob(var_project_id, var_job_submission, include_state = var_include_state, include_command = var_include_command, include_affected_compounds = var_include_affected_compoundsdata_file = "result.txt")
#' result <- api_instance$computations_api$StartJob(var_project_id, var_job_submission, include_state = var_include_state, include_command = var_include_command, include_affected_compounds = var_include_affected_compounds)
#' dput(result)
#'
#'
#' ####################  StartJobFromConfig  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to run jobs on
#' var_job_config_name <- "job_config_name_example" # character | name if the config to be used
#' var_request_body <- c("property_example") # array[character] | compound ids to be computed
#' var_recompute <- "recompute_example" # character | enable or disable recompute. If null the stored value will be used. (Optional)
#' var_include_state <- TRUE # character | include {@link de.unijena.bioinf.ms.middleware.compute.model.JobProgress de.unijena.bioinf.ms.middleware.compute.model.JobProgress} state. (Optional)
#' var_include_command <- TRUE # character | include job command. (Optional)
#' var_include_affected_compounds <- FALSE # character | include list of compound ids affected by this job (if available) (Optional)
#'
#' #Start computation for given compounds and with parameters from a stored job-config.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$StartJobFromConfig(var_project_id, var_job_config_name, var_request_body, recompute = var_recompute, include_state = var_include_state, include_command = var_include_command, include_affected_compounds = var_include_affected_compoundsdata_file = "result.txt")
#' result <- api_instance$computations_api$StartJobFromConfig(var_project_id, var_job_config_name, var_request_body, recompute = var_recompute, include_state = var_include_state, include_command = var_include_command, include_affected_compounds = var_include_affected_compounds)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ComputationsApi <- R6::R6Class(
  "ComputationsApi",
  public = list(
    api_client = NULL,
    #' Initialize a new ComputationsApi.
    #'
    #' @description
    #' Initialize a new ComputationsApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Delete job.
    #'
    #' @description
    #' Delete job.
    #'
    #' @param project_id project-space to run jobs on
    #' @param job_id of the job to be deleted
    #' @param cancel_if_running (optional) If true job will be canceled if it is not finished. Otherwise,                         deletion will fail for running jobs or request will block until job has finished. (default value: TRUE)
    #' @param await_deletion (optional) If true request will block until deletion succeeded or failed.                         If the job is still running the request will wait until the job has finished. (default value: TRUE)
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    DeleteJob = function(project_id, job_id, cancel_if_running = TRUE, await_deletion = TRUE, ...) {
      local_var_response <- self$DeleteJobWithHttpInfo(project_id, job_id, cancel_if_running, await_deletion, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Delete job.
    #'
    #' @description
    #' Delete job.
    #'
    #' @param project_id project-space to run jobs on
    #' @param job_id of the job to be deleted
    #' @param cancel_if_running (optional) If true job will be canceled if it is not finished. Otherwise,                         deletion will fail for running jobs or request will block until job has finished. (default value: TRUE)
    #' @param await_deletion (optional) If true request will block until deletion succeeded or failed.                         If the job is still running the request will wait until the job has finished. (default value: TRUE)
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    DeleteJobWithHttpInfo = function(project_id, job_id, cancel_if_running = TRUE, await_deletion = TRUE, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }





      query_params[["cancelIfRunning"]] <- `cancel_if_running`

      query_params[["awaitDeletion"]] <- `await_deletion`

      local_var_url_path <- "/api/projects/{projectId}/jobs/{jobId}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{jobId\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Delete job configuration with given name.
    #'
    #' @description
    #' Delete job configuration with given name.
    #'
    #' @param name name of the job-config to delete
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    DeleteJobConfig = function(name, ...) {
      local_var_response <- self$DeleteJobConfigWithHttpInfo(name, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Delete job configuration with given name.
    #'
    #' @description
    #' Delete job configuration with given name.
    #'
    #' @param name name of the job-config to delete
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    DeleteJobConfigWithHttpInfo = function(name, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }


      local_var_url_path <- "/api/job-configs/{name}"
      if (!missing(`name`)) {
        local_var_url_path <- gsub("\\{name\\}", URLencode(as.character(`name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Request default job configuration
    #'
    #' @description
    #' Request default job configuration
    #'
    #' @param include_config_map (optional) if true, generic configmap with-defaults will be included (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return JobSubmission
    #' @export
    GetDefaultJobConfig = function(include_config_map = FALSE, data_file = NULL, ...) {
      local_var_response <- self$GetDefaultJobConfigWithHttpInfo(include_config_map, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Request default job configuration
    #'
    #' @description
    #' Request default job configuration
    #'
    #' @param include_config_map (optional) if true, generic configmap with-defaults will be included (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (JobSubmission) with additional information such as HTTP status code, headers
    #' @export
    GetDefaultJobConfigWithHttpInfo = function(include_config_map = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      query_params[["includeConfigMap"]] <- `include_config_map`

      local_var_url_path <- "/api/default-job-config"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "JobSubmission", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get job information and its current state and progress (if available).
    #'
    #' @description
    #' Get job information and its current state and progress (if available).
    #'
    #' @param project_id project-space to run jobs on
    #' @param job_id of the job to be returned
    #' @param include_state (optional) include {@link de.unijena.bioinf.ms.middleware.compute.model.JobProgress de.unijena.bioinf.ms.middleware.compute.model.JobProgress} state. (default value: TRUE)
    #' @param include_command (optional) include job command. (default value: FALSE)
    #' @param include_affected_compounds (optional) include list of compound ids affected by this job (if available) (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return JobId
    #' @export
    GetJob = function(project_id, job_id, include_state = TRUE, include_command = FALSE, include_affected_compounds = FALSE, data_file = NULL, ...) {
      local_var_response <- self$GetJobWithHttpInfo(project_id, job_id, include_state, include_command, include_affected_compounds, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get job information and its current state and progress (if available).
    #'
    #' @description
    #' Get job information and its current state and progress (if available).
    #'
    #' @param project_id project-space to run jobs on
    #' @param job_id of the job to be returned
    #' @param include_state (optional) include {@link de.unijena.bioinf.ms.middleware.compute.model.JobProgress de.unijena.bioinf.ms.middleware.compute.model.JobProgress} state. (default value: TRUE)
    #' @param include_command (optional) include job command. (default value: FALSE)
    #' @param include_affected_compounds (optional) include list of compound ids affected by this job (if available) (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (JobId) with additional information such as HTTP status code, headers
    #' @export
    GetJobWithHttpInfo = function(project_id, job_id, include_state = TRUE, include_command = FALSE, include_affected_compounds = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`job_id`)) {
        stop("Missing required parameter `job_id`.")
      }






      query_params[["includeState"]] <- `include_state`

      query_params[["includeCommand"]] <- `include_command`

      query_params[["includeAffectedCompounds"]] <- `include_affected_compounds`

      local_var_url_path <- "/api/projects/{projectId}/jobs/{jobId}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`job_id`)) {
        local_var_url_path <- gsub("\\{jobId\\}", URLencode(as.character(`job_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "JobId", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Request job configuration with given name.
    #'
    #' @description
    #' Request job configuration with given name.
    #'
    #' @param name name of the job-config to return
    #' @param include_config_map (optional) if true the generic configmap will be part of the output (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return JobSubmission
    #' @export
    GetJobConfig = function(name, include_config_map = FALSE, data_file = NULL, ...) {
      local_var_response <- self$GetJobConfigWithHttpInfo(name, include_config_map, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Request job configuration with given name.
    #'
    #' @description
    #' Request job configuration with given name.
    #'
    #' @param name name of the job-config to return
    #' @param include_config_map (optional) if true the generic configmap will be part of the output (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (JobSubmission) with additional information such as HTTP status code, headers
    #' @export
    GetJobConfigWithHttpInfo = function(name, include_config_map = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }



      query_params[["includeConfigMap"]] <- `include_config_map`

      local_var_url_path <- "/api/job-configs/{name}"
      if (!missing(`name`)) {
        local_var_url_path <- gsub("\\{name\\}", URLencode(as.character(`name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "JobSubmission", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Request all available job configurations
    #'
    #' @description
    #' Request all available job configurations
    #'
    #' @param include_config_map (optional) if true the generic configmap will be part of the output (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[JobSubmission]
    #' @export
    GetJobConfigs = function(include_config_map = FALSE, data_file = NULL, ...) {
      local_var_response <- self$GetJobConfigsWithHttpInfo(include_config_map, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Request all available job configurations
    #'
    #' @description
    #' Request all available job configurations
    #'
    #' @param include_config_map (optional) if true the generic configmap will be part of the output (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[JobSubmission]) with additional information such as HTTP status code, headers
    #' @export
    GetJobConfigsWithHttpInfo = function(include_config_map = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      query_params[["includeConfigMap"]] <- `include_config_map`

      local_var_url_path <- "/api/job-configs"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[JobSubmission]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get job information and its current state and progress (if available).
    #'
    #' @description
    #' Get job information and its current state and progress (if available).
    #'
    #' @param project_id project-space to run jobs on
    #' @param include_state (optional) include {@link de.unijena.bioinf.ms.middleware.compute.model.JobProgress de.unijena.bioinf.ms.middleware.compute.model.JobProgress} states. (default value: FALSE)
    #' @param include_command (optional) include job commands. (default value: FALSE)
    #' @param include_affected_compounds (optional) include list of compound ids affected by this job (if available) (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[JobId]
    #' @export
    GetJobs = function(project_id, include_state = FALSE, include_command = FALSE, include_affected_compounds = FALSE, data_file = NULL, ...) {
      local_var_response <- self$GetJobsWithHttpInfo(project_id, include_state, include_command, include_affected_compounds, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get job information and its current state and progress (if available).
    #'
    #' @description
    #' Get job information and its current state and progress (if available).
    #'
    #' @param project_id project-space to run jobs on
    #' @param include_state (optional) include {@link de.unijena.bioinf.ms.middleware.compute.model.JobProgress de.unijena.bioinf.ms.middleware.compute.model.JobProgress} states. (default value: FALSE)
    #' @param include_command (optional) include job commands. (default value: FALSE)
    #' @param include_affected_compounds (optional) include list of compound ids affected by this job (if available) (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[JobId]) with additional information such as HTTP status code, headers
    #' @export
    GetJobsWithHttpInfo = function(project_id, include_state = FALSE, include_command = FALSE, include_affected_compounds = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }





      query_params[["includeState"]] <- `include_state`

      query_params[["includeCommand"]] <- `include_command`

      query_params[["includeAffectedCompounds"]] <- `include_affected_compounds`

      local_var_url_path <- "/api/projects/{projectId}/jobs"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[JobId]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Add new job configuration with given name.
    #'
    #' @description
    #' Add new job configuration with given name.
    #'
    #' @param name name of the job-config to add
    #' @param job_submission to add
    #' @param override_existing (optional) No description (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return character
    #' @export
    PostJobConfig = function(name, job_submission, override_existing = FALSE, data_file = NULL, ...) {
      local_var_response <- self$PostJobConfigWithHttpInfo(name, job_submission, override_existing, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Add new job configuration with given name.
    #'
    #' @description
    #' Add new job configuration with given name.
    #'
    #' @param name name of the job-config to add
    #' @param job_submission to add
    #' @param override_existing (optional) No description (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (character) with additional information such as HTTP status code, headers
    #' @export
    PostJobConfigWithHttpInfo = function(name, job_submission, override_existing = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`name`)) {
        stop("Missing required parameter `name`.")
      }

      if (missing(`job_submission`)) {
        stop("Missing required parameter `job_submission`.")
      }




      query_params[["overrideExisting"]] <- `override_existing`

      if (!is.null(`job_submission`)) {
        local_var_body <- `job_submission`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/job-configs/{name}"
      if (!missing(`name`)) {
        local_var_url_path <- gsub("\\{name\\}", URLencode(as.character(`name`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(jsonlite::toJSON(local_var_resp$response), "character", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Start computation for given compounds and with given parameters.
    #'
    #' @description
    #' Start computation for given compounds and with given parameters.
    #'
    #' @param project_id project-space to run jobs on
    #' @param job_submission configuration of the job that will be submitted of the job to be returned
    #' @param include_state (optional) include {@link de.unijena.bioinf.ms.middleware.compute.model.JobProgress de.unijena.bioinf.ms.middleware.compute.model.JobProgress} state. (default value: TRUE)
    #' @param include_command (optional) include job command. (default value: TRUE)
    #' @param include_affected_compounds (optional) include list of compound ids affected by this job (if available) (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return JobId
    #' @export
    StartJob = function(project_id, job_submission, include_state = TRUE, include_command = TRUE, include_affected_compounds = FALSE, data_file = NULL, ...) {
      local_var_response <- self$StartJobWithHttpInfo(project_id, job_submission, include_state, include_command, include_affected_compounds, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Start computation for given compounds and with given parameters.
    #'
    #' @description
    #' Start computation for given compounds and with given parameters.
    #'
    #' @param project_id project-space to run jobs on
    #' @param job_submission configuration of the job that will be submitted of the job to be returned
    #' @param include_state (optional) include {@link de.unijena.bioinf.ms.middleware.compute.model.JobProgress de.unijena.bioinf.ms.middleware.compute.model.JobProgress} state. (default value: TRUE)
    #' @param include_command (optional) include job command. (default value: TRUE)
    #' @param include_affected_compounds (optional) include list of compound ids affected by this job (if available) (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (JobId) with additional information such as HTTP status code, headers
    #' @export
    StartJobWithHttpInfo = function(project_id, job_submission, include_state = TRUE, include_command = TRUE, include_affected_compounds = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`job_submission`)) {
        stop("Missing required parameter `job_submission`.")
      }






      query_params[["includeState"]] <- `include_state`

      query_params[["includeCommand"]] <- `include_command`

      query_params[["includeAffectedCompounds"]] <- `include_affected_compounds`

      if (!is.null(`job_submission`)) {
        local_var_body <- `job_submission`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/projects/{projectId}/jobs"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "JobId", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Start computation for given compounds and with parameters from a stored job-config.
    #'
    #' @description
    #' Start computation for given compounds and with parameters from a stored job-config.
    #'
    #' @param project_id project-space to run jobs on
    #' @param job_config_name name if the config to be used
    #' @param request_body compound ids to be computed
    #' @param recompute (optional) enable or disable recompute. If null the stored value will be used.
    #' @param include_state (optional) include {@link de.unijena.bioinf.ms.middleware.compute.model.JobProgress de.unijena.bioinf.ms.middleware.compute.model.JobProgress} state. (default value: TRUE)
    #' @param include_command (optional) include job command. (default value: TRUE)
    #' @param include_affected_compounds (optional) include list of compound ids affected by this job (if available) (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return JobId
    #' @export
    StartJobFromConfig = function(project_id, job_config_name, request_body, recompute = NULL, include_state = TRUE, include_command = TRUE, include_affected_compounds = FALSE, data_file = NULL, ...) {
      local_var_response <- self$StartJobFromConfigWithHttpInfo(project_id, job_config_name, request_body, recompute, include_state, include_command, include_affected_compounds, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Start computation for given compounds and with parameters from a stored job-config.
    #'
    #' @description
    #' Start computation for given compounds and with parameters from a stored job-config.
    #'
    #' @param project_id project-space to run jobs on
    #' @param job_config_name name if the config to be used
    #' @param request_body compound ids to be computed
    #' @param recompute (optional) enable or disable recompute. If null the stored value will be used.
    #' @param include_state (optional) include {@link de.unijena.bioinf.ms.middleware.compute.model.JobProgress de.unijena.bioinf.ms.middleware.compute.model.JobProgress} state. (default value: TRUE)
    #' @param include_command (optional) include job command. (default value: TRUE)
    #' @param include_affected_compounds (optional) include list of compound ids affected by this job (if available) (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (JobId) with additional information such as HTTP status code, headers
    #' @export
    StartJobFromConfigWithHttpInfo = function(project_id, job_config_name, request_body, recompute = NULL, include_state = TRUE, include_command = TRUE, include_affected_compounds = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`job_config_name`)) {
        stop("Missing required parameter `job_config_name`.")
      }

      if (missing(`request_body`)) {
        stop("Missing required parameter `request_body`.")
      }








      query_params[["jobConfigName"]] <- `job_config_name`

      query_params[["recompute"]] <- `recompute`

      query_params[["includeState"]] <- `include_state`

      query_params[["includeCommand"]] <- `include_command`

      query_params[["includeAffectedCompounds"]] <- `include_affected_compounds`

      if (!is.null(`request_body`)) {
        local_var_body <- jsonlite::toJSON(`request_body`)
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/projects/{projectId}/jobs-from-config"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "JobId", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
