#' SIRIUS Nightsky API
#'
#' REST API that provides the full functionality of SIRIUS and its web services as background service. It is intended as entry-point for scripting languages and software integration SDKs.This API is exposed by SIRIUS 6.0.0-SNAPSHOT
#'
#' The version of the OpenAPI document: 2.0
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title ExperimentalGUI operations
#' @description ExperimentalGUIApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ ApplyToGui } \emph{ Apply given changes to the running GUI instance. }
#' Apply given changes to the running GUI instance.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } gui_parameters \link{GuiParameters}
#'
#'
#' \item status code : 200 | OK
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CloseGui } \emph{ Close GUI instance of given project-space if available. }
#' Close GUI instance of given project-space if available.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } close_project character
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : character
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetGuis } \emph{ Get list of currently running gui windows, managed by this SIRIUS instance. }
#' Get list of currently running gui windows, managed by this SIRIUS instance.  Note this will not show any Clients that are connected from a separate process!
#'
#' \itemize{
#' \item \emph{ @param } page integer
#' \item \emph{ @param } size integer
#' \item \emph{ @param } sort list( character )
#' \item \emph{ @returnType } \link{PageGuiInfo} \cr
#'
#'
#' \item status code : 200 | List of GUI windows that are currently managed by this SIRIUS instance.
#'
#' \item return type : PageGuiInfo
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ OpenGui } \emph{ Open GUI instance on specified project-space and bring the GUI window to foreground. }
#' Open GUI instance on specified project-space and bring the GUI window to foreground.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } read_only character
#' \item \emph{ @param } gui_parameters \link{GuiParameters}
#'
#'
#' \item status code : 201 | Created
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  ApplyToGui  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | of project-space the GUI instance is connected to.
#' var_gui_parameters <- GuiParameters$new(GuiResultTab$new(), "cid_example", "fid_example", "structureCandidateInChIKey_example", "bringToFront_example") # GuiParameters | parameters that should be applied.
#'
#' #Apply given changes to the running GUI instance.
#' api_instance <- rsirius_api$new()
#'
#' api_instance$experimental_gui_api$ApplyToGui(var_project_id, var_gui_parameters)
#'
#'
#' ####################  CloseGui  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | if project-space the GUI instance is connected to.
#' var_close_project <- "close_project_example" # character |  (Optional)
#'
#' #Close GUI instance of given project-space if available.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CloseGui(var_project_id, close_project = var_close_projectdata_file = "result.txt")
#' result <- api_instance$experimental_gui_api$CloseGui(var_project_id, close_project = var_close_project)
#' dput(result)
#'
#'
#' ####################  GetGuis  ####################
#'
#' library(Rsirius)
#' var_page <- 0 # integer | Zero-based page index (0..N) (Optional)
#' var_size <- 20 # integer | The size of the page to be returned (Optional)
#' var_sort <- c("inner_example") # array[character] | Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported. (Optional)
#'
#' #Get list of currently running gui windows, managed by this SIRIUS instance.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetGuis(page = var_page, size = var_size, sort = var_sortdata_file = "result.txt")
#' result <- api_instance$experimental_gui_api$GetGuis(page = var_page, size = var_size, sort = var_sort)
#' dput(result)
#'
#'
#' ####################  OpenGui  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | of project-space the GUI instance will connect to.
#' var_read_only <- TRUE # character | open in read-only mode. (Optional)
#' var_gui_parameters <- GuiParameters$new(GuiResultTab$new(), "cid_example", "fid_example", "structureCandidateInChIKey_example", "bringToFront_example") # GuiParameters |  (Optional)
#'
#' #Open GUI instance on specified project-space and bring the GUI window to foreground.
#' api_instance <- rsirius_api$new()
#'
#' api_instance$experimental_gui_api$OpenGui(var_project_id, read_only = var_read_only, gui_parameters = var_gui_parameters)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ExperimentalGUIApi <- R6::R6Class(
  "ExperimentalGUIApi",
  public = list(
    api_client = NULL,
    #' Initialize a new ExperimentalGUIApi.
    #'
    #' @description
    #' Initialize a new ExperimentalGUIApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Apply given changes to the running GUI instance.
    #'
    #' @description
    #' Apply given changes to the running GUI instance.
    #'
    #' @param project_id of project-space the GUI instance is connected to.
    #' @param gui_parameters parameters that should be applied.
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    ApplyToGui = function(project_id, gui_parameters, ...) {
      local_var_response <- self$ApplyToGuiWithHttpInfo(project_id, gui_parameters, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Apply given changes to the running GUI instance.
    #'
    #' @description
    #' Apply given changes to the running GUI instance.
    #'
    #' @param project_id of project-space the GUI instance is connected to.
    #' @param gui_parameters parameters that should be applied.
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    ApplyToGuiWithHttpInfo = function(project_id, gui_parameters, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`gui_parameters`)) {
        stop("Missing required parameter `gui_parameters`.")
      }



      if (!is.null(`gui_parameters`)) {
        local_var_body <- `gui_parameters`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/projects/{projectId}/gui"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PATCH",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Close GUI instance of given project-space if available.
    #'
    #' @description
    #' Close GUI instance of given project-space if available.
    #'
    #' @param project_id if project-space the GUI instance is connected to.
    #' @param close_project (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return character
    #' @export
    CloseGui = function(project_id, close_project = NULL, data_file = NULL, ...) {
      local_var_response <- self$CloseGuiWithHttpInfo(project_id, close_project, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Close GUI instance of given project-space if available.
    #'
    #' @description
    #' Close GUI instance of given project-space if available.
    #'
    #' @param project_id if project-space the GUI instance is connected to.
    #' @param close_project (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (character) with additional information such as HTTP status code, headers
    #' @export
    CloseGuiWithHttpInfo = function(project_id, close_project = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }



      query_params[["closeProject"]] <- `close_project`

      local_var_url_path <- "/api/projects/{projectId}/gui"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "character", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get list of currently running gui windows, managed by this SIRIUS instance.
    #'
    #' @description
    #' Get list of currently running gui windows, managed by this SIRIUS instance.
    #'
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PageGuiInfo
    #' @export
    GetGuis = function(page = 0, size = 20, sort = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetGuisWithHttpInfo(page, size, sort, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get list of currently running gui windows, managed by this SIRIUS instance.
    #'
    #' @description
    #' Get list of currently running gui windows, managed by this SIRIUS instance.
    #'
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PageGuiInfo) with additional information such as HTTP status code, headers
    #' @export
    GetGuisWithHttpInfo = function(page = 0, size = 20, sort = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (`page` < 0) {
        stop("Invalid value for `page` when calling ExperimentalGUIApi$GetGuis, must be bigger than or equal to 0.")
      }

      if (`size` < 1) {
        stop("Invalid value for `size` when calling ExperimentalGUIApi$GetGuis, must be bigger than or equal to 1.")
      }


      query_params[["page"]] <- `page`

      query_params[["size"]] <- `size`

      # explore
      for (query_item in `sort`) {
        query_params[["sort"]] <- c(query_params[["sort"]], list(`sort` = query_item))
      }

      local_var_url_path <- "/api/guis"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PageGuiInfo", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Open GUI instance on specified project-space and bring the GUI window to foreground.
    #'
    #' @description
    #' Open GUI instance on specified project-space and bring the GUI window to foreground.
    #'
    #' @param project_id of project-space the GUI instance will connect to.
    #' @param read_only (optional) open in read-only mode. (default value: TRUE)
    #' @param gui_parameters (optional) No description
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    OpenGui = function(project_id, read_only = TRUE, gui_parameters = NULL, ...) {
      local_var_response <- self$OpenGuiWithHttpInfo(project_id, read_only, gui_parameters, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Open GUI instance on specified project-space and bring the GUI window to foreground.
    #'
    #' @description
    #' Open GUI instance on specified project-space and bring the GUI window to foreground.
    #'
    #' @param project_id of project-space the GUI instance will connect to.
    #' @param read_only (optional) open in read-only mode. (default value: TRUE)
    #' @param gui_parameters (optional) No description
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    OpenGuiWithHttpInfo = function(project_id, read_only = TRUE, gui_parameters = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }




      query_params[["readOnly"]] <- `read_only`

      if (!is.null(`gui_parameters`)) {
        local_var_body <- `gui_parameters`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/projects/{projectId}/gui"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
