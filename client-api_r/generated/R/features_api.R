#' SIRIUS Nightsky API
#'
#' REST API that provides the full functionality of SIRIUS and its web services as background service. It is intended as entry-point for scripting languages and software integration SDKs.This API is exposed by SIRIUS 6
#'
#' The version of the OpenAPI document: 2.1
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Features operations
#' @description FeaturesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ AddAlignedFeatures } \emph{ Import (aligned) features into the project. }
#' Import (aligned) features into the project. Features must not exist in the project.  Otherwise, they will exist twice.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } feature_import list( \link{FeatureImport} )
#' \item \emph{ @param } profile \link{InstrumentProfile}
#' \item \emph{ @param } opt_fields list( \link{AlignedFeatureOptField} )
#' \item \emph{ @returnType } list( \link{AlignedFeature} ) \cr
#'
#'
#' \item status code : 200 | the Features that have been imported with specified optional fields
#'
#' \item return type : array[AlignedFeature]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ DeleteAlignedFeature } \emph{ Delete feature (aligned over runs) with the given identifier from the specified project-space. }
#' Delete feature (aligned over runs) with the given identifier from the specified project-space.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#'
#'
#' \item status code : 200 | OK
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ DeleteAlignedFeatures } \emph{ Delete feature (aligned over runs) with the given identifier from the specified project-space. }
#' Delete feature (aligned over runs) with the given identifier from the specified project-space.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } request_body list( character )
#'
#'
#' \item status code : 200 | OK
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetAlignedFeature } \emph{ Get feature (aligned over runs) with the given identifier from the specified project-space. }
#' Get feature (aligned over runs) with the given identifier from the specified project-space.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } opt_fields list( \link{AlignedFeatureOptField} )
#' \item \emph{ @returnType } \link{AlignedFeature} \cr
#'
#'
#' \item status code : 200 | AlignedFeature with additional annotations and MS/MS data (if specified).
#'
#' \item return type : AlignedFeature
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetAlignedFeatures } \emph{ Get all available features (aligned over runs) in the given project-space. }
#' Get all available features (aligned over runs) in the given project-space.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } opt_fields list( \link{AlignedFeatureOptField} )
#' \item \emph{ @returnType } list( \link{AlignedFeature} ) \cr
#'
#'
#' \item status code : 200 | AlignedFeatures with additional annotations and MS/MS data (if specified).
#'
#' \item return type : array[AlignedFeature]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetAlignedFeaturesPaged } \emph{ Get all available features (aligned over runs) in the given project-space. }
#' Get all available features (aligned over runs) in the given project-space.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } page integer
#' \item \emph{ @param } size integer
#' \item \emph{ @param } sort list( character )
#' \item \emph{ @param } opt_fields list( \link{AlignedFeatureOptField} )
#' \item \emph{ @returnType } \link{PageAlignedFeature} \cr
#'
#'
#' \item status code : 200 | AlignedFeatures with additional annotations and MS/MS data (if specified).
#'
#' \item return type : PageAlignedFeature
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetAlignedFeaturesQuality } \emph{ Get data quality information for feature (aligned over runs) with the given identifier from the specified project-space. }
#' Get data quality information for feature (aligned over runs) with the given identifier from the specified project-space.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @returnType } \link{AlignedFeatureQuality} \cr
#'
#'
#' \item status code : 200 | AlignedFeatureQuality quality information of the respective feature.
#'
#' \item return type : AlignedFeatureQuality
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetBestMatchingCompoundClasses } \emph{ Best matching compound classes,  Set of the highest scoring compound classes (CANOPUS) on each hierarchy level of  the ClassyFire and NPC ontology, }
#' Best matching compound classes,  Set of the highest scoring compound classes (CANOPUS) on each hierarchy level of  the ClassyFire and NPC ontology,
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } formula_id character
#' \item \emph{ @returnType } \link{CompoundClasses} \cr
#'
#'
#' \item status code : 200 | Best matching Predicted compound classes
#'
#' \item return type : CompoundClasses
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetCanopusPrediction } \emph{ All predicted compound classes (CANOPUS) from ClassyFire and NPC and their probabilities, }
#' All predicted compound classes (CANOPUS) from ClassyFire and NPC and their probabilities,
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } formula_id character
#' \item \emph{ @returnType } \link{CanopusPrediction} \cr
#'
#'
#' \item status code : 200 | Predicted compound classes
#'
#' \item return type : CanopusPrediction
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetDeNovoStructureCandidates } \emph{ List of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given &#39;alignedFeatureId&#39; with minimal information.  StructureCandidates can be enriched with molecular fingerprint. }
#' List of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } opt_fields list( \link{StructureCandidateOptField} )
#' \item \emph{ @returnType } list( \link{StructureCandidateFormula} ) \cr
#'
#'
#' \item status code : 200 | StructureCandidate of this feature (aligned over runs) candidate with specified optional fields.
#'
#' \item return type : array[StructureCandidateFormula]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetDeNovoStructureCandidatesByFormula } \emph{ List of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given &#39;formulaId&#39; with minimal information.  StructureCandidates can be enriched with molecular fingerprint. }
#' List of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'formulaId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } formula_id character
#' \item \emph{ @param } opt_fields list( \link{StructureCandidateOptField} )
#' \item \emph{ @returnType } list( \link{StructureCandidateScored} ) \cr
#'
#'
#' \item status code : 200 | StructureCandidate of this formula candidate with specified optional fields.
#'
#' \item return type : array[StructureCandidateScored]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetDeNovoStructureCandidatesByFormulaPaged } \emph{ Page of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given &#39;formulaId&#39; with minimal information.  StructureCandidates can be enriched with molecular fingerprint. }
#' Page of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'formulaId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } formula_id character
#' \item \emph{ @param } page integer
#' \item \emph{ @param } size integer
#' \item \emph{ @param } sort list( character )
#' \item \emph{ @param } opt_fields list( \link{StructureCandidateOptField} )
#' \item \emph{ @returnType } \link{PageStructureCandidateScored} \cr
#'
#'
#' \item status code : 200 | StructureCandidate of this formula candidate with specified optional fields.
#'
#' \item return type : PageStructureCandidateScored
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetDeNovoStructureCandidatesPaged } \emph{ Page of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given &#39;alignedFeatureId&#39; with minimal information.  StructureCandidates can be enriched with molecular fingerprint. }
#' Page of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } page integer
#' \item \emph{ @param } size integer
#' \item \emph{ @param } sort list( character )
#' \item \emph{ @param } opt_fields list( \link{StructureCandidateOptField} )
#' \item \emph{ @returnType } \link{PageStructureCandidateFormula} \cr
#'
#'
#' \item status code : 200 | StructureCandidate of this feature (aligned over runs) candidate with specified optional fields.
#'
#' \item return type : PageStructureCandidateFormula
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetFingerprintPrediction } \emph{ Returns predicted fingerprint (CSI:FingerID) for the given formula result identifier  This fingerprint is used to perform structure database search and predict compound classes. }
#' Returns predicted fingerprint (CSI:FingerID) for the given formula result identifier  This fingerprint is used to perform structure database search and predict compound classes.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } formula_id character
#'
#'
#' \item status code : 200 | probabilistic fingerprint predicted by CSI:FingerID
#'
#' \item return type : array[numeric]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetFormulaAnnotatedMsMsData } \emph{ Returns MS/MS Spectrum (Merged MS/MS and measured MS/MS) which is annotated with fragments and losses  for the given formula result identifier  These annotations are only available if a fragmentation tree and the structure candidate are available. }
#' Returns MS/MS Spectrum (Merged MS/MS and measured MS/MS) which is annotated with fragments and losses  for the given formula result identifier  These annotations are only available if a fragmentation tree and the structure candidate are available.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } formula_id character
#' \item \emph{ @returnType } \link{AnnotatedMsMsData} \cr
#'
#'
#' \item status code : 200 | Fragmentation spectra annotated with fragment formulas and losses.
#'
#' \item return type : AnnotatedMsMsData
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetFormulaAnnotatedSpectrum } \emph{ Returns a fragmentation spectrum (e.g. Merged MS/MS) which is annotated with fragments and losses for the given formula result identifier  These annotations are only available if a fragmentation tree is available. }
#' Returns a fragmentation spectrum (e.g. Merged MS/MS) which is annotated with fragments and losses for the given formula result identifier  These annotations are only available if a fragmentation tree is available.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } formula_id character
#' \item \emph{ @param } spectrum_index integer
#' \item \emph{ @returnType } \link{AnnotatedSpectrum} \cr
#'
#'
#' \item status code : 200 | Fragmentation spectrum annotated with fragment formulas and losses.
#'
#' \item return type : AnnotatedSpectrum
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetFormulaCandidate } \emph{ FormulaResultContainers for the given &#39;formulaId&#39; with minimal information. }
#' FormulaResultContainers for the given 'formulaId' with minimal information.  Can be enriched with an optional results overview and formula candidate information.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } formula_id character
#' \item \emph{ @param } opt_fields list( \link{FormulaCandidateOptField} )
#' \item \emph{ @returnType } \link{FormulaCandidate} \cr
#'
#'
#' \item status code : 200 | FormulaCandidate of this feature (aligned over runs) with.
#'
#' \item return type : FormulaCandidate
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetFormulaCandidates } \emph{ List of FormulaResultContainers available for this feature with minimal information. }
#' List of FormulaResultContainers available for this feature with minimal information.  Can be enriched with an optional results overview.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } opt_fields list( \link{FormulaCandidateOptField} )
#' \item \emph{ @returnType } list( \link{FormulaCandidate} ) \cr
#'
#'
#' \item status code : 200 | All FormulaCandidate of this feature with.
#'
#' \item return type : array[FormulaCandidate]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetFormulaCandidatesPaged } \emph{ Page of FormulaResultContainers available for this feature with minimal information. }
#' Page of FormulaResultContainers available for this feature with minimal information.  Can be enriched with an optional results overview.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } page integer
#' \item \emph{ @param } size integer
#' \item \emph{ @param } sort list( character )
#' \item \emph{ @param } opt_fields list( \link{FormulaCandidateOptField} )
#' \item \emph{ @returnType } \link{PageFormulaCandidate} \cr
#'
#'
#' \item status code : 200 | All FormulaCandidate of this feature with.
#'
#' \item return type : PageFormulaCandidate
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetFragTree } \emph{ Returns fragmentation tree (SIRIUS) for the given formula result identifier  This tree is used to rank formula candidates (treeScore). }
#' Returns fragmentation tree (SIRIUS) for the given formula result identifier  This tree is used to rank formula candidates (treeScore).
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } formula_id character
#' \item \emph{ @returnType } \link{FragmentationTree} \cr
#'
#'
#' \item status code : 200 | Fragmentation Tree
#'
#' \item return type : FragmentationTree
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetIsotopePatternAnnotation } \emph{ Returns Isotope pattern information (simulated isotope pattern, measured isotope pattern, isotope pattern highlighting)  for the given formula result identifier. }
#' Returns Isotope pattern information (simulated isotope pattern, measured isotope pattern, isotope pattern highlighting)  for the given formula result identifier. This simulated isotope pattern is used to rank formula candidates (treeScore).
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } formula_id character
#' \item \emph{ @returnType } \link{IsotopePatternAnnotation} \cr
#'
#'
#' \item status code : 200 | Isotope pattern information
#'
#' \item return type : IsotopePatternAnnotation
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetLipidAnnotation } \emph{ Returns Lipid annotation (ElGordo) for the given formula result identifier. }
#' Returns Lipid annotation (ElGordo) for the given formula result identifier.  ElGordo lipid annotation runs as part of the SIRIUS formula identification step.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } formula_id character
#' \item \emph{ @returnType } \link{LipidAnnotation} \cr
#'
#'
#' \item status code : 200 | LipidAnnotation
#'
#' \item return type : LipidAnnotation
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetMsData } \emph{ Mass Spec data (input data) for the given &#39;alignedFeatureId&#39; . }
#' Mass Spec data (input data) for the given 'alignedFeatureId' .
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @returnType } \link{MsData} \cr
#'
#'
#' \item status code : 200 | Mass Spec data of this feature (aligned over runs).
#'
#' \item return type : MsData
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetQuantification } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } type Enum < [APEX_HEIGHT] >
#' \item \emph{ @returnType } \link{QuantificationTable} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : QuantificationTable
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetSiriusFragTree } \emph{ Returns fragmentation tree (SIRIUS) for the given formula result identifier in SIRIUS&#39; internal format. }
#' Returns fragmentation tree (SIRIUS) for the given formula result identifier in SIRIUS' internal format.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } formula_id character
#'
#'
#' \item status code : 200 | Fragmentation Tree in internal format.  <p>  NOTE: This endpoint is likely to be removed in future versions of the API.
#'
#' \item return type : character
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetSpectralLibraryMatch } \emph{ List of spectral library matches for the given &#39;alignedFeatureId&#39;. }
#' List of spectral library matches for the given 'alignedFeatureId'.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } match_id character
#' \item \emph{ @param } opt_fields list( \link{SpectralLibraryMatchOptField} )
#' \item \emph{ @returnType } \link{SpectralLibraryMatch} \cr
#'
#'
#' \item status code : 200 | Spectral library matches of this feature (aligned over runs).
#'
#' \item return type : SpectralLibraryMatch
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetSpectralLibraryMatches } \emph{ List of spectral library matches for the given &#39;alignedFeatureId&#39;. }
#' List of spectral library matches for the given 'alignedFeatureId'.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } min_shared_peaks integer
#' \item \emph{ @param } min_similarity numeric
#' \item \emph{ @param } candidate_in_chi_key character
#' \item \emph{ @param } opt_fields list( \link{SpectralLibraryMatchOptField} )
#' \item \emph{ @returnType } list( \link{SpectralLibraryMatch} ) \cr
#'
#'
#' \item status code : 200 | Spectral library matches of this feature (aligned over runs).
#'
#' \item return type : array[SpectralLibraryMatch]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetSpectralLibraryMatchesPaged } \emph{ Page of spectral library matches for the given &#39;alignedFeatureId&#39;. }
#' Page of spectral library matches for the given 'alignedFeatureId'.  If a 'candidateInChiKey' is provided, returns only matches for the database compound with the given InChI key.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } page integer
#' \item \emph{ @param } size integer
#' \item \emph{ @param } sort list( character )
#' \item \emph{ @param } min_shared_peaks integer
#' \item \emph{ @param } min_similarity numeric
#' \item \emph{ @param } candidate_in_chi_key character
#' \item \emph{ @param } opt_fields list( \link{SpectralLibraryMatchOptField} )
#' \item \emph{ @returnType } \link{PageSpectralLibraryMatch} \cr
#'
#'
#' \item status code : 200 | Spectral library matches of this feature (aligned over runs).
#'
#' \item return type : PageSpectralLibraryMatch
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetSpectralLibraryMatchesSummary } \emph{ Summarize matched reference spectra for the given &#39;alignedFeatureId&#39;. }
#' Summarize matched reference spectra for the given 'alignedFeatureId'.  If a 'candidateInChiKey' is provided, summarizes only matches for the database compound with the given InChI key.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } min_shared_peaks integer
#' \item \emph{ @param } min_similarity numeric
#' \item \emph{ @param } candidate_in_chi_key character
#' \item \emph{ @returnType } \link{SpectralLibraryMatchSummary} \cr
#'
#'
#' \item status code : 200 | Summary object with best match, number of spectral library matches, matched reference spectra and matched database compounds of this feature (aligned over runs).
#'
#' \item return type : SpectralLibraryMatchSummary
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetStructureAnnotatedMsData } \emph{ Returns MS/MS Data (Merged MS/MS and list of measured MS/MS ) which are annotated with fragments and losses  for the given formula result identifier and structure candidate inChIKey. }
#' Returns MS/MS Data (Merged MS/MS and list of measured MS/MS ) which are annotated with fragments and losses  for the given formula result identifier and structure candidate inChIKey.  These annotations are only available if a fragmentation tree and the structure candidate are available.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } formula_id character
#' \item \emph{ @param } inchi_key character
#' \item \emph{ @returnType } \link{AnnotatedMsMsData} \cr
#'
#'
#' \item status code : 200 | Fragmentation spectrum annotated with fragments and sub-structures.
#'
#' \item return type : AnnotatedMsMsData
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetStructureAnnotatedSpectrum } \emph{ Returns a fragmentation spectrum (e.g. Merged MS/MS) which is annotated with fragments and losses for the given formula result identifier  These annotations are only available if a fragmentation tree is available. }
#' Returns a fragmentation spectrum (e.g. Merged MS/MS) which is annotated with fragments and losses for the given formula result identifier  These annotations are only available if a fragmentation tree is available.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } formula_id character
#' \item \emph{ @param } inchi_key character
#' \item \emph{ @param } spectrum_index integer
#' \item \emph{ @returnType } \link{AnnotatedSpectrum} \cr
#'
#'
#' \item status code : 200 | Fragmentation spectrum annotated with fragments and sub-structures.
#'
#' \item return type : AnnotatedSpectrum
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetStructureCandidates } \emph{ List of structure database search candidates ranked by CSI:FingerID score for the given &#39;alignedFeatureId&#39; with minimal information. }
#' List of structure database search candidates ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint, structure database links.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } opt_fields list( \link{StructureCandidateOptField} )
#' \item \emph{ @returnType } list( \link{StructureCandidateFormula} ) \cr
#'
#'
#' \item status code : 200 | StructureCandidate of this feature (aligned over runs) candidate with specified optional fields.
#'
#' \item return type : array[StructureCandidateFormula]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetStructureCandidatesByFormula } \emph{ List of CSI:FingerID structure database search candidates for the given &#39;formulaId&#39; with minimal information. }
#' List of CSI:FingerID structure database search candidates for the given 'formulaId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint, structure database links.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } formula_id character
#' \item \emph{ @param } opt_fields list( \link{StructureCandidateOptField} )
#' \item \emph{ @returnType } list( \link{StructureCandidateScored} ) \cr
#'
#'
#' \item status code : 200 | StructureCandidate of this formula candidate with specified optional fields.
#'
#' \item return type : array[StructureCandidateScored]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetStructureCandidatesByFormulaPaged } \emph{ Page of CSI:FingerID structure database search candidates for the given &#39;formulaId&#39; with minimal information. }
#' Page of CSI:FingerID structure database search candidates for the given 'formulaId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint, structure database links.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } formula_id character
#' \item \emph{ @param } page integer
#' \item \emph{ @param } size integer
#' \item \emph{ @param } sort list( character )
#' \item \emph{ @param } opt_fields list( \link{StructureCandidateOptField} )
#' \item \emph{ @returnType } \link{PageStructureCandidateScored} \cr
#'
#'
#' \item status code : 200 | StructureCandidate of this formula candidate with specified optional fields.
#'
#' \item return type : PageStructureCandidateScored
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetStructureCandidatesPaged } \emph{ Page of structure database search candidates ranked by CSI:FingerID score for the given &#39;alignedFeatureId&#39; with minimal information. }
#' Page of structure database search candidates ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint, structure database links.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @param } page integer
#' \item \emph{ @param } size integer
#' \item \emph{ @param } sort list( character )
#' \item \emph{ @param } opt_fields list( \link{StructureCandidateOptField} )
#' \item \emph{ @returnType } \link{PageStructureCandidateFormula} \cr
#'
#'
#' \item status code : 200 | StructureCandidate of this feature (aligned over runs) candidate with specified optional fields.
#'
#' \item return type : PageStructureCandidateFormula
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetTraces1 } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } aligned_feature_id character
#' \item \emph{ @returnType } \link{TraceSet} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : TraceSet
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  AddAlignedFeatures  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to import into.
#' var_feature_import <- c(FeatureImport$new(123, 123, "name_example", "externalFeatureId_example", c("detectedAdducts_example"), 123, 123, BasicSpectrum$new(c(SimplePeak$new(123, 123)), "name_example", 123, "collisionEnergy_example", 123, 123, 123), c(BasicSpectrum$new(c(SimplePeak$new(123, 123)), "name_example", 123, "collisionEnergy_example", 123, 123, 123)), c(BasicSpectrum$new(c(SimplePeak$new(123, 123)), "name_example", 123, "collisionEnergy_example", 123, 123, 123)))) # array[FeatureImport] | the feature data to be imported
#' var_profile <- InstrumentProfile$new() # InstrumentProfile | profile describing the instrument used to measure the data. Used to merge spectra. (Optional)
#' var_opt_fields <- c(AlignedFeatureOptField$new()) # array[AlignedFeatureOptField] | set of optional fields to be included. Use 'none' to override defaults. (Optional)
#'
#' #Import (aligned) features into the project.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AddAlignedFeatures(var_project_id, var_feature_import, profile = var_profile, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$AddAlignedFeatures(var_project_id, var_feature_import, profile = var_profile, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  DeleteAlignedFeature  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to delete from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | identifier of feature (aligned over runs) to delete.
#'
#' #Delete feature (aligned over runs) with the given identifier from the specified project-space.
#' api_instance <- rsirius_api$new()
#'
#' api_instance$features_api$DeleteAlignedFeature(var_project_id, var_aligned_feature_id)
#'
#'
#' ####################  DeleteAlignedFeatures  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to delete from.
#' var_request_body <- c("property_example") # array[character] | 
#'
#' #Delete feature (aligned over runs) with the given identifier from the specified project-space.
#' api_instance <- rsirius_api$new()
#'
#' api_instance$features_api$DeleteAlignedFeatures(var_project_id, var_request_body)
#'
#'
#' ####################  GetAlignedFeature  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | identifier of feature (aligned over runs) to access.
#' var_opt_fields <- c(AlignedFeatureOptField$new()) # array[AlignedFeatureOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#'
#' #Get feature (aligned over runs) with the given identifier from the specified project-space.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAlignedFeature(var_project_id, var_aligned_feature_id, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$GetAlignedFeature(var_project_id, var_aligned_feature_id, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetAlignedFeatures  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_opt_fields <- c(AlignedFeatureOptField$new()) # array[AlignedFeatureOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#'
#' #Get all available features (aligned over runs) in the given project-space.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAlignedFeatures(var_project_id, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$GetAlignedFeatures(var_project_id, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetAlignedFeaturesPaged  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_page <- 0 # integer | Zero-based page index (0..N) (Optional)
#' var_size <- 20 # integer | The size of the page to be returned (Optional)
#' var_sort <- c("inner_example") # array[character] | Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported. (Optional)
#' var_opt_fields <- c(AlignedFeatureOptField$new()) # array[AlignedFeatureOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#'
#' #Get all available features (aligned over runs) in the given project-space.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAlignedFeaturesPaged(var_project_id, page = var_page, size = var_size, sort = var_sort, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$GetAlignedFeaturesPaged(var_project_id, page = var_page, size = var_size, sort = var_sort, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetAlignedFeaturesQuality  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | identifier of feature (aligned over runs) to access.
#'
#' #Get data quality information for feature (aligned over runs) with the given identifier from the specified project-space.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAlignedFeaturesQuality(var_project_id, var_aligned_feature_iddata_file = "result.txt")
#' result <- api_instance$features_api$GetAlignedFeaturesQuality(var_project_id, var_aligned_feature_id)
#' dput(result)
#'
#'
#' ####################  GetBestMatchingCompoundClasses  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_formula_id <- "formula_id_example" # character | identifier of the requested formula result
#'
#' #Best matching compound classes,  Set of the highest scoring compound classes (CANOPUS) on each hierarchy level of  the ClassyFire and NPC ontology,
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetBestMatchingCompoundClasses(var_project_id, var_aligned_feature_id, var_formula_iddata_file = "result.txt")
#' result <- api_instance$features_api$GetBestMatchingCompoundClasses(var_project_id, var_aligned_feature_id, var_formula_id)
#' dput(result)
#'
#'
#' ####################  GetCanopusPrediction  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_formula_id <- "formula_id_example" # character | identifier of the requested formula result
#'
#' #All predicted compound classes (CANOPUS) from ClassyFire and NPC and their probabilities,
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCanopusPrediction(var_project_id, var_aligned_feature_id, var_formula_iddata_file = "result.txt")
#' result <- api_instance$features_api$GetCanopusPrediction(var_project_id, var_aligned_feature_id, var_formula_id)
#' dput(result)
#'
#'
#' ####################  GetDeNovoStructureCandidates  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the structure candidates belong to.
#' var_opt_fields <- c(StructureCandidateOptField$new()) # array[StructureCandidateOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#'
#' #List of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetDeNovoStructureCandidates(var_project_id, var_aligned_feature_id, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$GetDeNovoStructureCandidates(var_project_id, var_aligned_feature_id, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetDeNovoStructureCandidatesByFormula  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_formula_id <- "formula_id_example" # character | identifier of the requested formula result
#' var_opt_fields <- c(StructureCandidateOptField$new()) # array[StructureCandidateOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#'
#' #List of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'formulaId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetDeNovoStructureCandidatesByFormula(var_project_id, var_aligned_feature_id, var_formula_id, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$GetDeNovoStructureCandidatesByFormula(var_project_id, var_aligned_feature_id, var_formula_id, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetDeNovoStructureCandidatesByFormulaPaged  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_formula_id <- "formula_id_example" # character | identifier of the requested formula result
#' var_page <- 0 # integer | Zero-based page index (0..N) (Optional)
#' var_size <- 20 # integer | The size of the page to be returned (Optional)
#' var_sort <- c("inner_example") # array[character] | Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported. (Optional)
#' var_opt_fields <- c(StructureCandidateOptField$new()) # array[StructureCandidateOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#'
#' #Page of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'formulaId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetDeNovoStructureCandidatesByFormulaPaged(var_project_id, var_aligned_feature_id, var_formula_id, page = var_page, size = var_size, sort = var_sort, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$GetDeNovoStructureCandidatesByFormulaPaged(var_project_id, var_aligned_feature_id, var_formula_id, page = var_page, size = var_size, sort = var_sort, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetDeNovoStructureCandidatesPaged  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the structure candidates belong to.
#' var_page <- 0 # integer | Zero-based page index (0..N) (Optional)
#' var_size <- 20 # integer | The size of the page to be returned (Optional)
#' var_sort <- c("inner_example") # array[character] | Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported. (Optional)
#' var_opt_fields <- c(StructureCandidateOptField$new()) # array[StructureCandidateOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#'
#' #Page of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetDeNovoStructureCandidatesPaged(var_project_id, var_aligned_feature_id, page = var_page, size = var_size, sort = var_sort, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$GetDeNovoStructureCandidatesPaged(var_project_id, var_aligned_feature_id, page = var_page, size = var_size, sort = var_sort, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetFingerprintPrediction  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_formula_id <- "formula_id_example" # character | identifier of the requested formula result
#'
#' #Returns predicted fingerprint (CSI:FingerID) for the given formula result identifier  This fingerprint is used to perform structure database search and predict compound classes.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetFingerprintPrediction(var_project_id, var_aligned_feature_id, var_formula_iddata_file = "result.txt")
#' result <- api_instance$features_api$GetFingerprintPrediction(var_project_id, var_aligned_feature_id, var_formula_id)
#' dput(result)
#'
#'
#' ####################  GetFormulaAnnotatedMsMsData  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_formula_id <- "formula_id_example" # character | identifier of the requested formula result
#'
#' #Returns MS/MS Spectrum (Merged MS/MS and measured MS/MS) which is annotated with fragments and losses  for the given formula result identifier  These annotations are only available if a fragmentation tree and the structure candidate are available.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetFormulaAnnotatedMsMsData(var_project_id, var_aligned_feature_id, var_formula_iddata_file = "result.txt")
#' result <- api_instance$features_api$GetFormulaAnnotatedMsMsData(var_project_id, var_aligned_feature_id, var_formula_id)
#' dput(result)
#'
#'
#' ####################  GetFormulaAnnotatedSpectrum  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_formula_id <- "formula_id_example" # character | identifier of the requested formula result
#' var_spectrum_index <- -1 # integer | index of the spectrum to be annotated. Merged MS/MS will be used if spectrumIndex < 0 (default) (Optional)
#'
#' #Returns a fragmentation spectrum (e.g. Merged MS/MS) which is annotated with fragments and losses for the given formula result identifier  These annotations are only available if a fragmentation tree is available.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetFormulaAnnotatedSpectrum(var_project_id, var_aligned_feature_id, var_formula_id, spectrum_index = var_spectrum_indexdata_file = "result.txt")
#' result <- api_instance$features_api$GetFormulaAnnotatedSpectrum(var_project_id, var_aligned_feature_id, var_formula_id, spectrum_index = var_spectrum_index)
#' dput(result)
#'
#'
#' ####################  GetFormulaCandidate  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_formula_id <- "formula_id_example" # character | identifier of the requested formula result
#' var_opt_fields <- c(FormulaCandidateOptField$new()) # array[FormulaCandidateOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#'
#' #FormulaResultContainers for the given 'formulaId' with minimal information.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetFormulaCandidate(var_project_id, var_aligned_feature_id, var_formula_id, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$GetFormulaCandidate(var_project_id, var_aligned_feature_id, var_formula_id, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetFormulaCandidates  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_opt_fields <- c(FormulaCandidateOptField$new()) # array[FormulaCandidateOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#'
#' #List of FormulaResultContainers available for this feature with minimal information.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetFormulaCandidates(var_project_id, var_aligned_feature_id, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$GetFormulaCandidates(var_project_id, var_aligned_feature_id, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetFormulaCandidatesPaged  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_page <- 0 # integer | Zero-based page index (0..N) (Optional)
#' var_size <- 20 # integer | The size of the page to be returned (Optional)
#' var_sort <- c("inner_example") # array[character] | Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported. (Optional)
#' var_opt_fields <- c(FormulaCandidateOptField$new()) # array[FormulaCandidateOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#'
#' #Page of FormulaResultContainers available for this feature with minimal information.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetFormulaCandidatesPaged(var_project_id, var_aligned_feature_id, page = var_page, size = var_size, sort = var_sort, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$GetFormulaCandidatesPaged(var_project_id, var_aligned_feature_id, page = var_page, size = var_size, sort = var_sort, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetFragTree  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_formula_id <- "formula_id_example" # character | identifier of the requested formula result
#'
#' #Returns fragmentation tree (SIRIUS) for the given formula result identifier  This tree is used to rank formula candidates (treeScore).
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetFragTree(var_project_id, var_aligned_feature_id, var_formula_iddata_file = "result.txt")
#' result <- api_instance$features_api$GetFragTree(var_project_id, var_aligned_feature_id, var_formula_id)
#' dput(result)
#'
#'
#' ####################  GetIsotopePatternAnnotation  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_formula_id <- "formula_id_example" # character | identifier of the requested formula result
#'
#' #Returns Isotope pattern information (simulated isotope pattern, measured isotope pattern, isotope pattern highlighting)  for the given formula result identifier.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetIsotopePatternAnnotation(var_project_id, var_aligned_feature_id, var_formula_iddata_file = "result.txt")
#' result <- api_instance$features_api$GetIsotopePatternAnnotation(var_project_id, var_aligned_feature_id, var_formula_id)
#' dput(result)
#'
#'
#' ####################  GetLipidAnnotation  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_formula_id <- "formula_id_example" # character | identifier of the requested formula result
#'
#' #Returns Lipid annotation (ElGordo) for the given formula result identifier.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetLipidAnnotation(var_project_id, var_aligned_feature_id, var_formula_iddata_file = "result.txt")
#' result <- api_instance$features_api$GetLipidAnnotation(var_project_id, var_aligned_feature_id, var_formula_id)
#' dput(result)
#'
#'
#' ####################  GetMsData  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the Mass Spec data belong sto.
#'
#' #Mass Spec data (input data) for the given 'alignedFeatureId' .
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetMsData(var_project_id, var_aligned_feature_iddata_file = "result.txt")
#' result <- api_instance$features_api$GetMsData(var_project_id, var_aligned_feature_id)
#' dput(result)
#'
#'
#' ####################  GetQuantification  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | 
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | 
#' var_type <- "APEX_HEIGHT" # character |  (Optional)
#'
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetQuantification(var_project_id, var_aligned_feature_id, type = var_typedata_file = "result.txt")
#' result <- api_instance$features_api$GetQuantification(var_project_id, var_aligned_feature_id, type = var_type)
#' dput(result)
#'
#'
#' ####################  GetSiriusFragTree  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_formula_id <- "formula_id_example" # character | identifier of the requested formula result
#'
#' #Returns fragmentation tree (SIRIUS) for the given formula result identifier in SIRIUS' internal format.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetSiriusFragTree(var_project_id, var_aligned_feature_id, var_formula_iddata_file = "result.txt")
#' result <- api_instance$features_api$GetSiriusFragTree(var_project_id, var_aligned_feature_id, var_formula_id)
#' dput(result)
#'
#'
#' ####################  GetSpectralLibraryMatch  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the structure candidates belong to.
#' var_match_id <- "match_id_example" # character | 
#' var_opt_fields <- c(SpectralLibraryMatchOptField$new()) # array[SpectralLibraryMatchOptField] |  (Optional)
#'
#' #List of spectral library matches for the given 'alignedFeatureId'.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetSpectralLibraryMatch(var_project_id, var_aligned_feature_id, var_match_id, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$GetSpectralLibraryMatch(var_project_id, var_aligned_feature_id, var_match_id, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetSpectralLibraryMatches  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the structure candidates belong to.
#' var_min_shared_peaks <- 1 # integer |  (Optional)
#' var_min_similarity <- 0.2 # numeric |  (Optional)
#' var_candidate_in_chi_key <- "" # character |  (Optional)
#' var_opt_fields <- c(SpectralLibraryMatchOptField$new()) # array[SpectralLibraryMatchOptField] |  (Optional)
#'
#' #List of spectral library matches for the given 'alignedFeatureId'.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetSpectralLibraryMatches(var_project_id, var_aligned_feature_id, min_shared_peaks = var_min_shared_peaks, min_similarity = var_min_similarity, candidate_in_chi_key = var_candidate_in_chi_key, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$GetSpectralLibraryMatches(var_project_id, var_aligned_feature_id, min_shared_peaks = var_min_shared_peaks, min_similarity = var_min_similarity, candidate_in_chi_key = var_candidate_in_chi_key, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetSpectralLibraryMatchesPaged  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the structure candidates belong to.
#' var_page <- 0 # integer | Zero-based page index (0..N) (Optional)
#' var_size <- 20 # integer | The size of the page to be returned (Optional)
#' var_sort <- c("inner_example") # array[character] | Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported. (Optional)
#' var_min_shared_peaks <- 1 # integer |  (Optional)
#' var_min_similarity <- 0.2 # numeric |  (Optional)
#' var_candidate_in_chi_key <- "" # character |  (Optional)
#' var_opt_fields <- c(SpectralLibraryMatchOptField$new()) # array[SpectralLibraryMatchOptField] |  (Optional)
#'
#' #Page of spectral library matches for the given 'alignedFeatureId'.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetSpectralLibraryMatchesPaged(var_project_id, var_aligned_feature_id, page = var_page, size = var_size, sort = var_sort, min_shared_peaks = var_min_shared_peaks, min_similarity = var_min_similarity, candidate_in_chi_key = var_candidate_in_chi_key, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$GetSpectralLibraryMatchesPaged(var_project_id, var_aligned_feature_id, page = var_page, size = var_size, sort = var_sort, min_shared_peaks = var_min_shared_peaks, min_similarity = var_min_similarity, candidate_in_chi_key = var_candidate_in_chi_key, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetSpectralLibraryMatchesSummary  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the structure candidates belong to.
#' var_min_shared_peaks <- 1 # integer | min threshold of shared peaks. (Optional)
#' var_min_similarity <- 0.2 # numeric | min spectral similarity threshold. (Optional)
#' var_candidate_in_chi_key <- "" # character | inchi key of the database compound. (Optional)
#'
#' #Summarize matched reference spectra for the given 'alignedFeatureId'.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetSpectralLibraryMatchesSummary(var_project_id, var_aligned_feature_id, min_shared_peaks = var_min_shared_peaks, min_similarity = var_min_similarity, candidate_in_chi_key = var_candidate_in_chi_keydata_file = "result.txt")
#' result <- api_instance$features_api$GetSpectralLibraryMatchesSummary(var_project_id, var_aligned_feature_id, min_shared_peaks = var_min_shared_peaks, min_similarity = var_min_similarity, candidate_in_chi_key = var_candidate_in_chi_key)
#' dput(result)
#'
#'
#' ####################  GetStructureAnnotatedMsData  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_formula_id <- "formula_id_example" # character | identifier of the requested formula result
#' var_inchi_key <- "inchi_key_example" # character | 2d InChIKey of the structure candidate to be used to annotate the spectrum annotation
#'
#' #Returns MS/MS Data (Merged MS/MS and list of measured MS/MS ) which are annotated with fragments and losses  for the given formula result identifier and structure candidate inChIKey.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetStructureAnnotatedMsData(var_project_id, var_aligned_feature_id, var_formula_id, var_inchi_keydata_file = "result.txt")
#' result <- api_instance$features_api$GetStructureAnnotatedMsData(var_project_id, var_aligned_feature_id, var_formula_id, var_inchi_key)
#' dput(result)
#'
#'
#' ####################  GetStructureAnnotatedSpectrum  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_formula_id <- "formula_id_example" # character | identifier of the requested formula result
#' var_inchi_key <- "inchi_key_example" # character | 2d InChIKey of the structure candidate to be used to annotate the spectrum annotation
#' var_spectrum_index <- -1 # integer | index of the spectrum to be annotated. Merged MS/MS will be used if spectrumIndex < 0 (default) (Optional)
#'
#' #Returns a fragmentation spectrum (e.g. Merged MS/MS) which is annotated with fragments and losses for the given formula result identifier  These annotations are only available if a fragmentation tree is available.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetStructureAnnotatedSpectrum(var_project_id, var_aligned_feature_id, var_formula_id, var_inchi_key, spectrum_index = var_spectrum_indexdata_file = "result.txt")
#' result <- api_instance$features_api$GetStructureAnnotatedSpectrum(var_project_id, var_aligned_feature_id, var_formula_id, var_inchi_key, spectrum_index = var_spectrum_index)
#' dput(result)
#'
#'
#' ####################  GetStructureCandidates  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the structure candidates belong to.
#' var_opt_fields <- c(StructureCandidateOptField$new()) # array[StructureCandidateOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#'
#' #List of structure database search candidates ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetStructureCandidates(var_project_id, var_aligned_feature_id, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$GetStructureCandidates(var_project_id, var_aligned_feature_id, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetStructureCandidatesByFormula  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_formula_id <- "formula_id_example" # character | identifier of the requested formula result
#' var_opt_fields <- c(StructureCandidateOptField$new()) # array[StructureCandidateOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#'
#' #List of CSI:FingerID structure database search candidates for the given 'formulaId' with minimal information.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetStructureCandidatesByFormula(var_project_id, var_aligned_feature_id, var_formula_id, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$GetStructureCandidatesByFormula(var_project_id, var_aligned_feature_id, var_formula_id, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetStructureCandidatesByFormulaPaged  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the formula result belongs to.
#' var_formula_id <- "formula_id_example" # character | identifier of the requested formula result
#' var_page <- 0 # integer | Zero-based page index (0..N) (Optional)
#' var_size <- 20 # integer | The size of the page to be returned (Optional)
#' var_sort <- c("inner_example") # array[character] | Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported. (Optional)
#' var_opt_fields <- c(StructureCandidateOptField$new()) # array[StructureCandidateOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#'
#' #Page of CSI:FingerID structure database search candidates for the given 'formulaId' with minimal information.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetStructureCandidatesByFormulaPaged(var_project_id, var_aligned_feature_id, var_formula_id, page = var_page, size = var_size, sort = var_sort, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$GetStructureCandidatesByFormulaPaged(var_project_id, var_aligned_feature_id, var_formula_id, page = var_page, size = var_size, sort = var_sort, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetStructureCandidatesPaged  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | feature (aligned over runs) the structure candidates belong to.
#' var_page <- 0 # integer | Zero-based page index (0..N) (Optional)
#' var_size <- 20 # integer | The size of the page to be returned (Optional)
#' var_sort <- c("inner_example") # array[character] | Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported. (Optional)
#' var_opt_fields <- c(StructureCandidateOptField$new()) # array[StructureCandidateOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#'
#' #Page of structure database search candidates ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetStructureCandidatesPaged(var_project_id, var_aligned_feature_id, page = var_page, size = var_size, sort = var_sort, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$features_api$GetStructureCandidatesPaged(var_project_id, var_aligned_feature_id, page = var_page, size = var_size, sort = var_sort, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetTraces1  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | 
#' var_aligned_feature_id <- "aligned_feature_id_example" # character | 
#'
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetTraces1(var_project_id, var_aligned_feature_iddata_file = "result.txt")
#' result <- api_instance$features_api$GetTraces1(var_project_id, var_aligned_feature_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
FeaturesApi <- R6::R6Class(
  "FeaturesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new FeaturesApi.
    #'
    #' @description
    #' Initialize a new FeaturesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Import (aligned) features into the project.
    #'
    #' @description
    #' Import (aligned) features into the project.
    #'
    #' @param project_id project-space to import into.
    #' @param feature_import the feature data to be imported
    #' @param profile (optional) profile describing the instrument used to measure the data. Used to merge spectra.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[AlignedFeature]
    #' @export
    AddAlignedFeatures = function(project_id, feature_import, profile = NULL, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$AddAlignedFeaturesWithHttpInfo(project_id, feature_import, profile, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Import (aligned) features into the project.
    #'
    #' @description
    #' Import (aligned) features into the project.
    #'
    #' @param project_id project-space to import into.
    #' @param feature_import the feature data to be imported
    #' @param profile (optional) profile describing the instrument used to measure the data. Used to merge spectra.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[AlignedFeature]) with additional information such as HTTP status code, headers
    #' @export
    AddAlignedFeaturesWithHttpInfo = function(project_id, feature_import, profile = NULL, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`feature_import`)) {
        stop("Missing required parameter `feature_import`.")
      }





      query_params[["profile"]] <- `profile`

      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      if (!is.null(`feature_import`)) {
        body.items <- paste(unlist(lapply(`feature_import`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "array[AlignedFeature]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Delete feature (aligned over runs) with the given identifier from the specified project-space.
    #'
    #' @description
    #' Delete feature (aligned over runs) with the given identifier from the specified project-space.
    #'
    #' @param project_id project-space to delete from.
    #' @param aligned_feature_id identifier of feature (aligned over runs) to delete.
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    DeleteAlignedFeature = function(project_id, aligned_feature_id, ...) {
      local_var_response <- self$DeleteAlignedFeatureWithHttpInfo(project_id, aligned_feature_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Delete feature (aligned over runs) with the given identifier from the specified project-space.
    #'
    #' @description
    #' Delete feature (aligned over runs) with the given identifier from the specified project-space.
    #'
    #' @param project_id project-space to delete from.
    #' @param aligned_feature_id identifier of feature (aligned over runs) to delete.
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    DeleteAlignedFeatureWithHttpInfo = function(project_id, aligned_feature_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }



      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Delete feature (aligned over runs) with the given identifier from the specified project-space.
    #'
    #' @description
    #' Delete feature (aligned over runs) with the given identifier from the specified project-space.
    #'
    #' @param project_id project-space to delete from.
    #' @param request_body 
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    DeleteAlignedFeatures = function(project_id, request_body, ...) {
      local_var_response <- self$DeleteAlignedFeaturesWithHttpInfo(project_id, request_body, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Delete feature (aligned over runs) with the given identifier from the specified project-space.
    #'
    #' @description
    #' Delete feature (aligned over runs) with the given identifier from the specified project-space.
    #'
    #' @param project_id project-space to delete from.
    #' @param request_body 
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    DeleteAlignedFeaturesWithHttpInfo = function(project_id, request_body, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`request_body`)) {
        stop("Missing required parameter `request_body`.")
      }



      if (!is.null(`request_body`)) {
        body.items <- paste(unlist(lapply(`request_body`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/delete"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get feature (aligned over runs) with the given identifier from the specified project-space.
    #'
    #' @description
    #' Get feature (aligned over runs) with the given identifier from the specified project-space.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id identifier of feature (aligned over runs) to access.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AlignedFeature
    #' @export
    GetAlignedFeature = function(project_id, aligned_feature_id, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetAlignedFeatureWithHttpInfo(project_id, aligned_feature_id, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get feature (aligned over runs) with the given identifier from the specified project-space.
    #'
    #' @description
    #' Get feature (aligned over runs) with the given identifier from the specified project-space.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id identifier of feature (aligned over runs) to access.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AlignedFeature) with additional information such as HTTP status code, headers
    #' @export
    GetAlignedFeatureWithHttpInfo = function(project_id, aligned_feature_id, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }




      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AlignedFeature", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get all available features (aligned over runs) in the given project-space.
    #'
    #' @description
    #' Get all available features (aligned over runs) in the given project-space.
    #'
    #' @param project_id project-space to read from.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[AlignedFeature]
    #' @export
    GetAlignedFeatures = function(project_id, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetAlignedFeaturesWithHttpInfo(project_id, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get all available features (aligned over runs) in the given project-space.
    #'
    #' @description
    #' Get all available features (aligned over runs) in the given project-space.
    #'
    #' @param project_id project-space to read from.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[AlignedFeature]) with additional information such as HTTP status code, headers
    #' @export
    GetAlignedFeaturesWithHttpInfo = function(project_id, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }



      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "array[AlignedFeature]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get all available features (aligned over runs) in the given project-space.
    #'
    #' @description
    #' Get all available features (aligned over runs) in the given project-space.
    #'
    #' @param project_id project-space to read from.
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PageAlignedFeature
    #' @export
    GetAlignedFeaturesPaged = function(project_id, page = 0, size = 20, sort = NULL, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetAlignedFeaturesPagedWithHttpInfo(project_id, page, size, sort, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get all available features (aligned over runs) in the given project-space.
    #'
    #' @description
    #' Get all available features (aligned over runs) in the given project-space.
    #'
    #' @param project_id project-space to read from.
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PageAlignedFeature) with additional information such as HTTP status code, headers
    #' @export
    GetAlignedFeaturesPagedWithHttpInfo = function(project_id, page = 0, size = 20, sort = NULL, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }


      if (`page` < 0) {
        stop("Invalid value for `page` when calling FeaturesApi$GetAlignedFeaturesPaged, must be bigger than or equal to 0.")
      }

      if (`size` < 1) {
        stop("Invalid value for `size` when calling FeaturesApi$GetAlignedFeaturesPaged, must be bigger than or equal to 1.")
      }



      query_params[["page"]] <- `page`

      query_params[["size"]] <- `size`

      # explore
      for (query_item in `sort`) {
        query_params[["sort"]] <- c(query_params[["sort"]], list(`sort` = query_item))
      }

      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/page"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PageAlignedFeature", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get data quality information for feature (aligned over runs) with the given identifier from the specified project-space.
    #'
    #' @description
    #' Get data quality information for feature (aligned over runs) with the given identifier from the specified project-space.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id identifier of feature (aligned over runs) to access.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AlignedFeatureQuality
    #' @export
    GetAlignedFeaturesQuality = function(project_id, aligned_feature_id, data_file = NULL, ...) {
      local_var_response <- self$GetAlignedFeaturesQualityWithHttpInfo(project_id, aligned_feature_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get data quality information for feature (aligned over runs) with the given identifier from the specified project-space.
    #'
    #' @description
    #' Get data quality information for feature (aligned over runs) with the given identifier from the specified project-space.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id identifier of feature (aligned over runs) to access.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AlignedFeatureQuality) with additional information such as HTTP status code, headers
    #' @export
    GetAlignedFeaturesQualityWithHttpInfo = function(project_id, aligned_feature_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }



      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/quality-report"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AlignedFeatureQuality", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Best matching compound classes,  Set of the highest scoring compound classes (CANOPUS) on each hierarchy level of  the ClassyFire and NPC ontology,
    #'
    #' @description
    #' Best matching compound classes,  Set of the highest scoring compound classes (CANOPUS) on each hierarchy level of  the ClassyFire and NPC ontology,
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return CompoundClasses
    #' @export
    GetBestMatchingCompoundClasses = function(project_id, aligned_feature_id, formula_id, data_file = NULL, ...) {
      local_var_response <- self$GetBestMatchingCompoundClassesWithHttpInfo(project_id, aligned_feature_id, formula_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Best matching compound classes,  Set of the highest scoring compound classes (CANOPUS) on each hierarchy level of  the ClassyFire and NPC ontology,
    #'
    #' @description
    #' Best matching compound classes,  Set of the highest scoring compound classes (CANOPUS) on each hierarchy level of  the ClassyFire and NPC ontology,
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (CompoundClasses) with additional information such as HTTP status code, headers
    #' @export
    GetBestMatchingCompoundClassesWithHttpInfo = function(project_id, aligned_feature_id, formula_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }

      if (missing(`formula_id`)) {
        stop("Missing required parameter `formula_id`.")
      }




      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas/{formulaId}/best-compound-classes"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`formula_id`)) {
        local_var_url_path <- gsub("\\{formulaId\\}", URLencode(as.character(`formula_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "CompoundClasses", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' All predicted compound classes (CANOPUS) from ClassyFire and NPC and their probabilities,
    #'
    #' @description
    #' All predicted compound classes (CANOPUS) from ClassyFire and NPC and their probabilities,
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return CanopusPrediction
    #' @export
    GetCanopusPrediction = function(project_id, aligned_feature_id, formula_id, data_file = NULL, ...) {
      local_var_response <- self$GetCanopusPredictionWithHttpInfo(project_id, aligned_feature_id, formula_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' All predicted compound classes (CANOPUS) from ClassyFire and NPC and their probabilities,
    #'
    #' @description
    #' All predicted compound classes (CANOPUS) from ClassyFire and NPC and their probabilities,
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (CanopusPrediction) with additional information such as HTTP status code, headers
    #' @export
    GetCanopusPredictionWithHttpInfo = function(project_id, aligned_feature_id, formula_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }

      if (missing(`formula_id`)) {
        stop("Missing required parameter `formula_id`.")
      }




      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas/{formulaId}/canopus-prediction"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`formula_id`)) {
        local_var_url_path <- gsub("\\{formulaId\\}", URLencode(as.character(`formula_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "CanopusPrediction", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' List of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
    #'
    #' @description
    #' List of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the structure candidates belong to.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[StructureCandidateFormula]
    #' @export
    GetDeNovoStructureCandidates = function(project_id, aligned_feature_id, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetDeNovoStructureCandidatesWithHttpInfo(project_id, aligned_feature_id, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
    #'
    #' @description
    #' List of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the structure candidates belong to.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[StructureCandidateFormula]) with additional information such as HTTP status code, headers
    #' @export
    GetDeNovoStructureCandidatesWithHttpInfo = function(project_id, aligned_feature_id, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }




      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/denovo-structures"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "array[StructureCandidateFormula]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' List of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'formulaId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
    #'
    #' @description
    #' List of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'formulaId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[StructureCandidateScored]
    #' @export
    GetDeNovoStructureCandidatesByFormula = function(project_id, aligned_feature_id, formula_id, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetDeNovoStructureCandidatesByFormulaWithHttpInfo(project_id, aligned_feature_id, formula_id, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'formulaId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
    #'
    #' @description
    #' List of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'formulaId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[StructureCandidateScored]) with additional information such as HTTP status code, headers
    #' @export
    GetDeNovoStructureCandidatesByFormulaWithHttpInfo = function(project_id, aligned_feature_id, formula_id, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }

      if (missing(`formula_id`)) {
        stop("Missing required parameter `formula_id`.")
      }





      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas/{formulaId}/denovo-structures"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`formula_id`)) {
        local_var_url_path <- gsub("\\{formulaId\\}", URLencode(as.character(`formula_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "array[StructureCandidateScored]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Page of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'formulaId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
    #'
    #' @description
    #' Page of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'formulaId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PageStructureCandidateScored
    #' @export
    GetDeNovoStructureCandidatesByFormulaPaged = function(project_id, aligned_feature_id, formula_id, page = 0, size = 20, sort = NULL, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetDeNovoStructureCandidatesByFormulaPagedWithHttpInfo(project_id, aligned_feature_id, formula_id, page, size, sort, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Page of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'formulaId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
    #'
    #' @description
    #' Page of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'formulaId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PageStructureCandidateScored) with additional information such as HTTP status code, headers
    #' @export
    GetDeNovoStructureCandidatesByFormulaPagedWithHttpInfo = function(project_id, aligned_feature_id, formula_id, page = 0, size = 20, sort = NULL, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }

      if (missing(`formula_id`)) {
        stop("Missing required parameter `formula_id`.")
      }




      if (`page` < 0) {
        stop("Invalid value for `page` when calling FeaturesApi$GetDeNovoStructureCandidatesByFormulaPaged, must be bigger than or equal to 0.")
      }

      if (`size` < 1) {
        stop("Invalid value for `size` when calling FeaturesApi$GetDeNovoStructureCandidatesByFormulaPaged, must be bigger than or equal to 1.")
      }



      query_params[["page"]] <- `page`

      query_params[["size"]] <- `size`

      # explore
      for (query_item in `sort`) {
        query_params[["sort"]] <- c(query_params[["sort"]], list(`sort` = query_item))
      }

      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas/{formulaId}/denovo-structures/page"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`formula_id`)) {
        local_var_url_path <- gsub("\\{formulaId\\}", URLencode(as.character(`formula_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PageStructureCandidateScored", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Page of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
    #'
    #' @description
    #' Page of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the structure candidates belong to.
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PageStructureCandidateFormula
    #' @export
    GetDeNovoStructureCandidatesPaged = function(project_id, aligned_feature_id, page = 0, size = 20, sort = NULL, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetDeNovoStructureCandidatesPagedWithHttpInfo(project_id, aligned_feature_id, page, size, sort, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Page of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
    #'
    #' @description
    #' Page of de novo structure candidates (e.g. generated by MsNovelist) ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.  StructureCandidates can be enriched with molecular fingerprint.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the structure candidates belong to.
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PageStructureCandidateFormula) with additional information such as HTTP status code, headers
    #' @export
    GetDeNovoStructureCandidatesPagedWithHttpInfo = function(project_id, aligned_feature_id, page = 0, size = 20, sort = NULL, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }



      if (`page` < 0) {
        stop("Invalid value for `page` when calling FeaturesApi$GetDeNovoStructureCandidatesPaged, must be bigger than or equal to 0.")
      }

      if (`size` < 1) {
        stop("Invalid value for `size` when calling FeaturesApi$GetDeNovoStructureCandidatesPaged, must be bigger than or equal to 1.")
      }



      query_params[["page"]] <- `page`

      query_params[["size"]] <- `size`

      # explore
      for (query_item in `sort`) {
        query_params[["sort"]] <- c(query_params[["sort"]], list(`sort` = query_item))
      }

      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/denovo-structures/page"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PageStructureCandidateFormula", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns predicted fingerprint (CSI:FingerID) for the given formula result identifier  This fingerprint is used to perform structure database search and predict compound classes.
    #'
    #' @description
    #' Returns predicted fingerprint (CSI:FingerID) for the given formula result identifier  This fingerprint is used to perform structure database search and predict compound classes.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[numeric]
    #' @export
    GetFingerprintPrediction = function(project_id, aligned_feature_id, formula_id, data_file = NULL, ...) {
      local_var_response <- self$GetFingerprintPredictionWithHttpInfo(project_id, aligned_feature_id, formula_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns predicted fingerprint (CSI:FingerID) for the given formula result identifier  This fingerprint is used to perform structure database search and predict compound classes.
    #'
    #' @description
    #' Returns predicted fingerprint (CSI:FingerID) for the given formula result identifier  This fingerprint is used to perform structure database search and predict compound classes.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[numeric]) with additional information such as HTTP status code, headers
    #' @export
    GetFingerprintPredictionWithHttpInfo = function(project_id, aligned_feature_id, formula_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }

      if (missing(`formula_id`)) {
        stop("Missing required parameter `formula_id`.")
      }




      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas/{formulaId}/fingerprint"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`formula_id`)) {
        local_var_url_path <- gsub("\\{formulaId\\}", URLencode(as.character(`formula_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "array[numeric]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns MS/MS Spectrum (Merged MS/MS and measured MS/MS) which is annotated with fragments and losses  for the given formula result identifier  These annotations are only available if a fragmentation tree and the structure candidate are available.
    #'
    #' @description
    #' Returns MS/MS Spectrum (Merged MS/MS and measured MS/MS) which is annotated with fragments and losses  for the given formula result identifier  These annotations are only available if a fragmentation tree and the structure candidate are available.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnnotatedMsMsData
    #' @export
    GetFormulaAnnotatedMsMsData = function(project_id, aligned_feature_id, formula_id, data_file = NULL, ...) {
      local_var_response <- self$GetFormulaAnnotatedMsMsDataWithHttpInfo(project_id, aligned_feature_id, formula_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns MS/MS Spectrum (Merged MS/MS and measured MS/MS) which is annotated with fragments and losses  for the given formula result identifier  These annotations are only available if a fragmentation tree and the structure candidate are available.
    #'
    #' @description
    #' Returns MS/MS Spectrum (Merged MS/MS and measured MS/MS) which is annotated with fragments and losses  for the given formula result identifier  These annotations are only available if a fragmentation tree and the structure candidate are available.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnnotatedMsMsData) with additional information such as HTTP status code, headers
    #' @export
    GetFormulaAnnotatedMsMsDataWithHttpInfo = function(project_id, aligned_feature_id, formula_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }

      if (missing(`formula_id`)) {
        stop("Missing required parameter `formula_id`.")
      }




      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas/{formulaId}/annotated-msmsdata"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`formula_id`)) {
        local_var_url_path <- gsub("\\{formulaId\\}", URLencode(as.character(`formula_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnnotatedMsMsData", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns a fragmentation spectrum (e.g. Merged MS/MS) which is annotated with fragments and losses for the given formula result identifier  These annotations are only available if a fragmentation tree is available.
    #'
    #' @description
    #' Returns a fragmentation spectrum (e.g. Merged MS/MS) which is annotated with fragments and losses for the given formula result identifier  These annotations are only available if a fragmentation tree is available.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param spectrum_index (optional) index of the spectrum to be annotated. Merged MS/MS will be used if spectrumIndex < 0 (default) (default value: -1)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnnotatedSpectrum
    #' @export
    GetFormulaAnnotatedSpectrum = function(project_id, aligned_feature_id, formula_id, spectrum_index = -1, data_file = NULL, ...) {
      local_var_response <- self$GetFormulaAnnotatedSpectrumWithHttpInfo(project_id, aligned_feature_id, formula_id, spectrum_index, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns a fragmentation spectrum (e.g. Merged MS/MS) which is annotated with fragments and losses for the given formula result identifier  These annotations are only available if a fragmentation tree is available.
    #'
    #' @description
    #' Returns a fragmentation spectrum (e.g. Merged MS/MS) which is annotated with fragments and losses for the given formula result identifier  These annotations are only available if a fragmentation tree is available.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param spectrum_index (optional) index of the spectrum to be annotated. Merged MS/MS will be used if spectrumIndex < 0 (default) (default value: -1)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnnotatedSpectrum) with additional information such as HTTP status code, headers
    #' @export
    GetFormulaAnnotatedSpectrumWithHttpInfo = function(project_id, aligned_feature_id, formula_id, spectrum_index = -1, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }

      if (missing(`formula_id`)) {
        stop("Missing required parameter `formula_id`.")
      }





      query_params[["spectrumIndex"]] <- `spectrum_index`

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas/{formulaId}/annotated-spectrum"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`formula_id`)) {
        local_var_url_path <- gsub("\\{formulaId\\}", URLencode(as.character(`formula_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnnotatedSpectrum", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' FormulaResultContainers for the given 'formulaId' with minimal information.
    #'
    #' @description
    #' FormulaResultContainers for the given 'formulaId' with minimal information.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return FormulaCandidate
    #' @export
    GetFormulaCandidate = function(project_id, aligned_feature_id, formula_id, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetFormulaCandidateWithHttpInfo(project_id, aligned_feature_id, formula_id, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' FormulaResultContainers for the given 'formulaId' with minimal information.
    #'
    #' @description
    #' FormulaResultContainers for the given 'formulaId' with minimal information.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (FormulaCandidate) with additional information such as HTTP status code, headers
    #' @export
    GetFormulaCandidateWithHttpInfo = function(project_id, aligned_feature_id, formula_id, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }

      if (missing(`formula_id`)) {
        stop("Missing required parameter `formula_id`.")
      }





      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas/{formulaId}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`formula_id`)) {
        local_var_url_path <- gsub("\\{formulaId\\}", URLencode(as.character(`formula_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "FormulaCandidate", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' List of FormulaResultContainers available for this feature with minimal information.
    #'
    #' @description
    #' List of FormulaResultContainers available for this feature with minimal information.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[FormulaCandidate]
    #' @export
    GetFormulaCandidates = function(project_id, aligned_feature_id, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetFormulaCandidatesWithHttpInfo(project_id, aligned_feature_id, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List of FormulaResultContainers available for this feature with minimal information.
    #'
    #' @description
    #' List of FormulaResultContainers available for this feature with minimal information.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[FormulaCandidate]) with additional information such as HTTP status code, headers
    #' @export
    GetFormulaCandidatesWithHttpInfo = function(project_id, aligned_feature_id, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }




      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "array[FormulaCandidate]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Page of FormulaResultContainers available for this feature with minimal information.
    #'
    #' @description
    #' Page of FormulaResultContainers available for this feature with minimal information.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PageFormulaCandidate
    #' @export
    GetFormulaCandidatesPaged = function(project_id, aligned_feature_id, page = 0, size = 20, sort = NULL, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetFormulaCandidatesPagedWithHttpInfo(project_id, aligned_feature_id, page, size, sort, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Page of FormulaResultContainers available for this feature with minimal information.
    #'
    #' @description
    #' Page of FormulaResultContainers available for this feature with minimal information.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PageFormulaCandidate) with additional information such as HTTP status code, headers
    #' @export
    GetFormulaCandidatesPagedWithHttpInfo = function(project_id, aligned_feature_id, page = 0, size = 20, sort = NULL, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }



      if (`page` < 0) {
        stop("Invalid value for `page` when calling FeaturesApi$GetFormulaCandidatesPaged, must be bigger than or equal to 0.")
      }

      if (`size` < 1) {
        stop("Invalid value for `size` when calling FeaturesApi$GetFormulaCandidatesPaged, must be bigger than or equal to 1.")
      }



      query_params[["page"]] <- `page`

      query_params[["size"]] <- `size`

      # explore
      for (query_item in `sort`) {
        query_params[["sort"]] <- c(query_params[["sort"]], list(`sort` = query_item))
      }

      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas/page"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PageFormulaCandidate", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns fragmentation tree (SIRIUS) for the given formula result identifier  This tree is used to rank formula candidates (treeScore).
    #'
    #' @description
    #' Returns fragmentation tree (SIRIUS) for the given formula result identifier  This tree is used to rank formula candidates (treeScore).
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return FragmentationTree
    #' @export
    GetFragTree = function(project_id, aligned_feature_id, formula_id, data_file = NULL, ...) {
      local_var_response <- self$GetFragTreeWithHttpInfo(project_id, aligned_feature_id, formula_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns fragmentation tree (SIRIUS) for the given formula result identifier  This tree is used to rank formula candidates (treeScore).
    #'
    #' @description
    #' Returns fragmentation tree (SIRIUS) for the given formula result identifier  This tree is used to rank formula candidates (treeScore).
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (FragmentationTree) with additional information such as HTTP status code, headers
    #' @export
    GetFragTreeWithHttpInfo = function(project_id, aligned_feature_id, formula_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }

      if (missing(`formula_id`)) {
        stop("Missing required parameter `formula_id`.")
      }




      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas/{formulaId}/fragtree"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`formula_id`)) {
        local_var_url_path <- gsub("\\{formulaId\\}", URLencode(as.character(`formula_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "FragmentationTree", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns Isotope pattern information (simulated isotope pattern, measured isotope pattern, isotope pattern highlighting)  for the given formula result identifier.
    #'
    #' @description
    #' Returns Isotope pattern information (simulated isotope pattern, measured isotope pattern, isotope pattern highlighting)  for the given formula result identifier.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return IsotopePatternAnnotation
    #' @export
    GetIsotopePatternAnnotation = function(project_id, aligned_feature_id, formula_id, data_file = NULL, ...) {
      local_var_response <- self$GetIsotopePatternAnnotationWithHttpInfo(project_id, aligned_feature_id, formula_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns Isotope pattern information (simulated isotope pattern, measured isotope pattern, isotope pattern highlighting)  for the given formula result identifier.
    #'
    #' @description
    #' Returns Isotope pattern information (simulated isotope pattern, measured isotope pattern, isotope pattern highlighting)  for the given formula result identifier.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (IsotopePatternAnnotation) with additional information such as HTTP status code, headers
    #' @export
    GetIsotopePatternAnnotationWithHttpInfo = function(project_id, aligned_feature_id, formula_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }

      if (missing(`formula_id`)) {
        stop("Missing required parameter `formula_id`.")
      }




      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas/{formulaId}/isotope-pattern"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`formula_id`)) {
        local_var_url_path <- gsub("\\{formulaId\\}", URLencode(as.character(`formula_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "IsotopePatternAnnotation", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns Lipid annotation (ElGordo) for the given formula result identifier.
    #'
    #' @description
    #' Returns Lipid annotation (ElGordo) for the given formula result identifier.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return LipidAnnotation
    #' @export
    GetLipidAnnotation = function(project_id, aligned_feature_id, formula_id, data_file = NULL, ...) {
      local_var_response <- self$GetLipidAnnotationWithHttpInfo(project_id, aligned_feature_id, formula_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns Lipid annotation (ElGordo) for the given formula result identifier.
    #'
    #' @description
    #' Returns Lipid annotation (ElGordo) for the given formula result identifier.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (LipidAnnotation) with additional information such as HTTP status code, headers
    #' @export
    GetLipidAnnotationWithHttpInfo = function(project_id, aligned_feature_id, formula_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }

      if (missing(`formula_id`)) {
        stop("Missing required parameter `formula_id`.")
      }




      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas/{formulaId}/lipid-annotation"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`formula_id`)) {
        local_var_url_path <- gsub("\\{formulaId\\}", URLencode(as.character(`formula_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "LipidAnnotation", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Mass Spec data (input data) for the given 'alignedFeatureId' .
    #'
    #' @description
    #' Mass Spec data (input data) for the given 'alignedFeatureId' .
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the Mass Spec data belong sto.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return MsData
    #' @export
    GetMsData = function(project_id, aligned_feature_id, data_file = NULL, ...) {
      local_var_response <- self$GetMsDataWithHttpInfo(project_id, aligned_feature_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Mass Spec data (input data) for the given 'alignedFeatureId' .
    #'
    #' @description
    #' Mass Spec data (input data) for the given 'alignedFeatureId' .
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the Mass Spec data belong sto.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (MsData) with additional information such as HTTP status code, headers
    #' @export
    GetMsDataWithHttpInfo = function(project_id, aligned_feature_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }



      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/ms-data"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "MsData", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param project_id 
    #' @param aligned_feature_id 
    #' @param type (optional) No description (default value: "APEX_HEIGHT")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return QuantificationTable
    #' @export
    GetQuantification = function(project_id, aligned_feature_id, type = "APEX_HEIGHT", data_file = NULL, ...) {
      local_var_response <- self$GetQuantificationWithHttpInfo(project_id, aligned_feature_id, type, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param project_id 
    #' @param aligned_feature_id 
    #' @param type (optional) No description (default value: "APEX_HEIGHT")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (QuantificationTable) with additional information such as HTTP status code, headers
    #' @export
    GetQuantificationWithHttpInfo = function(project_id, aligned_feature_id, type = "APEX_HEIGHT", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }




      if (!(`type` %in% c("APEX_HEIGHT"))) {
        stop("Invalid value for type when calling FeaturesApi$GetQuantification. Must be [APEX_HEIGHT].")
      }
      query_params[["type"]] <- `type`

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/quantification"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "QuantificationTable", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns fragmentation tree (SIRIUS) for the given formula result identifier in SIRIUS' internal format.
    #'
    #' @description
    #' Returns fragmentation tree (SIRIUS) for the given formula result identifier in SIRIUS' internal format.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return character
    #' @export
    GetSiriusFragTree = function(project_id, aligned_feature_id, formula_id, data_file = NULL, ...) {
      local_var_response <- self$GetSiriusFragTreeWithHttpInfo(project_id, aligned_feature_id, formula_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns fragmentation tree (SIRIUS) for the given formula result identifier in SIRIUS' internal format.
    #'
    #' @description
    #' Returns fragmentation tree (SIRIUS) for the given formula result identifier in SIRIUS' internal format.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (character) with additional information such as HTTP status code, headers
    #' @export
    GetSiriusFragTreeWithHttpInfo = function(project_id, aligned_feature_id, formula_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }

      if (missing(`formula_id`)) {
        stop("Missing required parameter `formula_id`.")
      }




      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas/{formulaId}/sirius-fragtree"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`formula_id`)) {
        local_var_url_path <- gsub("\\{formulaId\\}", URLencode(as.character(`formula_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "character", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' List of spectral library matches for the given 'alignedFeatureId'.
    #'
    #' @description
    #' List of spectral library matches for the given 'alignedFeatureId'.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the structure candidates belong to.
    #' @param match_id 
    #' @param opt_fields (optional) No description (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return SpectralLibraryMatch
    #' @export
    GetSpectralLibraryMatch = function(project_id, aligned_feature_id, match_id, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetSpectralLibraryMatchWithHttpInfo(project_id, aligned_feature_id, match_id, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List of spectral library matches for the given 'alignedFeatureId'.
    #'
    #' @description
    #' List of spectral library matches for the given 'alignedFeatureId'.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the structure candidates belong to.
    #' @param match_id 
    #' @param opt_fields (optional) No description (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (SpectralLibraryMatch) with additional information such as HTTP status code, headers
    #' @export
    GetSpectralLibraryMatchWithHttpInfo = function(project_id, aligned_feature_id, match_id, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }

      if (missing(`match_id`)) {
        stop("Missing required parameter `match_id`.")
      }





      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/spectral-library-matches/{matchId}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`match_id`)) {
        local_var_url_path <- gsub("\\{matchId\\}", URLencode(as.character(`match_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "SpectralLibraryMatch", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' List of spectral library matches for the given 'alignedFeatureId'.
    #'
    #' @description
    #' List of spectral library matches for the given 'alignedFeatureId'.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the structure candidates belong to.
    #' @param min_shared_peaks (optional) No description (default value: 1)
    #' @param min_similarity (optional) No description (default value: 0.2)
    #' @param candidate_in_chi_key (optional) No description (default value: "")
    #' @param opt_fields (optional) No description (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SpectralLibraryMatch]
    #' @export
    GetSpectralLibraryMatches = function(project_id, aligned_feature_id, min_shared_peaks = 1, min_similarity = 0.2, candidate_in_chi_key = "", opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetSpectralLibraryMatchesWithHttpInfo(project_id, aligned_feature_id, min_shared_peaks, min_similarity, candidate_in_chi_key, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List of spectral library matches for the given 'alignedFeatureId'.
    #'
    #' @description
    #' List of spectral library matches for the given 'alignedFeatureId'.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the structure candidates belong to.
    #' @param min_shared_peaks (optional) No description (default value: 1)
    #' @param min_similarity (optional) No description (default value: 0.2)
    #' @param candidate_in_chi_key (optional) No description (default value: "")
    #' @param opt_fields (optional) No description (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SpectralLibraryMatch]) with additional information such as HTTP status code, headers
    #' @export
    GetSpectralLibraryMatchesWithHttpInfo = function(project_id, aligned_feature_id, min_shared_peaks = 1, min_similarity = 0.2, candidate_in_chi_key = "", opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }







      query_params[["minSharedPeaks"]] <- `min_shared_peaks`

      query_params[["minSimilarity"]] <- `min_similarity`

      query_params[["candidateInChiKey"]] <- `candidate_in_chi_key`

      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/spectral-library-matches"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "array[SpectralLibraryMatch]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Page of spectral library matches for the given 'alignedFeatureId'.
    #'
    #' @description
    #' Page of spectral library matches for the given 'alignedFeatureId'.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the structure candidates belong to.
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param min_shared_peaks (optional) No description (default value: 1)
    #' @param min_similarity (optional) No description (default value: 0.2)
    #' @param candidate_in_chi_key (optional) No description (default value: "")
    #' @param opt_fields (optional) No description (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PageSpectralLibraryMatch
    #' @export
    GetSpectralLibraryMatchesPaged = function(project_id, aligned_feature_id, page = 0, size = 20, sort = NULL, min_shared_peaks = 1, min_similarity = 0.2, candidate_in_chi_key = "", opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetSpectralLibraryMatchesPagedWithHttpInfo(project_id, aligned_feature_id, page, size, sort, min_shared_peaks, min_similarity, candidate_in_chi_key, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Page of spectral library matches for the given 'alignedFeatureId'.
    #'
    #' @description
    #' Page of spectral library matches for the given 'alignedFeatureId'.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the structure candidates belong to.
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param min_shared_peaks (optional) No description (default value: 1)
    #' @param min_similarity (optional) No description (default value: 0.2)
    #' @param candidate_in_chi_key (optional) No description (default value: "")
    #' @param opt_fields (optional) No description (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PageSpectralLibraryMatch) with additional information such as HTTP status code, headers
    #' @export
    GetSpectralLibraryMatchesPagedWithHttpInfo = function(project_id, aligned_feature_id, page = 0, size = 20, sort = NULL, min_shared_peaks = 1, min_similarity = 0.2, candidate_in_chi_key = "", opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }



      if (`page` < 0) {
        stop("Invalid value for `page` when calling FeaturesApi$GetSpectralLibraryMatchesPaged, must be bigger than or equal to 0.")
      }

      if (`size` < 1) {
        stop("Invalid value for `size` when calling FeaturesApi$GetSpectralLibraryMatchesPaged, must be bigger than or equal to 1.")
      }






      query_params[["page"]] <- `page`

      query_params[["size"]] <- `size`

      # explore
      for (query_item in `sort`) {
        query_params[["sort"]] <- c(query_params[["sort"]], list(`sort` = query_item))
      }

      query_params[["minSharedPeaks"]] <- `min_shared_peaks`

      query_params[["minSimilarity"]] <- `min_similarity`

      query_params[["candidateInChiKey"]] <- `candidate_in_chi_key`

      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/spectral-library-matches/page"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PageSpectralLibraryMatch", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Summarize matched reference spectra for the given 'alignedFeatureId'.
    #'
    #' @description
    #' Summarize matched reference spectra for the given 'alignedFeatureId'.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the structure candidates belong to.
    #' @param min_shared_peaks (optional) min threshold of shared peaks. (default value: 1)
    #' @param min_similarity (optional) min spectral similarity threshold. (default value: 0.2)
    #' @param candidate_in_chi_key (optional) inchi key of the database compound. (default value: "")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return SpectralLibraryMatchSummary
    #' @export
    GetSpectralLibraryMatchesSummary = function(project_id, aligned_feature_id, min_shared_peaks = 1, min_similarity = 0.2, candidate_in_chi_key = "", data_file = NULL, ...) {
      local_var_response <- self$GetSpectralLibraryMatchesSummaryWithHttpInfo(project_id, aligned_feature_id, min_shared_peaks, min_similarity, candidate_in_chi_key, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Summarize matched reference spectra for the given 'alignedFeatureId'.
    #'
    #' @description
    #' Summarize matched reference spectra for the given 'alignedFeatureId'.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the structure candidates belong to.
    #' @param min_shared_peaks (optional) min threshold of shared peaks. (default value: 1)
    #' @param min_similarity (optional) min spectral similarity threshold. (default value: 0.2)
    #' @param candidate_in_chi_key (optional) inchi key of the database compound. (default value: "")
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (SpectralLibraryMatchSummary) with additional information such as HTTP status code, headers
    #' @export
    GetSpectralLibraryMatchesSummaryWithHttpInfo = function(project_id, aligned_feature_id, min_shared_peaks = 1, min_similarity = 0.2, candidate_in_chi_key = "", data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }






      query_params[["minSharedPeaks"]] <- `min_shared_peaks`

      query_params[["minSimilarity"]] <- `min_similarity`

      query_params[["candidateInChiKey"]] <- `candidate_in_chi_key`

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/spectral-library-matches/summary"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "SpectralLibraryMatchSummary", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns MS/MS Data (Merged MS/MS and list of measured MS/MS ) which are annotated with fragments and losses  for the given formula result identifier and structure candidate inChIKey.
    #'
    #' @description
    #' Returns MS/MS Data (Merged MS/MS and list of measured MS/MS ) which are annotated with fragments and losses  for the given formula result identifier and structure candidate inChIKey.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param inchi_key 2d InChIKey of the structure candidate to be used to annotate the spectrum annotation
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnnotatedMsMsData
    #' @export
    GetStructureAnnotatedMsData = function(project_id, aligned_feature_id, formula_id, inchi_key, data_file = NULL, ...) {
      local_var_response <- self$GetStructureAnnotatedMsDataWithHttpInfo(project_id, aligned_feature_id, formula_id, inchi_key, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns MS/MS Data (Merged MS/MS and list of measured MS/MS ) which are annotated with fragments and losses  for the given formula result identifier and structure candidate inChIKey.
    #'
    #' @description
    #' Returns MS/MS Data (Merged MS/MS and list of measured MS/MS ) which are annotated with fragments and losses  for the given formula result identifier and structure candidate inChIKey.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param inchi_key 2d InChIKey of the structure candidate to be used to annotate the spectrum annotation
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnnotatedMsMsData) with additional information such as HTTP status code, headers
    #' @export
    GetStructureAnnotatedMsDataWithHttpInfo = function(project_id, aligned_feature_id, formula_id, inchi_key, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }

      if (missing(`formula_id`)) {
        stop("Missing required parameter `formula_id`.")
      }

      if (missing(`inchi_key`)) {
        stop("Missing required parameter `inchi_key`.")
      }





      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas/{formulaId}/structures/{inchiKey}/annotated-msmsdata"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`formula_id`)) {
        local_var_url_path <- gsub("\\{formulaId\\}", URLencode(as.character(`formula_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`inchi_key`)) {
        local_var_url_path <- gsub("\\{inchiKey\\}", URLencode(as.character(`inchi_key`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnnotatedMsMsData", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Returns a fragmentation spectrum (e.g. Merged MS/MS) which is annotated with fragments and losses for the given formula result identifier  These annotations are only available if a fragmentation tree is available.
    #'
    #' @description
    #' Returns a fragmentation spectrum (e.g. Merged MS/MS) which is annotated with fragments and losses for the given formula result identifier  These annotations are only available if a fragmentation tree is available.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param inchi_key 2d InChIKey of the structure candidate to be used to annotate the spectrum annotation
    #' @param spectrum_index (optional) index of the spectrum to be annotated. Merged MS/MS will be used if spectrumIndex < 0 (default) (default value: -1)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnnotatedSpectrum
    #' @export
    GetStructureAnnotatedSpectrum = function(project_id, aligned_feature_id, formula_id, inchi_key, spectrum_index = -1, data_file = NULL, ...) {
      local_var_response <- self$GetStructureAnnotatedSpectrumWithHttpInfo(project_id, aligned_feature_id, formula_id, inchi_key, spectrum_index, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Returns a fragmentation spectrum (e.g. Merged MS/MS) which is annotated with fragments and losses for the given formula result identifier  These annotations are only available if a fragmentation tree is available.
    #'
    #' @description
    #' Returns a fragmentation spectrum (e.g. Merged MS/MS) which is annotated with fragments and losses for the given formula result identifier  These annotations are only available if a fragmentation tree is available.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param inchi_key 2d InChIKey of the structure candidate to be used to annotate the spectrum annotation
    #' @param spectrum_index (optional) index of the spectrum to be annotated. Merged MS/MS will be used if spectrumIndex < 0 (default) (default value: -1)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnnotatedSpectrum) with additional information such as HTTP status code, headers
    #' @export
    GetStructureAnnotatedSpectrumWithHttpInfo = function(project_id, aligned_feature_id, formula_id, inchi_key, spectrum_index = -1, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }

      if (missing(`formula_id`)) {
        stop("Missing required parameter `formula_id`.")
      }

      if (missing(`inchi_key`)) {
        stop("Missing required parameter `inchi_key`.")
      }






      query_params[["spectrumIndex"]] <- `spectrum_index`

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas/{formulaId}/structures/{inchiKey}/annotated-spectrum"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`formula_id`)) {
        local_var_url_path <- gsub("\\{formulaId\\}", URLencode(as.character(`formula_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`inchi_key`)) {
        local_var_url_path <- gsub("\\{inchiKey\\}", URLencode(as.character(`inchi_key`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnnotatedSpectrum", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' List of structure database search candidates ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.
    #'
    #' @description
    #' List of structure database search candidates ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the structure candidates belong to.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[StructureCandidateFormula]
    #' @export
    GetStructureCandidates = function(project_id, aligned_feature_id, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetStructureCandidatesWithHttpInfo(project_id, aligned_feature_id, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List of structure database search candidates ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.
    #'
    #' @description
    #' List of structure database search candidates ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the structure candidates belong to.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[StructureCandidateFormula]) with additional information such as HTTP status code, headers
    #' @export
    GetStructureCandidatesWithHttpInfo = function(project_id, aligned_feature_id, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }




      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/db-structures"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "array[StructureCandidateFormula]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' List of CSI:FingerID structure database search candidates for the given 'formulaId' with minimal information.
    #'
    #' @description
    #' List of CSI:FingerID structure database search candidates for the given 'formulaId' with minimal information.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[StructureCandidateScored]
    #' @export
    GetStructureCandidatesByFormula = function(project_id, aligned_feature_id, formula_id, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetStructureCandidatesByFormulaWithHttpInfo(project_id, aligned_feature_id, formula_id, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List of CSI:FingerID structure database search candidates for the given 'formulaId' with minimal information.
    #'
    #' @description
    #' List of CSI:FingerID structure database search candidates for the given 'formulaId' with minimal information.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[StructureCandidateScored]) with additional information such as HTTP status code, headers
    #' @export
    GetStructureCandidatesByFormulaWithHttpInfo = function(project_id, aligned_feature_id, formula_id, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }

      if (missing(`formula_id`)) {
        stop("Missing required parameter `formula_id`.")
      }





      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas/{formulaId}/db-structures"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`formula_id`)) {
        local_var_url_path <- gsub("\\{formulaId\\}", URLencode(as.character(`formula_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "array[StructureCandidateScored]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Page of CSI:FingerID structure database search candidates for the given 'formulaId' with minimal information.
    #'
    #' @description
    #' Page of CSI:FingerID structure database search candidates for the given 'formulaId' with minimal information.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PageStructureCandidateScored
    #' @export
    GetStructureCandidatesByFormulaPaged = function(project_id, aligned_feature_id, formula_id, page = 0, size = 20, sort = NULL, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetStructureCandidatesByFormulaPagedWithHttpInfo(project_id, aligned_feature_id, formula_id, page, size, sort, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Page of CSI:FingerID structure database search candidates for the given 'formulaId' with minimal information.
    #'
    #' @description
    #' Page of CSI:FingerID structure database search candidates for the given 'formulaId' with minimal information.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the formula result belongs to.
    #' @param formula_id identifier of the requested formula result
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PageStructureCandidateScored) with additional information such as HTTP status code, headers
    #' @export
    GetStructureCandidatesByFormulaPagedWithHttpInfo = function(project_id, aligned_feature_id, formula_id, page = 0, size = 20, sort = NULL, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }

      if (missing(`formula_id`)) {
        stop("Missing required parameter `formula_id`.")
      }




      if (`page` < 0) {
        stop("Invalid value for `page` when calling FeaturesApi$GetStructureCandidatesByFormulaPaged, must be bigger than or equal to 0.")
      }

      if (`size` < 1) {
        stop("Invalid value for `size` when calling FeaturesApi$GetStructureCandidatesByFormulaPaged, must be bigger than or equal to 1.")
      }



      query_params[["page"]] <- `page`

      query_params[["size"]] <- `size`

      # explore
      for (query_item in `sort`) {
        query_params[["sort"]] <- c(query_params[["sort"]], list(`sort` = query_item))
      }

      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/formulas/{formulaId}/db-structures/page"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`formula_id`)) {
        local_var_url_path <- gsub("\\{formulaId\\}", URLencode(as.character(`formula_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PageStructureCandidateScored", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Page of structure database search candidates ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.
    #'
    #' @description
    #' Page of structure database search candidates ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the structure candidates belong to.
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PageStructureCandidateFormula
    #' @export
    GetStructureCandidatesPaged = function(project_id, aligned_feature_id, page = 0, size = 20, sort = NULL, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetStructureCandidatesPagedWithHttpInfo(project_id, aligned_feature_id, page, size, sort, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Page of structure database search candidates ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.
    #'
    #' @description
    #' Page of structure database search candidates ranked by CSI:FingerID score for the given 'alignedFeatureId' with minimal information.
    #'
    #' @param project_id project-space to read from.
    #' @param aligned_feature_id feature (aligned over runs) the structure candidates belong to.
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PageStructureCandidateFormula) with additional information such as HTTP status code, headers
    #' @export
    GetStructureCandidatesPagedWithHttpInfo = function(project_id, aligned_feature_id, page = 0, size = 20, sort = NULL, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }



      if (`page` < 0) {
        stop("Invalid value for `page` when calling FeaturesApi$GetStructureCandidatesPaged, must be bigger than or equal to 0.")
      }

      if (`size` < 1) {
        stop("Invalid value for `size` when calling FeaturesApi$GetStructureCandidatesPaged, must be bigger than or equal to 1.")
      }



      query_params[["page"]] <- `page`

      query_params[["size"]] <- `size`

      # explore
      for (query_item in `sort`) {
        query_params[["sort"]] <- c(query_params[["sort"]], list(`sort` = query_item))
      }

      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/db-structures/page"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PageStructureCandidateFormula", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param project_id 
    #' @param aligned_feature_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return TraceSet
    #' @export
    GetTraces1 = function(project_id, aligned_feature_id, data_file = NULL, ...) {
      local_var_response <- self$GetTraces1WithHttpInfo(project_id, aligned_feature_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param project_id 
    #' @param aligned_feature_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (TraceSet) with additional information such as HTTP status code, headers
    #' @export
    GetTraces1WithHttpInfo = function(project_id, aligned_feature_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`aligned_feature_id`)) {
        stop("Missing required parameter `aligned_feature_id`.")
      }



      local_var_url_path <- "/api/projects/{projectId}/aligned-features/{alignedFeatureId}/traces"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`aligned_feature_id`)) {
        local_var_url_path <- gsub("\\{alignedFeatureId\\}", URLencode(as.character(`aligned_feature_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "TraceSet", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
