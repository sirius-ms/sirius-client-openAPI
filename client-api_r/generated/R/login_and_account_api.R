#' SIRIUS Nightsky API
#'
#' REST API that provides the full functionality of SIRIUS and its web services as background service. It is intended as entry-point for scripting languages and software integration SDKs.This API is exposed by SIRIUS 6.0.0-SNAPSHOT
#'
#' The version of the OpenAPI document: 2.0
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title LoginAndAccount operations
#' @description LoginAndAccountApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ GetAccountInfo } \emph{ Get information about the account currently logged in. }
#' Get information about the account currently logged in. Fails if not logged in.
#'
#' \itemize{
#' \item \emph{ @param } include_subs character
#' \item \emph{ @returnType } \link{AccountInfo} \cr
#'
#'
#' \item status code : 200 | Basic information about the account that has been logged in and its subscriptions.
#'
#' \item return type : AccountInfo
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetSignUpURL } \emph{ Get SignUp URL (For signUp via web browser) }
#' Get SignUp URL (For signUp via web browser)
#'
#' \itemize{
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : character
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetSubscriptions } \emph{ Get available subscriptions of the account currently logged in. }
#' Get available subscriptions of the account currently logged in. Fails if not logged in.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{Subscription} ) \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : array[Subscription]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ IsLoggedIn } \emph{ Check if a user is logged in. }
#' Check if a user is logged in.
#'
#' \itemize{
#'
#'
#' \item status code : 200 | true if the user is logged in
#'
#' \item return type : character
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ Login } \emph{ Login into SIRIUS web services and activate default subscription if available. }
#' Login into SIRIUS web services and activate default subscription if available.
#'
#' \itemize{
#' \item \emph{ @param } accept_terms character
#' \item \emph{ @param } account_credentials \link{AccountCredentials}
#' \item \emph{ @param } fail_when_logged_in character
#' \item \emph{ @param } include_subs character
#' \item \emph{ @returnType } \link{AccountInfo} \cr
#'
#'
#' \item status code : 200 | Basic information about the account that has been logged in and its subscriptions.
#'
#' \item return type : AccountInfo
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ Logout } \emph{ Logout from SIRIUS web services. }
#' Logout from SIRIUS web services.
#'
#' \itemize{
#'
#'
#' \item status code : 200 | OK
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ OpenPortal } \emph{ Open User portal in browser. }
#' Open User portal in browser. If user is logged in SIRIUS tries to transfer the login state to the browser.
#'
#' \itemize{
#'
#'
#' \item status code : 200 | OK
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SelectSubscription } \emph{ Select a subscription as active subscription to be used for computations. }
#' Select a subscription as active subscription to be used for computations.
#'
#' \itemize{
#' \item \emph{ @param } sid character
#' \item \emph{ @returnType } \link{AccountInfo} \cr
#'
#'
#' \item status code : 200 | Account information with updated active subscription
#'
#' \item return type : AccountInfo
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SignUp } \emph{ Open SignUp window in system browser and return signUp link. }
#' Open SignUp window in system browser and return signUp link.
#'
#' \itemize{
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : character
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  GetAccountInfo  ####################
#'
#' library(Rsirius)
#' var_include_subs <- FALSE # character | include available and active subscriptions in {@link AccountInfo AccountInfo}. (Optional)
#'
#' #Get information about the account currently logged in.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAccountInfo(include_subs = var_include_subsdata_file = "result.txt")
#' result <- api_instance$login_and_account_api$GetAccountInfo(include_subs = var_include_subs)
#' dput(result)
#'
#'
#' ####################  GetSignUpURL  ####################
#'
#' library(Rsirius)
#'
#' #Get SignUp URL (For signUp via web browser)
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetSignUpURL(data_file = "result.txt")
#' result <- api_instance$login_and_account_api$GetSignUpURL()
#' dput(result)
#'
#'
#' ####################  GetSubscriptions  ####################
#'
#' library(Rsirius)
#'
#' #Get available subscriptions of the account currently logged in.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetSubscriptions(data_file = "result.txt")
#' result <- api_instance$login_and_account_api$GetSubscriptions()
#' dput(result)
#'
#'
#' ####################  IsLoggedIn  ####################
#'
#' library(Rsirius)
#'
#' #Check if a user is logged in.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$IsLoggedIn(data_file = "result.txt")
#' result <- api_instance$login_and_account_api$IsLoggedIn()
#' dput(result)
#'
#'
#' ####################  Login  ####################
#'
#' library(Rsirius)
#' var_accept_terms <- "accept_terms_example" # character | 
#' var_account_credentials <- AccountCredentials$new("username_example", "password_example", "refreshToken_example") # AccountCredentials | used to log in.
#' var_fail_when_logged_in <- FALSE # character | if true request fails if an active login already exists. (Optional)
#' var_include_subs <- FALSE # character | include available and active subscriptions in {@link AccountInfo AccountInfo}. (Optional)
#'
#' #Login into SIRIUS web services and activate default subscription if available.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$Login(var_accept_terms, var_account_credentials, fail_when_logged_in = var_fail_when_logged_in, include_subs = var_include_subsdata_file = "result.txt")
#' result <- api_instance$login_and_account_api$Login(var_accept_terms, var_account_credentials, fail_when_logged_in = var_fail_when_logged_in, include_subs = var_include_subs)
#' dput(result)
#'
#'
#' ####################  Logout  ####################
#'
#' library(Rsirius)
#'
#' #Logout from SIRIUS web services.
#' api_instance <- rsirius_api$new()
#'
#' api_instance$login_and_account_api$Logout()
#'
#'
#' ####################  OpenPortal  ####################
#'
#' library(Rsirius)
#'
#' #Open User portal in browser.
#' api_instance <- rsirius_api$new()
#'
#' api_instance$login_and_account_api$OpenPortal()
#'
#'
#' ####################  SelectSubscription  ####################
#'
#' library(Rsirius)
#' var_sid <- "sid_example" # character | 
#'
#' #Select a subscription as active subscription to be used for computations.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SelectSubscription(var_siddata_file = "result.txt")
#' result <- api_instance$login_and_account_api$SelectSubscription(var_sid)
#' dput(result)
#'
#'
#' ####################  SignUp  ####################
#'
#' library(Rsirius)
#'
#' #Open SignUp window in system browser and return signUp link.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$SignUp(data_file = "result.txt")
#' result <- api_instance$login_and_account_api$SignUp()
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
LoginAndAccountApi <- R6::R6Class(
  "LoginAndAccountApi",
  public = list(
    api_client = NULL,
    #' Initialize a new LoginAndAccountApi.
    #'
    #' @description
    #' Initialize a new LoginAndAccountApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Get information about the account currently logged in.
    #'
    #' @description
    #' Get information about the account currently logged in.
    #'
    #' @param include_subs (optional) include available and active subscriptions in {@link AccountInfo AccountInfo}. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AccountInfo
    #' @export
    GetAccountInfo = function(include_subs = FALSE, data_file = NULL, ...) {
      local_var_response <- self$GetAccountInfoWithHttpInfo(include_subs, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get information about the account currently logged in.
    #'
    #' @description
    #' Get information about the account currently logged in.
    #'
    #' @param include_subs (optional) include available and active subscriptions in {@link AccountInfo AccountInfo}. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AccountInfo) with additional information such as HTTP status code, headers
    #' @export
    GetAccountInfoWithHttpInfo = function(include_subs = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      query_params[["includeSubs"]] <- `include_subs`

      local_var_url_path <- "/api/account/"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AccountInfo", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get SignUp URL (For signUp via web browser)
    #'
    #' @description
    #' Get SignUp URL (For signUp via web browser)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return character
    #' @export
    GetSignUpURL = function(data_file = NULL, ...) {
      local_var_response <- self$GetSignUpURLWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get SignUp URL (For signUp via web browser)
    #'
    #' @description
    #' Get SignUp URL (For signUp via web browser)
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (character) with additional information such as HTTP status code, headers
    #' @export
    GetSignUpURLWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/api/account/signUpURL"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain;charset=UTF-8")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "character", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get available subscriptions of the account currently logged in.
    #'
    #' @description
    #' Get available subscriptions of the account currently logged in.
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[Subscription]
    #' @export
    GetSubscriptions = function(data_file = NULL, ...) {
      local_var_response <- self$GetSubscriptionsWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get available subscriptions of the account currently logged in.
    #'
    #' @description
    #' Get available subscriptions of the account currently logged in.
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[Subscription]) with additional information such as HTTP status code, headers
    #' @export
    GetSubscriptionsWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/api/account/subscriptions"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "array[Subscription]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Check if a user is logged in.
    #'
    #' @description
    #' Check if a user is logged in.
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return character
    #' @export
    IsLoggedIn = function(data_file = NULL, ...) {
      local_var_response <- self$IsLoggedInWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Check if a user is logged in.
    #'
    #' @description
    #' Check if a user is logged in.
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (character) with additional information such as HTTP status code, headers
    #' @export
    IsLoggedInWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/api/account/isLoggedIn"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "character", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Login into SIRIUS web services and activate default subscription if available.
    #'
    #' @description
    #' Login into SIRIUS web services and activate default subscription if available.
    #'
    #' @param accept_terms 
    #' @param account_credentials used to log in.
    #' @param fail_when_logged_in (optional) if true request fails if an active login already exists. (default value: FALSE)
    #' @param include_subs (optional) include available and active subscriptions in {@link AccountInfo AccountInfo}. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AccountInfo
    #' @export
    Login = function(accept_terms, account_credentials, fail_when_logged_in = FALSE, include_subs = FALSE, data_file = NULL, ...) {
      local_var_response <- self$LoginWithHttpInfo(accept_terms, account_credentials, fail_when_logged_in, include_subs, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Login into SIRIUS web services and activate default subscription if available.
    #'
    #' @description
    #' Login into SIRIUS web services and activate default subscription if available.
    #'
    #' @param accept_terms 
    #' @param account_credentials used to log in.
    #' @param fail_when_logged_in (optional) if true request fails if an active login already exists. (default value: FALSE)
    #' @param include_subs (optional) include available and active subscriptions in {@link AccountInfo AccountInfo}. (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AccountInfo) with additional information such as HTTP status code, headers
    #' @export
    LoginWithHttpInfo = function(accept_terms, account_credentials, fail_when_logged_in = FALSE, include_subs = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`accept_terms`)) {
        stop("Missing required parameter `accept_terms`.")
      }

      if (missing(`account_credentials`)) {
        stop("Missing required parameter `account_credentials`.")
      }





      query_params[["acceptTerms"]] <- `accept_terms`

      query_params[["failWhenLoggedIn"]] <- `fail_when_logged_in`

      query_params[["includeSubs"]] <- `include_subs`

      if (!is.null(`account_credentials`)) {
        local_var_body <- `account_credentials`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/account/login"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AccountInfo", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Logout from SIRIUS web services.
    #'
    #' @description
    #' Logout from SIRIUS web services.
    #'
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    Logout = function(...) {
      local_var_response <- self$LogoutWithHttpInfo(...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Logout from SIRIUS web services.
    #'
    #' @description
    #' Logout from SIRIUS web services.
    #'
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    LogoutWithHttpInfo = function(...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/api/account/logout"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Open User portal in browser.
    #'
    #' @description
    #' Open User portal in browser.
    #'
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    OpenPortal = function(...) {
      local_var_response <- self$OpenPortalWithHttpInfo(...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Open User portal in browser.
    #'
    #' @description
    #' Open User portal in browser.
    #'
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    OpenPortalWithHttpInfo = function(...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/api/account/openPortal"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Select a subscription as active subscription to be used for computations.
    #'
    #' @description
    #' Select a subscription as active subscription to be used for computations.
    #'
    #' @param sid 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AccountInfo
    #' @export
    SelectSubscription = function(sid, data_file = NULL, ...) {
      local_var_response <- self$SelectSubscriptionWithHttpInfo(sid, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Select a subscription as active subscription to be used for computations.
    #'
    #' @description
    #' Select a subscription as active subscription to be used for computations.
    #'
    #' @param sid 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AccountInfo) with additional information such as HTTP status code, headers
    #' @export
    SelectSubscriptionWithHttpInfo = function(sid, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`sid`)) {
        stop("Missing required parameter `sid`.")
      }


      query_params[["sid"]] <- `sid`

      local_var_url_path <- "/api/account/subscriptions/select-active"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AccountInfo", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Open SignUp window in system browser and return signUp link.
    #'
    #' @description
    #' Open SignUp window in system browser and return signUp link.
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return character
    #' @export
    SignUp = function(data_file = NULL, ...) {
      local_var_response <- self$SignUpWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Open SignUp window in system browser and return signUp link.
    #'
    #' @description
    #' Open SignUp window in system browser and return signUp link.
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (character) with additional information such as HTTP status code, headers
    #' @export
    SignUpWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/api/account/signUp"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain;charset=UTF-8")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "character", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
