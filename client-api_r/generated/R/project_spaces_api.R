#' SIRIUS Nightsky API
#'
#' REST API that provides the full functionality of SIRIUS and its web services as background service. It is intended as entry-point for scripting languages and software integration SDKs.
#'
#' The version of the OpenAPI document: v0.9 on SIRIUS 5.6.3-SNAPSHOT
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title ProjectSpaces operations
#' @description ProjectSpacesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ CloseProjectSpace } \emph{ Close project-space and remove it from application. }
#' Close project-space and remove it from application. Project-space will NOT be deleted from disk.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#'
#'
#' \item status code : 200 | OK
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateProjectSpace } \emph{ Create and open a new project-space at given location and make it accessible via the given projectId. }
#' Create and open a new project-space at given location and make it accessible via the given projectId.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } path_to_project character
#' \item \emph{ @param } path_to_source_project character
#' \item \emph{ @param } await_import character
#' \item \emph{ @returnType } \link{ProjectSpaceId} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ProjectSpaceId
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetProjectSpace } \emph{ Get project space info by its projectId. }
#' Get project space info by its projectId.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @returnType } \link{ProjectSpaceId} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ProjectSpaceId
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetProjectSpaces } \emph{ List all opened project spaces. }
#' List all opened project spaces.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{ProjectSpaceId} ) \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : array[ProjectSpaceId]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ OpenProjectSpace } \emph{ Open an existing project-space and make it accessible via the given projectId. }
#' Open an existing project-space and make it accessible via the given projectId.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } path_to_project character
#' \item \emph{ @returnType } \link{ProjectSpaceId} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ProjectSpaceId
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  CloseProjectSpace  ####################
#'
#' library(RSirius)
#' var_project_id <- "project_id_example" # character | unique name/identifier of the  project-space to be closed.
#'
#' #Close project-space and remove it from application.
#' api_instance <- rsirius_api$new()
#'
#' api_instance$project_spaces_api$CloseProjectSpace(var_project_id)
#'
#'
#' ####################  CreateProjectSpace  ####################
#'
#' library(RSirius)
#' var_project_id <- "project_id_example" # character | unique name/identifier that shall be used to access the newly created project-space.
#' var_path_to_project <- "path_to_project_example" # character | 
#' var_path_to_source_project <- "path_to_source_project_example" # character |  (Optional)
#' var_await_import <- TRUE # character |  (Optional)
#'
#' #Create and open a new project-space at given location and make it accessible via the given projectId.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateProjectSpace(var_project_id, var_path_to_project, path_to_source_project = var_path_to_source_project, await_import = var_await_importdata_file = "result.txt")
#' result <- api_instance$project_spaces_api$CreateProjectSpace(var_project_id, var_path_to_project, path_to_source_project = var_path_to_source_project, await_import = var_await_import)
#' dput(result)
#'
#'
#' ####################  GetProjectSpace  ####################
#'
#' library(RSirius)
#' var_project_id <- "project_id_example" # character | unique name/identifier tof the project-space to be accessed.
#'
#' #Get project space info by its projectId.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetProjectSpace(var_project_iddata_file = "result.txt")
#' result <- api_instance$project_spaces_api$GetProjectSpace(var_project_id)
#' dput(result)
#'
#'
#' ####################  GetProjectSpaces  ####################
#'
#' library(RSirius)
#'
#' #List all opened project spaces.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetProjectSpaces(data_file = "result.txt")
#' result <- api_instance$project_spaces_api$GetProjectSpaces()
#' dput(result)
#'
#'
#' ####################  OpenProjectSpace  ####################
#'
#' library(RSirius)
#' var_project_id <- "project_id_example" # character | unique name/identifier that shall be used to access the opened project-space.
#' var_path_to_project <- "path_to_project_example" # character | 
#'
#' #Open an existing project-space and make it accessible via the given projectId.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$OpenProjectSpace(var_project_id, var_path_to_projectdata_file = "result.txt")
#' result <- api_instance$project_spaces_api$OpenProjectSpace(var_project_id, var_path_to_project)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ProjectSpacesApi <- R6::R6Class(
  "ProjectSpacesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new ProjectSpacesApi.
    #'
    #' @description
    #' Initialize a new ProjectSpacesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Close project-space and remove it from application.
    #'
    #' @description
    #' Close project-space and remove it from application.
    #'
    #' @param project_id unique name/identifier of the  project-space to be closed.
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    CloseProjectSpace = function(project_id, ...) {
      local_var_response <- self$CloseProjectSpaceWithHttpInfo(project_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Close project-space and remove it from application.
    #'
    #' @description
    #' Close project-space and remove it from application.
    #'
    #' @param project_id unique name/identifier of the  project-space to be closed.
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    CloseProjectSpaceWithHttpInfo = function(project_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }


      local_var_url_path <- "/api/projects/{projectId}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Create and open a new project-space at given location and make it accessible via the given projectId.
    #'
    #' @description
    #' Create and open a new project-space at given location and make it accessible via the given projectId.
    #'
    #' @param project_id unique name/identifier that shall be used to access the newly created project-space.
    #' @param path_to_project 
    #' @param path_to_source_project (optional) No description
    #' @param await_import (optional) No description (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ProjectSpaceId
    #' @export
    CreateProjectSpace = function(project_id, path_to_project, path_to_source_project = NULL, await_import = TRUE, data_file = NULL, ...) {
      local_var_response <- self$CreateProjectSpaceWithHttpInfo(project_id, path_to_project, path_to_source_project, await_import, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Create and open a new project-space at given location and make it accessible via the given projectId.
    #'
    #' @description
    #' Create and open a new project-space at given location and make it accessible via the given projectId.
    #'
    #' @param project_id unique name/identifier that shall be used to access the newly created project-space.
    #' @param path_to_project 
    #' @param path_to_source_project (optional) No description
    #' @param await_import (optional) No description (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ProjectSpaceId) with additional information such as HTTP status code, headers
    #' @export
    CreateProjectSpaceWithHttpInfo = function(project_id, path_to_project, path_to_source_project = NULL, await_import = TRUE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`path_to_project`)) {
        stop("Missing required parameter `path_to_project`.")
      }





      query_params[["pathToProject"]] <- `path_to_project`

      query_params[["pathToSourceProject"]] <- `path_to_source_project`

      query_params[["awaitImport"]] <- `await_import`

      local_var_url_path <- "/api/projects/{projectId}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "ProjectSpaceId", loadNamespace("RSirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get project space info by its projectId.
    #'
    #' @description
    #' Get project space info by its projectId.
    #'
    #' @param project_id unique name/identifier tof the project-space to be accessed.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ProjectSpaceId
    #' @export
    GetProjectSpace = function(project_id, data_file = NULL, ...) {
      local_var_response <- self$GetProjectSpaceWithHttpInfo(project_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get project space info by its projectId.
    #'
    #' @description
    #' Get project space info by its projectId.
    #'
    #' @param project_id unique name/identifier tof the project-space to be accessed.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ProjectSpaceId) with additional information such as HTTP status code, headers
    #' @export
    GetProjectSpaceWithHttpInfo = function(project_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }


      local_var_url_path <- "/api/projects/{projectId}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "ProjectSpaceId", loadNamespace("RSirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' List all opened project spaces.
    #'
    #' @description
    #' List all opened project spaces.
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[ProjectSpaceId]
    #' @export
    GetProjectSpaces = function(data_file = NULL, ...) {
      local_var_response <- self$GetProjectSpacesWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List all opened project spaces.
    #'
    #' @description
    #' List all opened project spaces.
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[ProjectSpaceId]) with additional information such as HTTP status code, headers
    #' @export
    GetProjectSpacesWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/api/projects"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "array[ProjectSpaceId]", loadNamespace("RSirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Open an existing project-space and make it accessible via the given projectId.
    #'
    #' @description
    #' Open an existing project-space and make it accessible via the given projectId.
    #'
    #' @param project_id unique name/identifier that shall be used to access the opened project-space.
    #' @param path_to_project 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ProjectSpaceId
    #' @export
    OpenProjectSpace = function(project_id, path_to_project, data_file = NULL, ...) {
      local_var_response <- self$OpenProjectSpaceWithHttpInfo(project_id, path_to_project, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Open an existing project-space and make it accessible via the given projectId.
    #'
    #' @description
    #' Open an existing project-space and make it accessible via the given projectId.
    #'
    #' @param project_id unique name/identifier that shall be used to access the opened project-space.
    #' @param path_to_project 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ProjectSpaceId) with additional information such as HTTP status code, headers
    #' @export
    OpenProjectSpaceWithHttpInfo = function(project_id, path_to_project, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`path_to_project`)) {
        stop("Missing required parameter `path_to_project`.")
      }



      query_params[["pathToProject"]] <- `path_to_project`

      local_var_url_path <- "/api/projects/{projectId}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response, "ProjectSpaceId", loadNamespace("RSirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
