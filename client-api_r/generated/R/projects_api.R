#' SIRIUS Nightsky API
#'
#' REST API that provides the full functionality of SIRIUS and its web services as background service. It is intended as entry-point for scripting languages and software integration SDKs.This API is exposed by SIRIUS 6.0.0-SNAPSHOT
#'
#' The version of the OpenAPI document: 2.1
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Projects operations
#' @description ProjectsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ CloseProjectSpace } \emph{ Close project-space and remove it from application }
#' Close project-space and remove it from application. Project will NOT be deleted from disk.  <p>  ATTENTION: This will cancel and remove all jobs running on this Project before closing it.  If there are many jobs, this might take some time.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#'
#'
#' \item status code : 200 | OK
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CopyProjectSpace } \emph{ Move an existing (opened) project-space to another location. }
#' Move an existing (opened) project-space to another location.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } path_to_copied_project character
#' \item \emph{ @param } copy_project_id character
#' \item \emph{ @param } opt_fields list( \link{ProjectInfoOptField} )
#' \item \emph{ @returnType } \link{ProjectInfo} \cr
#'
#'
#' \item status code : 200 | ProjectInfo of the newly created project if opened (copyProjectId != null) or the project info of  the source project otherwise  <p>  DEPRECATED: This endpoint relies on the local filesystem and will likely be removed in later versions of this API to allow for more flexible use cases.
#'
#' \item return type : ProjectInfo
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateProjectSpace } \emph{ Create and open a new project-space at given location and make it accessible via the given projectId. }
#' Create and open a new project-space at given location and make it accessible via the given projectId.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } path_to_project character
#' \item \emph{ @returnType } \link{ProjectInfo} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ProjectInfo
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetCanopusClassyFireData } \emph{ Get CANOPUS prediction vector definition for ClassyFire classes }
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } charge integer
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : character
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetCanopusNpcData } \emph{ Get CANOPUS prediction vector definition for NPC classes }
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } charge integer
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : character
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetFingerIdData } \emph{ Get CSI:FingerID fingerprint (prediction vector) definition }
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } charge integer
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : character
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetProjectSpace } \emph{ Get project space info by its projectId. }
#' Get project space info by its projectId.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } opt_fields list( \link{ProjectInfoOptField} )
#' \item \emph{ @returnType } \link{ProjectInfo} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ProjectInfo
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetProjectSpaces } \emph{ List opened project spaces. }
#' List opened project spaces.
#'
#' \itemize{
#' \item \emph{ @returnType } list( \link{ProjectInfo} ) \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : array[ProjectInfo]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ImportMsRunData } \emph{ Import and Align full MS-Runs from various formats into the specified project  Possible formats (mzML, mzXML) }
#' Import and Align full MS-Runs from various formats into the specified project  Possible formats (mzML, mzXML)
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } align_runs character
#' \item \emph{ @param } allow_ms1_only character
#' \item \emph{ @param } input_files list( data.frame )
#' \item \emph{ @returnType } \link{ImportResult} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ImportResult
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ImportMsRunDataAsJob } \emph{ Import and Align full MS-Runs from various formats into the specified project as background job. }
#' Import and Align full MS-Runs from various formats into the specified project as background job.  Possible formats (mzML, mzXML)
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } align_runs character
#' \item \emph{ @param } allow_ms1_only character
#' \item \emph{ @param } opt_fields list( \link{JobOptField} )
#' \item \emph{ @param } input_files list( data.frame )
#' \item \emph{ @returnType } \link{Job} \cr
#'
#'
#' \item status code : 200 | the import job.
#'
#' \item return type : Job
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ImportPreprocessedData } \emph{ Import already preprocessed ms/ms data from various formats into the specified project  Possible formats (ms, mgf, cef, msp) }
#' Import already preprocessed ms/ms data from various formats into the specified project  Possible formats (ms, mgf, cef, msp)
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } ignore_formulas character
#' \item \emph{ @param } allow_ms1_only character
#' \item \emph{ @param } input_files list( data.frame )
#' \item \emph{ @returnType } \link{ImportResult} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ImportResult
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ImportPreprocessedDataAsJob } \emph{ Import ms/ms data from the given format into the specified project-space as background job. }
#' Import ms/ms data from the given format into the specified project-space as background job.  Possible formats (ms, mgf, cef, msp)
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } ignore_formulas character
#' \item \emph{ @param } allow_ms1_only character
#' \item \emph{ @param } opt_fields list( \link{JobOptField} )
#' \item \emph{ @param } imput_files list( data.frame )
#' \item \emph{ @returnType } \link{Job} \cr
#'
#'
#' \item status code : 200 | the import job.
#'
#' \item return type : Job
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ OpenProjectSpace } \emph{ Open an existing project-space and make it accessible via the given projectId. }
#' Open an existing project-space and make it accessible via the given projectId.
#'
#' \itemize{
#' \item \emph{ @param } project_id character
#' \item \emph{ @param } path_to_project character
#' \item \emph{ @param } opt_fields list( \link{ProjectInfoOptField} )
#' \item \emph{ @returnType } \link{ProjectInfo} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : ProjectInfo
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  CloseProjectSpace  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | unique name/identifier of the  project-space to be closed.
#'
#' #Close project-space and remove it from application
#' api_instance <- rsirius_api$new()
#'
#' api_instance$projects_api$CloseProjectSpace(var_project_id)
#'
#'
#' ####################  CopyProjectSpace  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | unique name/identifier of the project-space that shall be copied.
#' var_path_to_copied_project <- "path_to_copied_project_example" # character | target location where the source project will be copied to.
#' var_copy_project_id <- "copy_project_id_example" # character | optional id/mame of the newly created project (copy). If given the project will be opened. (Optional)
#' var_opt_fields <- c(ProjectInfoOptField$new()) # array[ProjectInfoOptField] |  (Optional)
#'
#' #Move an existing (opened) project-space to another location.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CopyProjectSpace(var_project_id, var_path_to_copied_project, copy_project_id = var_copy_project_id, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$projects_api$CopyProjectSpace(var_project_id, var_path_to_copied_project, copy_project_id = var_copy_project_id, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  CreateProjectSpace  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | unique name/identifier that shall be used to access the newly created project-space. Must consist only of [a-zA-Z0-9_-].
#' var_path_to_project <- "path_to_project_example" # character | local file path where the project will be created. If NULL, project will be stored by its projectId in default project location. DEPRECATED: This parameter relies on the local filesystem and will likely be removed in later versions of this API to allow for more flexible use cases. (Optional)
#'
#' #Create and open a new project-space at given location and make it accessible via the given projectId.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateProjectSpace(var_project_id, path_to_project = var_path_to_projectdata_file = "result.txt")
#' result <- api_instance$projects_api$CreateProjectSpace(var_project_id, path_to_project = var_path_to_project)
#' dput(result)
#'
#'
#' ####################  GetCanopusClassyFireData  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | 
#' var_charge <- 56 # integer | 
#'
#' #Get CANOPUS prediction vector definition for ClassyFire classes
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCanopusClassyFireData(var_project_id, var_chargedata_file = "result.txt")
#' result <- api_instance$projects_api$GetCanopusClassyFireData(var_project_id, var_charge)
#' dput(result)
#'
#'
#' ####################  GetCanopusNpcData  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | 
#' var_charge <- 56 # integer | 
#'
#' #Get CANOPUS prediction vector definition for NPC classes
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCanopusNpcData(var_project_id, var_chargedata_file = "result.txt")
#' result <- api_instance$projects_api$GetCanopusNpcData(var_project_id, var_charge)
#' dput(result)
#'
#'
#' ####################  GetFingerIdData  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | 
#' var_charge <- 56 # integer | 
#'
#' #Get CSI:FingerID fingerprint (prediction vector) definition
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetFingerIdData(var_project_id, var_chargedata_file = "result.txt")
#' result <- api_instance$projects_api$GetFingerIdData(var_project_id, var_charge)
#' dput(result)
#'
#'
#' ####################  GetProjectSpace  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | unique name/identifier tof the project-space to be accessed.
#' var_opt_fields <- c(ProjectInfoOptField$new()) # array[ProjectInfoOptField] |  (Optional)
#'
#' #Get project space info by its projectId.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetProjectSpace(var_project_id, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$projects_api$GetProjectSpace(var_project_id, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetProjectSpaces  ####################
#'
#' library(Rsirius)
#'
#' #List opened project spaces.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetProjectSpaces(data_file = "result.txt")
#' result <- api_instance$projects_api$GetProjectSpaces()
#' dput(result)
#'
#'
#' ####################  ImportMsRunData  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to import into.
#' var_align_runs <- TRUE # character |  (Optional)
#' var_allow_ms1_only <- TRUE # character |  (Optional)
#' var_input_files <- c(123) # array[data.frame] |  (Optional)
#'
#' #Import and Align full MS-Runs from various formats into the specified project  Possible formats (mzML, mzXML)
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ImportMsRunData(var_project_id, align_runs = var_align_runs, allow_ms1_only = var_allow_ms1_only, input_files = var_input_filesdata_file = "result.txt")
#' result <- api_instance$projects_api$ImportMsRunData(var_project_id, align_runs = var_align_runs, allow_ms1_only = var_allow_ms1_only, input_files = var_input_files)
#' dput(result)
#'
#'
#' ####################  ImportMsRunDataAsJob  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to import into.
#' var_align_runs <- TRUE # character |  (Optional)
#' var_allow_ms1_only <- TRUE # character |  (Optional)
#' var_opt_fields <- c(JobOptField$new()) # array[JobOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#' var_input_files <- c(123) # array[data.frame] |  (Optional)
#'
#' #Import and Align full MS-Runs from various formats into the specified project as background job.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ImportMsRunDataAsJob(var_project_id, align_runs = var_align_runs, allow_ms1_only = var_allow_ms1_only, opt_fields = var_opt_fields, input_files = var_input_filesdata_file = "result.txt")
#' result <- api_instance$projects_api$ImportMsRunDataAsJob(var_project_id, align_runs = var_align_runs, allow_ms1_only = var_allow_ms1_only, opt_fields = var_opt_fields, input_files = var_input_files)
#' dput(result)
#'
#'
#' ####################  ImportPreprocessedData  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to import into.
#' var_ignore_formulas <- FALSE # character |  (Optional)
#' var_allow_ms1_only <- TRUE # character |  (Optional)
#' var_input_files <- c(123) # array[data.frame] |  (Optional)
#'
#' #Import already preprocessed ms/ms data from various formats into the specified project  Possible formats (ms, mgf, cef, msp)
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ImportPreprocessedData(var_project_id, ignore_formulas = var_ignore_formulas, allow_ms1_only = var_allow_ms1_only, input_files = var_input_filesdata_file = "result.txt")
#' result <- api_instance$projects_api$ImportPreprocessedData(var_project_id, ignore_formulas = var_ignore_formulas, allow_ms1_only = var_allow_ms1_only, input_files = var_input_files)
#' dput(result)
#'
#'
#' ####################  ImportPreprocessedDataAsJob  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | project-space to import into.
#' var_ignore_formulas <- FALSE # character |  (Optional)
#' var_allow_ms1_only <- TRUE # character |  (Optional)
#' var_opt_fields <- c(JobOptField$new()) # array[JobOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#' var_imput_files <- c(123) # array[data.frame] |  (Optional)
#'
#' #Import ms/ms data from the given format into the specified project-space as background job.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ImportPreprocessedDataAsJob(var_project_id, ignore_formulas = var_ignore_formulas, allow_ms1_only = var_allow_ms1_only, opt_fields = var_opt_fields, imput_files = var_imput_filesdata_file = "result.txt")
#' result <- api_instance$projects_api$ImportPreprocessedDataAsJob(var_project_id, ignore_formulas = var_ignore_formulas, allow_ms1_only = var_allow_ms1_only, opt_fields = var_opt_fields, imput_files = var_imput_files)
#' dput(result)
#'
#'
#' ####################  OpenProjectSpace  ####################
#'
#' library(Rsirius)
#' var_project_id <- "project_id_example" # character | unique name/identifier that shall be used to access the opened project-space. Must consist only of [a-zA-Z0-9_-].
#' var_path_to_project <- "path_to_project_example" # character | local file path to open the project from. If NULL, project will be loaded by it projectId from default project location.  DEPRECATED: This parameter relies on the local filesystem and will likely be removed in later versions of this API to allow for more flexible use cases. (Optional)
#' var_opt_fields <- c(ProjectInfoOptField$new()) # array[ProjectInfoOptField] |  (Optional)
#'
#' #Open an existing project-space and make it accessible via the given projectId.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$OpenProjectSpace(var_project_id, path_to_project = var_path_to_project, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$projects_api$OpenProjectSpace(var_project_id, path_to_project = var_path_to_project, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
ProjectsApi <- R6::R6Class(
  "ProjectsApi",
  public = list(
    api_client = NULL,
    #' Initialize a new ProjectsApi.
    #'
    #' @description
    #' Initialize a new ProjectsApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' Close project-space and remove it from application
    #'
    #' @description
    #' Close project-space and remove it from application
    #'
    #' @param project_id unique name/identifier of the  project-space to be closed.
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    CloseProjectSpace = function(project_id, ...) {
      local_var_response <- self$CloseProjectSpaceWithHttpInfo(project_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Close project-space and remove it from application
    #'
    #' @description
    #' Close project-space and remove it from application
    #'
    #' @param project_id unique name/identifier of the  project-space to be closed.
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    CloseProjectSpaceWithHttpInfo = function(project_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }


      local_var_url_path <- "/api/projects/{projectId}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Move an existing (opened) project-space to another location.
    #'
    #' @description
    #' Move an existing (opened) project-space to another location.
    #'
    #' @param project_id unique name/identifier of the project-space that shall be copied.
    #' @param path_to_copied_project target location where the source project will be copied to.
    #' @param copy_project_id (optional) optional id/mame of the newly created project (copy). If given the project will be opened.
    #' @param opt_fields (optional) No description (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ProjectInfo
    #' @export
    CopyProjectSpace = function(project_id, path_to_copied_project, copy_project_id = NULL, opt_fields = [], data_file = NULL, ...) {
      local_var_response <- self$CopyProjectSpaceWithHttpInfo(project_id, path_to_copied_project, copy_project_id, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Move an existing (opened) project-space to another location.
    #'
    #' @description
    #' Move an existing (opened) project-space to another location.
    #'
    #' @param project_id unique name/identifier of the project-space that shall be copied.
    #' @param path_to_copied_project target location where the source project will be copied to.
    #' @param copy_project_id (optional) optional id/mame of the newly created project (copy). If given the project will be opened.
    #' @param opt_fields (optional) No description (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ProjectInfo) with additional information such as HTTP status code, headers
    #' @export
    CopyProjectSpaceWithHttpInfo = function(project_id, path_to_copied_project, copy_project_id = NULL, opt_fields = [], data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`path_to_copied_project`)) {
        stop("Missing required parameter `path_to_copied_project`.")
      }





      query_params[["pathToCopiedProject"]] <- `path_to_copied_project`

      query_params[["copyProjectId"]] <- `copy_project_id`

      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/copy"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ProjectInfo", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Create and open a new project-space at given location and make it accessible via the given projectId.
    #'
    #' @description
    #' Create and open a new project-space at given location and make it accessible via the given projectId.
    #'
    #' @param project_id unique name/identifier that shall be used to access the newly created project-space. Must consist only of [a-zA-Z0-9_-].
    #' @param path_to_project (optional) local file path where the project will be created. If NULL, project will be stored by its projectId in default project location. DEPRECATED: This parameter relies on the local filesystem and will likely be removed in later versions of this API to allow for more flexible use cases.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ProjectInfo
    #' @export
    CreateProjectSpace = function(project_id, path_to_project = NULL, data_file = NULL, ...) {
      local_var_response <- self$CreateProjectSpaceWithHttpInfo(project_id, path_to_project, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Create and open a new project-space at given location and make it accessible via the given projectId.
    #'
    #' @description
    #' Create and open a new project-space at given location and make it accessible via the given projectId.
    #'
    #' @param project_id unique name/identifier that shall be used to access the newly created project-space. Must consist only of [a-zA-Z0-9_-].
    #' @param path_to_project (optional) local file path where the project will be created. If NULL, project will be stored by its projectId in default project location. DEPRECATED: This parameter relies on the local filesystem and will likely be removed in later versions of this API to allow for more flexible use cases.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ProjectInfo) with additional information such as HTTP status code, headers
    #' @export
    CreateProjectSpaceWithHttpInfo = function(project_id, path_to_project = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }



      query_params[["pathToProject"]] <- `path_to_project`

      local_var_url_path <- "/api/projects/{projectId}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ProjectInfo", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get CANOPUS prediction vector definition for ClassyFire classes
    #'
    #' @description
    #' Get CANOPUS prediction vector definition for ClassyFire classes
    #'
    #' @param project_id 
    #' @param charge 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return character
    #' @export
    GetCanopusClassyFireData = function(project_id, charge, data_file = NULL, ...) {
      local_var_response <- self$GetCanopusClassyFireDataWithHttpInfo(project_id, charge, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get CANOPUS prediction vector definition for ClassyFire classes
    #'
    #' @description
    #' Get CANOPUS prediction vector definition for ClassyFire classes
    #'
    #' @param project_id 
    #' @param charge 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (character) with additional information such as HTTP status code, headers
    #' @export
    GetCanopusClassyFireDataWithHttpInfo = function(project_id, charge, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`charge`)) {
        stop("Missing required parameter `charge`.")
      }



      query_params[["charge"]] <- `charge`

      local_var_url_path <- "/api/projects/{projectId}/cf-data"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/csv")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "character", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get CANOPUS prediction vector definition for NPC classes
    #'
    #' @description
    #' Get CANOPUS prediction vector definition for NPC classes
    #'
    #' @param project_id 
    #' @param charge 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return character
    #' @export
    GetCanopusNpcData = function(project_id, charge, data_file = NULL, ...) {
      local_var_response <- self$GetCanopusNpcDataWithHttpInfo(project_id, charge, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get CANOPUS prediction vector definition for NPC classes
    #'
    #' @description
    #' Get CANOPUS prediction vector definition for NPC classes
    #'
    #' @param project_id 
    #' @param charge 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (character) with additional information such as HTTP status code, headers
    #' @export
    GetCanopusNpcDataWithHttpInfo = function(project_id, charge, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`charge`)) {
        stop("Missing required parameter `charge`.")
      }



      query_params[["charge"]] <- `charge`

      local_var_url_path <- "/api/projects/{projectId}/npc-data"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/csv")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "character", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get CSI:FingerID fingerprint (prediction vector) definition
    #'
    #' @description
    #' Get CSI:FingerID fingerprint (prediction vector) definition
    #'
    #' @param project_id 
    #' @param charge 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return character
    #' @export
    GetFingerIdData = function(project_id, charge, data_file = NULL, ...) {
      local_var_response <- self$GetFingerIdDataWithHttpInfo(project_id, charge, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get CSI:FingerID fingerprint (prediction vector) definition
    #'
    #' @description
    #' Get CSI:FingerID fingerprint (prediction vector) definition
    #'
    #' @param project_id 
    #' @param charge 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (character) with additional information such as HTTP status code, headers
    #' @export
    GetFingerIdDataWithHttpInfo = function(project_id, charge, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`charge`)) {
        stop("Missing required parameter `charge`.")
      }



      query_params[["charge"]] <- `charge`

      local_var_url_path <- "/api/projects/{projectId}/fingerid-data"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/csv")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "character", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Get project space info by its projectId.
    #'
    #' @description
    #' Get project space info by its projectId.
    #'
    #' @param project_id unique name/identifier tof the project-space to be accessed.
    #' @param opt_fields (optional) No description (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ProjectInfo
    #' @export
    GetProjectSpace = function(project_id, opt_fields = [], data_file = NULL, ...) {
      local_var_response <- self$GetProjectSpaceWithHttpInfo(project_id, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Get project space info by its projectId.
    #'
    #' @description
    #' Get project space info by its projectId.
    #'
    #' @param project_id unique name/identifier tof the project-space to be accessed.
    #' @param opt_fields (optional) No description (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ProjectInfo) with additional information such as HTTP status code, headers
    #' @export
    GetProjectSpaceWithHttpInfo = function(project_id, opt_fields = [], data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }



      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ProjectInfo", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' List opened project spaces.
    #'
    #' @description
    #' List opened project spaces.
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[ProjectInfo]
    #' @export
    GetProjectSpaces = function(data_file = NULL, ...) {
      local_var_response <- self$GetProjectSpacesWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' List opened project spaces.
    #'
    #' @description
    #' List opened project spaces.
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[ProjectInfo]) with additional information such as HTTP status code, headers
    #' @export
    GetProjectSpacesWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/api/projects"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "array[ProjectInfo]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Import and Align full MS-Runs from various formats into the specified project  Possible formats (mzML, mzXML)
    #'
    #' @description
    #' Import and Align full MS-Runs from various formats into the specified project  Possible formats (mzML, mzXML)
    #'
    #' @param project_id project-space to import into.
    #' @param align_runs (optional) No description (default value: TRUE)
    #' @param allow_ms1_only (optional) No description (default value: TRUE)
    #' @param input_files (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ImportResult
    #' @export
    ImportMsRunData = function(project_id, align_runs = TRUE, allow_ms1_only = TRUE, input_files = NULL, data_file = NULL, ...) {
      local_var_response <- self$ImportMsRunDataWithHttpInfo(project_id, align_runs, allow_ms1_only, input_files, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Import and Align full MS-Runs from various formats into the specified project  Possible formats (mzML, mzXML)
    #'
    #' @description
    #' Import and Align full MS-Runs from various formats into the specified project  Possible formats (mzML, mzXML)
    #'
    #' @param project_id project-space to import into.
    #' @param align_runs (optional) No description (default value: TRUE)
    #' @param allow_ms1_only (optional) No description (default value: TRUE)
    #' @param input_files (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ImportResult) with additional information such as HTTP status code, headers
    #' @export
    ImportMsRunDataWithHttpInfo = function(project_id, align_runs = TRUE, allow_ms1_only = TRUE, input_files = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }





      query_params[["alignRuns"]] <- `align_runs`

      query_params[["allowMs1Only"]] <- `allow_ms1_only`

      file_params["inputFiles"] <- curl::form_file(`input_files`)
      local_var_url_path <- "/api/projects/{projectId}/import/ms-data-files"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("multipart/form-data")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ImportResult", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Import and Align full MS-Runs from various formats into the specified project as background job.
    #'
    #' @description
    #' Import and Align full MS-Runs from various formats into the specified project as background job.
    #'
    #' @param project_id project-space to import into.
    #' @param align_runs (optional) No description (default value: TRUE)
    #' @param allow_ms1_only (optional) No description (default value: TRUE)
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: ["progress"])
    #' @param input_files (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return Job
    #' @export
    ImportMsRunDataAsJob = function(project_id, align_runs = TRUE, allow_ms1_only = TRUE, opt_fields = ["progress"], input_files = NULL, data_file = NULL, ...) {
      local_var_response <- self$ImportMsRunDataAsJobWithHttpInfo(project_id, align_runs, allow_ms1_only, opt_fields, input_files, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Import and Align full MS-Runs from various formats into the specified project as background job.
    #'
    #' @description
    #' Import and Align full MS-Runs from various formats into the specified project as background job.
    #'
    #' @param project_id project-space to import into.
    #' @param align_runs (optional) No description (default value: TRUE)
    #' @param allow_ms1_only (optional) No description (default value: TRUE)
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: ["progress"])
    #' @param input_files (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (Job) with additional information such as HTTP status code, headers
    #' @export
    ImportMsRunDataAsJobWithHttpInfo = function(project_id, align_runs = TRUE, allow_ms1_only = TRUE, opt_fields = ["progress"], input_files = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }






      query_params[["alignRuns"]] <- `align_runs`

      query_params[["allowMs1Only"]] <- `allow_ms1_only`

      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      file_params["inputFiles"] <- curl::form_file(`input_files`)
      local_var_url_path <- "/api/projects/{projectId}/jobs/import/ms-data-files-job"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("multipart/form-data")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "Job", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Import already preprocessed ms/ms data from various formats into the specified project  Possible formats (ms, mgf, cef, msp)
    #'
    #' @description
    #' Import already preprocessed ms/ms data from various formats into the specified project  Possible formats (ms, mgf, cef, msp)
    #'
    #' @param project_id project-space to import into.
    #' @param ignore_formulas (optional) No description (default value: FALSE)
    #' @param allow_ms1_only (optional) No description (default value: TRUE)
    #' @param input_files (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ImportResult
    #' @export
    ImportPreprocessedData = function(project_id, ignore_formulas = FALSE, allow_ms1_only = TRUE, input_files = NULL, data_file = NULL, ...) {
      local_var_response <- self$ImportPreprocessedDataWithHttpInfo(project_id, ignore_formulas, allow_ms1_only, input_files, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Import already preprocessed ms/ms data from various formats into the specified project  Possible formats (ms, mgf, cef, msp)
    #'
    #' @description
    #' Import already preprocessed ms/ms data from various formats into the specified project  Possible formats (ms, mgf, cef, msp)
    #'
    #' @param project_id project-space to import into.
    #' @param ignore_formulas (optional) No description (default value: FALSE)
    #' @param allow_ms1_only (optional) No description (default value: TRUE)
    #' @param input_files (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ImportResult) with additional information such as HTTP status code, headers
    #' @export
    ImportPreprocessedDataWithHttpInfo = function(project_id, ignore_formulas = FALSE, allow_ms1_only = TRUE, input_files = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }





      query_params[["ignoreFormulas"]] <- `ignore_formulas`

      query_params[["allowMs1Only"]] <- `allow_ms1_only`

      file_params["inputFiles"] <- curl::form_file(`input_files`)
      local_var_url_path <- "/api/projects/{projectId}/import/preprocessed-data-files"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("multipart/form-data")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ImportResult", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Import ms/ms data from the given format into the specified project-space as background job.
    #'
    #' @description
    #' Import ms/ms data from the given format into the specified project-space as background job.
    #'
    #' @param project_id project-space to import into.
    #' @param ignore_formulas (optional) No description (default value: FALSE)
    #' @param allow_ms1_only (optional) No description (default value: TRUE)
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: ["progress"])
    #' @param imput_files (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return Job
    #' @export
    ImportPreprocessedDataAsJob = function(project_id, ignore_formulas = FALSE, allow_ms1_only = TRUE, opt_fields = ["progress"], imput_files = NULL, data_file = NULL, ...) {
      local_var_response <- self$ImportPreprocessedDataAsJobWithHttpInfo(project_id, ignore_formulas, allow_ms1_only, opt_fields, imput_files, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Import ms/ms data from the given format into the specified project-space as background job.
    #'
    #' @description
    #' Import ms/ms data from the given format into the specified project-space as background job.
    #'
    #' @param project_id project-space to import into.
    #' @param ignore_formulas (optional) No description (default value: FALSE)
    #' @param allow_ms1_only (optional) No description (default value: TRUE)
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: ["progress"])
    #' @param imput_files (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (Job) with additional information such as HTTP status code, headers
    #' @export
    ImportPreprocessedDataAsJobWithHttpInfo = function(project_id, ignore_formulas = FALSE, allow_ms1_only = TRUE, opt_fields = ["progress"], imput_files = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }






      query_params[["ignoreFormulas"]] <- `ignore_formulas`

      query_params[["allowMs1Only"]] <- `allow_ms1_only`

      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      file_params["imputFiles"] <- curl::form_file(`imput_files`)
      local_var_url_path <- "/api/projects/{projectId}/import/preprocessed-data-files-job"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("multipart/form-data")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "Job", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Open an existing project-space and make it accessible via the given projectId.
    #'
    #' @description
    #' Open an existing project-space and make it accessible via the given projectId.
    #'
    #' @param project_id unique name/identifier that shall be used to access the opened project-space. Must consist only of [a-zA-Z0-9_-].
    #' @param path_to_project (optional) local file path to open the project from. If NULL, project will be loaded by it projectId from default project location.  DEPRECATED: This parameter relies on the local filesystem and will likely be removed in later versions of this API to allow for more flexible use cases.
    #' @param opt_fields (optional) No description (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return ProjectInfo
    #' @export
    OpenProjectSpace = function(project_id, path_to_project = NULL, opt_fields = [], data_file = NULL, ...) {
      local_var_response <- self$OpenProjectSpaceWithHttpInfo(project_id, path_to_project, opt_fields, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Open an existing project-space and make it accessible via the given projectId.
    #'
    #' @description
    #' Open an existing project-space and make it accessible via the given projectId.
    #'
    #' @param project_id unique name/identifier that shall be used to access the opened project-space. Must consist only of [a-zA-Z0-9_-].
    #' @param path_to_project (optional) local file path to open the project from. If NULL, project will be loaded by it projectId from default project location.  DEPRECATED: This parameter relies on the local filesystem and will likely be removed in later versions of this API to allow for more flexible use cases.
    #' @param opt_fields (optional) No description (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (ProjectInfo) with additional information such as HTTP status code, headers
    #' @export
    OpenProjectSpaceWithHttpInfo = function(project_id, path_to_project = NULL, opt_fields = [], data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }




      query_params[["pathToProject"]] <- `path_to_project`

      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "ProjectInfo", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
