#' SIRIUS Nightsky API
#'
#' REST API that provides the full functionality of SIRIUS and its web services as background service. It is intended as entry-point for scripting languages and software integration SDKs.This API is exposed by SIRIUS 6
#'
#' The version of the OpenAPI document: 3.2
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title Runs operations
#' @description RunsApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @examples
#' \dontrun{
#' ####################  GetRun  ####################
#'
#' library(RSirius)
#' var_project_id <- "project_id_example" # character | project-space to read from.
#' var_run_id <- "run_id_example" # character | identifier of run to access.
#' var_opt_fields <- c(RunOptField$new()) # array[RunOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#'
#' #Get run with the given identifier from the specified project-space.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRun(var_project_id, var_run_id, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$runs_api$GetRun(var_project_id, var_run_id, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetRunsPage  ####################
#'
#' library(RSirius)
#' var_project_id <- "project_id_example" # character | project space to get runs from.
#' var_search_query <- "search_query_example" # character | Optional search query in lucene syntax. Omit this parameter to page over all runs. (Optional)
#' var_page <- 0 # integer | Zero-based page index (0..N) (Optional)
#' var_size <- 20 # integer | The size of the page to be returned (Optional)
#' var_sort <- c("inner_example") # array[character] | Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported. (Optional)
#' var_opt_fields <- c(RunOptField$new()) # array[RunOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#'
#' #Get runs in the given project-space
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRunsPage(var_project_id, search_query = var_search_query, page = var_page, size = var_size, sort = var_sort, opt_fields = var_opt_fieldsdata_file = "result.txt")
#' result <- api_instance$runs_api$GetRunsPage(var_project_id, search_query = var_search_query, page = var_page, size = var_size, sort = var_sort, opt_fields = var_opt_fields)
#' dput(result)
#'
#'
#' ####################  GetRunsSearchableFields  ####################
#'
#' library(RSirius)
#' var_project_id <- "project_id_example" # character | project space to read the searchable run fields from.
#'
#' #Get all fields that can be used in the searchQuery parameter of run endpoints
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRunsSearchableFields(var_project_iddata_file = "result.txt")
#' result <- api_instance$runs_api$GetRunsSearchableFields(var_project_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
RunsApi <- R6::R6Class(
  "RunsApi",
  public = list(
    api_client = NULL,

    #' @description
    #' Initialize a new RunsApi.
    #'
    #' @param api_client An instance of API client.
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },

    #' @description
    #' Get run with the given identifier from the specified project-space.
    #'
    #' @param project_id project-space to read from.
    #' @param run_id identifier of run to access.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return Run
    GetRun = function(project_id, run_id, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetRunWithHttpInfo(project_id, run_id, opt_fields, data_file = data_file, ...)
      if (is.null(local_var_response$status_code)) { # defensive: no status code means the call never reached the API
        local_var_response
      } else if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get run with the given identifier from the specified project-space.
    #'
    #' @param project_id project-space to read from.
    #' @param run_id identifier of run to access.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (Run) with additional information such as HTTP status code, headers
    GetRunWithHttpInfo = function(project_id, run_id, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }

      if (missing(`run_id`)) {
        stop("Missing required parameter `run_id`.")
      }




      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/runs/{runId}"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`run_id`)) {
        local_var_url_path <- gsub("\\{runId\\}", URLencode(as.character(`run_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        # Check if we are expecting a CSV response
        is_csv_response <- any(grepl("csv", local_var_accepts, ignore.case = TRUE))

        if (is_csv_response) {
          # For CSV responses, parse into data.frame
          csv_resp_obj <- tryCatch(
            {
              csv_text <- rawToChar(local_var_resp$response)

              # Detect separator by examining first line
              first_line <- strsplit(csv_text, "\n")[[1]][1]
              if (grepl("\t", first_line)) {
                # Tab-separated (TSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = "\t")
              } else {
                # Comma-separated (CSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = ",")
              }
            },
            error = function(e) {
              stop("Failed to parse CSV response")
            }
          )
          local_var_resp$content <- csv_resp_obj
        } else {
          # For JSON responses, deserialize normally
          deserialized_resp_obj <- tryCatch(
            self$api_client$deserialize(local_var_resp$response_as_text(), "Run", loadNamespace("RSirius")),
            error = function(e) {
              stop("Failed to deserialize response")
            }
          )
          local_var_resp$content <- deserialized_resp_obj
        }
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (length(local_var_resp$response) == 0) {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get runs in the given project-space
    #'
    #' @param project_id project space to get runs from.
    #' @param search_query (optional) Optional search query in lucene syntax. Omit this parameter to page over all runs.
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return PagedModelRun
    GetRunsPage = function(project_id, search_query = NULL, page = 0, size = 20, sort = NULL, opt_fields = list(), data_file = NULL, ...) {
      local_var_response <- self$GetRunsPageWithHttpInfo(project_id, search_query, page, size, sort, opt_fields, data_file = data_file, ...)
      if (is.null(local_var_response$status_code)) { # defensive: no status code means the call never reached the API
        local_var_response
      } else if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get runs in the given project-space
    #'
    #' @param project_id project space to get runs from.
    #' @param search_query (optional) Optional search query in lucene syntax. Omit this parameter to page over all runs.
    #' @param page (optional) Zero-based page index (0..N) (default value: 0)
    #' @param size (optional) The size of the page to be returned (default value: 20)
    #' @param sort (optional) Sorting criteria in the format: property,(asc|desc). Default sort order is ascending. Multiple sort criteria are supported.
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: [])
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (PagedModelRun) with additional information such as HTTP status code, headers
    GetRunsPageWithHttpInfo = function(project_id, search_query = NULL, page = 0, size = 20, sort = NULL, opt_fields = list(), data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }



      if (`page` < 0) {
        stop("Invalid value for `page` when calling RunsApi$GetRunsPage, must be bigger than or equal to 0.")
      }

      if (`size` < 1) {
        stop("Invalid value for `size` when calling RunsApi$GetRunsPage, must be bigger than or equal to 1.")
      }



      query_params[["searchQuery"]] <- `search_query`

      query_params[["page"]] <- `page`

      query_params[["size"]] <- `size`

      # explore
      for (query_item in `sort`) {
        query_params[["sort"]] <- c(query_params[["sort"]], list(`sort` = query_item))
      }

      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      local_var_url_path <- "/api/projects/{projectId}/runs/page"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        # Check if we are expecting a CSV response
        is_csv_response <- any(grepl("csv", local_var_accepts, ignore.case = TRUE))

        if (is_csv_response) {
          # For CSV responses, parse into data.frame
          csv_resp_obj <- tryCatch(
            {
              csv_text <- rawToChar(local_var_resp$response)

              # Detect separator by examining first line
              first_line <- strsplit(csv_text, "\n")[[1]][1]
              if (grepl("\t", first_line)) {
                # Tab-separated (TSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = "\t")
              } else {
                # Comma-separated (CSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = ",")
              }
            },
            error = function(e) {
              stop("Failed to parse CSV response")
            }
          )
          local_var_resp$content <- csv_resp_obj
        } else {
          # For JSON responses, deserialize normally
          deserialized_resp_obj <- tryCatch(
            self$api_client$deserialize(local_var_resp$response_as_text(), "PagedModelRun", loadNamespace("RSirius")),
            error = function(e) {
              stop("Failed to deserialize response")
            }
          )
          local_var_resp$content <- deserialized_resp_obj
        }
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (length(local_var_resp$response) == 0) {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },

    #' @description
    #' Get all fields that can be used in the searchQuery parameter of run endpoints
    #'
    #' @param project_id project space to read the searchable run fields from.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return array[SearchableField]
    GetRunsSearchableFields = function(project_id, data_file = NULL, ...) {
      local_var_response <- self$GetRunsSearchableFieldsWithHttpInfo(project_id, data_file = data_file, ...)
      if (is.null(local_var_response$status_code)) { # defensive: no status code means the call never reached the API
        local_var_response
      } else if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },

    #' @description
    #' Get all fields that can be used in the searchQuery parameter of run endpoints
    #'
    #' @param project_id project space to read the searchable run fields from.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #'
    #' @return API response (array[SearchableField]) with additional information such as HTTP status code, headers
    GetRunsSearchableFieldsWithHttpInfo = function(project_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`project_id`)) {
        stop("Missing required parameter `project_id`.")
      }


      local_var_url_path <- "/api/projects/{projectId}/runs/searchable-fields"
      if (!missing(`project_id`)) {
        local_var_url_path <- gsub("\\{projectId\\}", URLencode(as.character(`project_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json", "application/problem+json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        # Check if we are expecting a CSV response
        is_csv_response <- any(grepl("csv", local_var_accepts, ignore.case = TRUE))

        if (is_csv_response) {
          # For CSV responses, parse into data.frame
          csv_resp_obj <- tryCatch(
            {
              csv_text <- rawToChar(local_var_resp$response)

              # Detect separator by examining first line
              first_line <- strsplit(csv_text, "\n")[[1]][1]
              if (grepl("\t", first_line)) {
                # Tab-separated (TSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = "\t")
              } else {
                # Comma-separated (CSV)
                read.csv(text = csv_text, stringsAsFactors = FALSE, sep = ",")
              }
            },
            error = function(e) {
              stop("Failed to parse CSV response")
            }
          )
          local_var_resp$content <- csv_resp_obj
        } else {
          # For JSON responses, deserialize normally
          deserialized_resp_obj <- tryCatch(
            self$api_client$deserialize(local_var_resp$response_as_text(), "array[SearchableField]", loadNamespace("RSirius")),
            error = function(e) {
              stop("Failed to deserialize response")
            }
          )
          local_var_resp$content <- deserialized_resp_obj
        }
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp, local_var_resp$status_code, local_var_resp$status_code_desc)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (length(local_var_resp$response) == 0) {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
