#' SIRIUS Nightsky API
#'
#' REST API that provides the full functionality of SIRIUS and its web services as background service. It is intended as entry-point for scripting languages and software integration SDKs.This API is exposed by SIRIUS 6
#'
#' The version of the OpenAPI document: 2.1
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title SearchableDatabases operations
#' @description SearchableDatabasesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ AddDatabases } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } request_body list( character )
#' \item \emph{ @returnType } list( \link{SearchableDatabase} ) \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : array[SearchableDatabase]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CreateDatabase } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } database_id character
#' \item \emph{ @param } searchable_database_parameters \link{SearchableDatabaseParameters}
#' \item \emph{ @returnType } \link{SearchableDatabase} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : SearchableDatabase
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetCustomDatabases } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } include_stats character
#' \item \emph{ @returnType } list( \link{SearchableDatabase} ) \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : array[SearchableDatabase]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetDatabase } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } database_id character
#' \item \emph{ @param } include_stats character
#' \item \emph{ @returnType } \link{SearchableDatabase} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : SearchableDatabase
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetDatabases } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } include_stats character
#' \item \emph{ @returnType } list( \link{SearchableDatabase} ) \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : array[SearchableDatabase]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetIncludedDatabases } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } include_stats character
#' \item \emph{ @returnType } list( \link{SearchableDatabase} ) \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : array[SearchableDatabase]
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ImportIntoDatabase } \emph{ Start import of structure and spectra files into the specified database. }
#' Start import of structure and spectra files into the specified database.
#'
#' \itemize{
#' \item \emph{ @param } database_id character
#' \item \emph{ @param } buffer_size integer
#' \item \emph{ @param } input_files list( data.frame )
#' \item \emph{ @returnType } \link{SearchableDatabase} \cr
#'
#'
#' \item status code : 200 | Job of the import command to be executed.
#'
#' \item return type : SearchableDatabase
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ImportIntoDatabaseAsJob } \emph{ Start import of structure and spectra files into the specified database. }
#' Start import of structure and spectra files into the specified database.
#'
#' \itemize{
#' \item \emph{ @param } database_id character
#' \item \emph{ @param } buffer_size integer
#' \item \emph{ @param } opt_fields list( \link{JobOptField} )
#' \item \emph{ @param } input_files list( data.frame )
#' \item \emph{ @returnType } \link{Job} \cr
#'
#'
#' \item status code : 200 | Job of the import command to be executed.
#'
#' \item return type : Job
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ RemoveDatabase } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } database_id character
#' \item \emph{ @param } delete character
#'
#'
#' \item status code : 200 | OK
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ UpdateDatabase } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } database_id character
#' \item \emph{ @param } searchable_database_parameters \link{SearchableDatabaseParameters}
#' \item \emph{ @returnType } \link{SearchableDatabase} \cr
#'
#'
#' \item status code : 200 | OK
#'
#' \item return type : SearchableDatabase
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  AddDatabases  ####################
#'
#' library(Rsirius)
#' var_request_body <- c("property_example") # array[character] | 
#'
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$AddDatabases(var_request_bodydata_file = "result.txt")
#' result <- api_instance$searchable_databases_api$AddDatabases(var_request_body)
#' dput(result)
#'
#'
#' ####################  CreateDatabase  ####################
#'
#' library(Rsirius)
#' var_database_id <- "database_id_example" # character | 
#' var_searchable_database_parameters <- SearchableDatabaseParameters$new("displayName_example", "location_example", "matchRtOfReferenceSpectra_example") # SearchableDatabaseParameters |  (Optional)
#'
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$CreateDatabase(var_database_id, searchable_database_parameters = var_searchable_database_parametersdata_file = "result.txt")
#' result <- api_instance$searchable_databases_api$CreateDatabase(var_database_id, searchable_database_parameters = var_searchable_database_parameters)
#' dput(result)
#'
#'
#' ####################  GetCustomDatabases  ####################
#'
#' library(Rsirius)
#' var_include_stats <- FALSE # character |  (Optional)
#'
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetCustomDatabases(include_stats = var_include_statsdata_file = "result.txt")
#' result <- api_instance$searchable_databases_api$GetCustomDatabases(include_stats = var_include_stats)
#' dput(result)
#'
#'
#' ####################  GetDatabase  ####################
#'
#' library(Rsirius)
#' var_database_id <- "database_id_example" # character | 
#' var_include_stats <- TRUE # character |  (Optional)
#'
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetDatabase(var_database_id, include_stats = var_include_statsdata_file = "result.txt")
#' result <- api_instance$searchable_databases_api$GetDatabase(var_database_id, include_stats = var_include_stats)
#' dput(result)
#'
#'
#' ####################  GetDatabases  ####################
#'
#' library(Rsirius)
#' var_include_stats <- FALSE # character |  (Optional)
#'
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetDatabases(include_stats = var_include_statsdata_file = "result.txt")
#' result <- api_instance$searchable_databases_api$GetDatabases(include_stats = var_include_stats)
#' dput(result)
#'
#'
#' ####################  GetIncludedDatabases  ####################
#'
#' library(Rsirius)
#' var_include_stats <- FALSE # character |  (Optional)
#'
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetIncludedDatabases(include_stats = var_include_statsdata_file = "result.txt")
#' result <- api_instance$searchable_databases_api$GetIncludedDatabases(include_stats = var_include_stats)
#' dput(result)
#'
#'
#' ####################  ImportIntoDatabase  ####################
#'
#' library(Rsirius)
#' var_database_id <- "database_id_example" # character | database to import into
#' var_buffer_size <- 1000 # integer |  (Optional)
#' var_input_files <- c(123) # array[data.frame] |  (Optional)
#'
#' #Start import of structure and spectra files into the specified database.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ImportIntoDatabase(var_database_id, buffer_size = var_buffer_size, input_files = var_input_filesdata_file = "result.txt")
#' result <- api_instance$searchable_databases_api$ImportIntoDatabase(var_database_id, buffer_size = var_buffer_size, input_files = var_input_files)
#' dput(result)
#'
#'
#' ####################  ImportIntoDatabaseAsJob  ####################
#'
#' library(Rsirius)
#' var_database_id <- "database_id_example" # character | database to import into
#' var_buffer_size <- 1000 # integer |  (Optional)
#' var_opt_fields <- c(JobOptField$new()) # array[JobOptField] | set of optional fields to be included. Use 'none' only to override defaults. (Optional)
#' var_input_files <- c(123) # array[data.frame] |  (Optional)
#'
#' #Start import of structure and spectra files into the specified database.
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$ImportIntoDatabaseAsJob(var_database_id, buffer_size = var_buffer_size, opt_fields = var_opt_fields, input_files = var_input_filesdata_file = "result.txt")
#' result <- api_instance$searchable_databases_api$ImportIntoDatabaseAsJob(var_database_id, buffer_size = var_buffer_size, opt_fields = var_opt_fields, input_files = var_input_files)
#' dput(result)
#'
#'
#' ####################  RemoveDatabase  ####################
#'
#' library(Rsirius)
#' var_database_id <- "database_id_example" # character | 
#' var_delete <- FALSE # character |  (Optional)
#'
#' api_instance <- rsirius_api$new()
#'
#' api_instance$searchable_databases_api$RemoveDatabase(var_database_id, delete = var_delete)
#'
#'
#' ####################  UpdateDatabase  ####################
#'
#' library(Rsirius)
#' var_database_id <- "database_id_example" # character | 
#' var_searchable_database_parameters <- SearchableDatabaseParameters$new("displayName_example", "location_example", "matchRtOfReferenceSpectra_example") # SearchableDatabaseParameters |  (Optional)
#'
#' api_instance <- rsirius_api$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$UpdateDatabase(var_database_id, searchable_database_parameters = var_searchable_database_parametersdata_file = "result.txt")
#' result <- api_instance$searchable_databases_api$UpdateDatabase(var_database_id, searchable_database_parameters = var_searchable_database_parameters)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
SearchableDatabasesApi <- R6::R6Class(
  "SearchableDatabasesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new SearchableDatabasesApi.
    #'
    #' @description
    #' Initialize a new SearchableDatabasesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param request_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SearchableDatabase]
    #' @export
    AddDatabases = function(request_body, data_file = NULL, ...) {
      local_var_response <- self$AddDatabasesWithHttpInfo(request_body, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param request_body 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SearchableDatabase]) with additional information such as HTTP status code, headers
    #' @export
    AddDatabasesWithHttpInfo = function(request_body, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`request_body`)) {
        stop("Missing required parameter `request_body`.")
      }


      if (!is.null(`request_body`)) {
        body.items <- paste(unlist(lapply(`request_body`, function(param) {
                                                             param$toJSONString()
                                                         })), collapse = ",")
        local_var_body <- paste0("[", body.items, "]")
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/databases"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "array[SearchableDatabase]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param database_id 
    #' @param searchable_database_parameters (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return SearchableDatabase
    #' @export
    CreateDatabase = function(database_id, searchable_database_parameters = NULL, data_file = NULL, ...) {
      local_var_response <- self$CreateDatabaseWithHttpInfo(database_id, searchable_database_parameters, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param database_id 
    #' @param searchable_database_parameters (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (SearchableDatabase) with additional information such as HTTP status code, headers
    #' @export
    CreateDatabaseWithHttpInfo = function(database_id, searchable_database_parameters = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`database_id`)) {
        stop("Missing required parameter `database_id`.")
      }

      if (!str_detect(`database_id`, "^[a-zA-Z0-9-_]+$")) {
        stop("Invalid value for `database_id` when calling SearchableDatabasesApi$CreateDatabase, must conform to the pattern ^[a-zA-Z0-9-_]+$.")
      }


      if (!is.null(`searchable_database_parameters`)) {
        local_var_body <- `searchable_database_parameters`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/databases/{databaseId}"
      if (!missing(`database_id`)) {
        local_var_url_path <- gsub("\\{databaseId\\}", URLencode(as.character(`database_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "SearchableDatabase", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param include_stats (optional) No description (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SearchableDatabase]
    #' @export
    GetCustomDatabases = function(include_stats = FALSE, data_file = NULL, ...) {
      local_var_response <- self$GetCustomDatabasesWithHttpInfo(include_stats, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param include_stats (optional) No description (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SearchableDatabase]) with additional information such as HTTP status code, headers
    #' @export
    GetCustomDatabasesWithHttpInfo = function(include_stats = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      query_params[["includeStats"]] <- `include_stats`

      local_var_url_path <- "/api/databases/custom"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "array[SearchableDatabase]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param database_id 
    #' @param include_stats (optional) No description (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return SearchableDatabase
    #' @export
    GetDatabase = function(database_id, include_stats = TRUE, data_file = NULL, ...) {
      local_var_response <- self$GetDatabaseWithHttpInfo(database_id, include_stats, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param database_id 
    #' @param include_stats (optional) No description (default value: TRUE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (SearchableDatabase) with additional information such as HTTP status code, headers
    #' @export
    GetDatabaseWithHttpInfo = function(database_id, include_stats = TRUE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`database_id`)) {
        stop("Missing required parameter `database_id`.")
      }



      query_params[["includeStats"]] <- `include_stats`

      local_var_url_path <- "/api/databases/{databaseId}"
      if (!missing(`database_id`)) {
        local_var_url_path <- gsub("\\{databaseId\\}", URLencode(as.character(`database_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "SearchableDatabase", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param include_stats (optional) No description (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SearchableDatabase]
    #' @export
    GetDatabases = function(include_stats = FALSE, data_file = NULL, ...) {
      local_var_response <- self$GetDatabasesWithHttpInfo(include_stats, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param include_stats (optional) No description (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SearchableDatabase]) with additional information such as HTTP status code, headers
    #' @export
    GetDatabasesWithHttpInfo = function(include_stats = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      query_params[["includeStats"]] <- `include_stats`

      local_var_url_path <- "/api/databases"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "array[SearchableDatabase]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param include_stats (optional) No description (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return array[SearchableDatabase]
    #' @export
    GetIncludedDatabases = function(include_stats = FALSE, data_file = NULL, ...) {
      local_var_response <- self$GetIncludedDatabasesWithHttpInfo(include_stats, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param include_stats (optional) No description (default value: FALSE)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (array[SearchableDatabase]) with additional information such as HTTP status code, headers
    #' @export
    GetIncludedDatabasesWithHttpInfo = function(include_stats = FALSE, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      query_params[["includeStats"]] <- `include_stats`

      local_var_url_path <- "/api/databases/included"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "array[SearchableDatabase]", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Start import of structure and spectra files into the specified database.
    #'
    #' @description
    #' Start import of structure and spectra files into the specified database.
    #'
    #' @param database_id database to import into
    #' @param buffer_size (optional) No description (default value: 1000)
    #' @param input_files (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return SearchableDatabase
    #' @export
    ImportIntoDatabase = function(database_id, buffer_size = 1000, input_files = NULL, data_file = NULL, ...) {
      local_var_response <- self$ImportIntoDatabaseWithHttpInfo(database_id, buffer_size, input_files, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Start import of structure and spectra files into the specified database.
    #'
    #' @description
    #' Start import of structure and spectra files into the specified database.
    #'
    #' @param database_id database to import into
    #' @param buffer_size (optional) No description (default value: 1000)
    #' @param input_files (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (SearchableDatabase) with additional information such as HTTP status code, headers
    #' @export
    ImportIntoDatabaseWithHttpInfo = function(database_id, buffer_size = 1000, input_files = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`database_id`)) {
        stop("Missing required parameter `database_id`.")
      }




      query_params[["bufferSize"]] <- `buffer_size`

      file_params[["inputFiles"]] <- curl::form_file(`input_files`)
      local_var_url_path <- "/api/databases/{databaseId}/import/from-files"
      if (!missing(`database_id`)) {
        local_var_url_path <- gsub("\\{databaseId\\}", URLencode(as.character(`database_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("multipart/form-data")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "SearchableDatabase", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' Start import of structure and spectra files into the specified database.
    #'
    #' @description
    #' Start import of structure and spectra files into the specified database.
    #'
    #' @param database_id database to import into
    #' @param buffer_size (optional) No description (default value: 1000)
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: ["progress"])
    #' @param input_files (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return Job
    #' @export
    ImportIntoDatabaseAsJob = function(database_id, buffer_size = 1000, opt_fields = list("progress"), input_files = NULL, data_file = NULL, ...) {
      local_var_response <- self$ImportIntoDatabaseAsJobWithHttpInfo(database_id, buffer_size, opt_fields, input_files, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' Start import of structure and spectra files into the specified database.
    #'
    #' @description
    #' Start import of structure and spectra files into the specified database.
    #'
    #' @param database_id database to import into
    #' @param buffer_size (optional) No description (default value: 1000)
    #' @param opt_fields (optional) set of optional fields to be included. Use 'none' only to override defaults. (default value: ["progress"])
    #' @param input_files (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (Job) with additional information such as HTTP status code, headers
    #' @export
    ImportIntoDatabaseAsJobWithHttpInfo = function(database_id, buffer_size = 1000, opt_fields = list("progress"), input_files = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`database_id`)) {
        stop("Missing required parameter `database_id`.")
      }





      query_params[["bufferSize"]] <- `buffer_size`

      # explore
      for (query_item in `opt_fields`) {
        query_params[["optFields"]] <- c(query_params[["optFields"]], list(`optFields` = query_item))
      }

      file_params[["inputFiles"]] <- curl::form_file(`input_files`)
      local_var_url_path <- "/api/databases/{databaseId}/import/from-files-job"
      if (!missing(`database_id`)) {
        local_var_url_path <- gsub("\\{databaseId\\}", URLencode(as.character(`database_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("multipart/form-data")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "Job", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param database_id 
    #' @param delete (optional) No description (default value: FALSE)
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    RemoveDatabase = function(database_id, delete = FALSE, ...) {
      local_var_response <- self$RemoveDatabaseWithHttpInfo(database_id, delete, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param database_id 
    #' @param delete (optional) No description (default value: FALSE)
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    RemoveDatabaseWithHttpInfo = function(database_id, delete = FALSE, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`database_id`)) {
        stop("Missing required parameter `database_id`.")
      }



      query_params[["delete"]] <- `delete`

      local_var_url_path <- "/api/databases/{databaseId}"
      if (!missing(`database_id`)) {
        local_var_url_path <- gsub("\\{databaseId\\}", URLencode(as.character(`database_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param database_id 
    #' @param searchable_database_parameters (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return SearchableDatabase
    #' @export
    UpdateDatabase = function(database_id, searchable_database_parameters = NULL, data_file = NULL, ...) {
      local_var_response <- self$UpdateDatabaseWithHttpInfo(database_id, searchable_database_parameters, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param database_id 
    #' @param searchable_database_parameters (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (SearchableDatabase) with additional information such as HTTP status code, headers
    #' @export
    UpdateDatabaseWithHttpInfo = function(database_id, searchable_database_parameters = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`database_id`)) {
        stop("Missing required parameter `database_id`.")
      }



      if (!is.null(`searchable_database_parameters`)) {
        local_var_body <- `searchable_database_parameters`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/api/databases/{databaseId}"
      if (!missing(`database_id`)) {
        local_var_url_path <- gsub("\\{databaseId\\}", URLencode(as.character(`database_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "SearchableDatabase", loadNamespace("Rsirius")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
