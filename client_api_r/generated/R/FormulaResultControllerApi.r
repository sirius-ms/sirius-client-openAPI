# Sirius Nightsky Middleware API
#
# Sirius Nightsky Middleware API
#
# OpenAPI spec version: 0.9
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' @title FormulaResultController operations
#' @description swagger.FormulaResultController
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' get_fingerprint_using_get getFingerprint
#'
#'
#' get_formula_ids_using_get getFormulaIds
#'
#'
#' get_formula_result_scores_using_get getFormulaResultScores
#'
#'
#' get_formula_result_using_get getFormulaResult
#'
#'
#' get_fragmentation_tree_using_get getFragmentationTree
#'
#'
#' get_structure_candidates_using_get getStructureCandidates
#'
#'
#' get_top_hit_candidate_using_get getTopHitCandidate
#'
#' }
#'
#' @export
FormulaResultControllerApi <- R6::R6Class(
  'FormulaResultControllerApi',
  public = list(
    userAgent = "Swagger-Codegen/1.0.0/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    get_fingerprint_using_get = function(cid, fid, pid, as_deterministic, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`as_deterministic`)) {
        queryParams['asDeterministic'] <- as_deterministic
      }

      urlPath <- "/api/projects/{pid}/compounds/{cid}/formulas/{fid}/fingerprint"
      if (!missing(`cid`)) {
        urlPath <- gsub(paste0("\\{", "cid", "\\}"), `cid`, urlPath)
      }

      if (!missing(`fid`)) {
        urlPath <- gsub(paste0("\\{", "fid", "\\}"), `fid`, urlPath)
      }

      if (!missing(`pid`)) {
        urlPath <- gsub(paste0("\\{", "pid", "\\}"), `pid`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Numeric$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_formula_ids_using_get = function(cid, pid, include_formula_scores, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`include_formula_scores`)) {
        queryParams['includeFormulaScores'] <- include_formula_scores
      }

      urlPath <- "/api/projects/{pid}/compounds/{cid}/formulas"
      if (!missing(`cid`)) {
        urlPath <- gsub(paste0("\\{", "cid", "\\}"), `cid`, urlPath)
      }

      if (!missing(`pid`)) {
        urlPath <- gsub(paste0("\\{", "pid", "\\}"), `pid`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- FormulaId$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_formula_result_scores_using_get = function(cid, fid, pid, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/api/projects/{pid}/compounds/{cid}/formulas/{fid}/scores"
      if (!missing(`cid`)) {
        urlPath <- gsub(paste0("\\{", "cid", "\\}"), `cid`, urlPath)
      }

      if (!missing(`fid`)) {
        urlPath <- gsub(paste0("\\{", "fid", "\\}"), `fid`, urlPath)
      }

      if (!missing(`pid`)) {
        urlPath <- gsub(paste0("\\{", "pid", "\\}"), `pid`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- FormulaResultScores$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_formula_result_using_get = function(cid, fid, pid, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/api/projects/{pid}/compounds/{cid}/formulas/{fid}"
      if (!missing(`cid`)) {
        urlPath <- gsub(paste0("\\{", "cid", "\\}"), `cid`, urlPath)
      }

      if (!missing(`fid`)) {
        urlPath <- gsub(paste0("\\{", "fid", "\\}"), `fid`, urlPath)
      }

      if (!missing(`pid`)) {
        urlPath <- gsub(paste0("\\{", "pid", "\\}"), `pid`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- FormulaId$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_fragmentation_tree_using_get = function(cid, fid, pid, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/api/projects/{pid}/compounds/{cid}/formulas/{fid}/tree"
      if (!missing(`cid`)) {
        urlPath <- gsub(paste0("\\{", "cid", "\\}"), `cid`, urlPath)
      }

      if (!missing(`fid`)) {
        urlPath <- gsub(paste0("\\{", "fid", "\\}"), `fid`, urlPath)
      }

      if (!missing(`pid`)) {
        urlPath <- gsub(paste0("\\{", "pid", "\\}"), `pid`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Character$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_structure_candidates_using_get = function(cid, fid, pid, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/api/projects/{pid}/compounds/{cid}/formulas/{fid}/candidates"
      if (!missing(`cid`)) {
        urlPath <- gsub(paste0("\\{", "cid", "\\}"), `cid`, urlPath)
      }

      if (!missing(`fid`)) {
        urlPath <- gsub(paste0("\\{", "fid", "\\}"), `fid`, urlPath)
      }

      if (!missing(`pid`)) {
        urlPath <- gsub(paste0("\\{", "pid", "\\}"), `pid`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Character$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_top_hit_candidate_using_get = function(cid, pid, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/api/projects/{pid}/compounds/{cid}/formulas/topHitCandidate"
      if (!missing(`cid`)) {
        urlPath <- gsub(paste0("\\{", "cid", "\\}"), `cid`, urlPath)
      }

      if (!missing(`pid`)) {
        urlPath <- gsub(paste0("\\{", "pid", "\\}"), `pid`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Character$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
  )
)
